---
phase: 02-core-ux-navigation
plan: 02-01
type: standard
wave: 1
depends_on: []
autonomous: true
files_modified:
  - site/src/*.html
  - site/src/*.css
  - site/templates/base.html
  - site/templates/partials/*.html
  - site/public/*
---

<objective>
Remove all student-project HTML/CSS/JS from `site/src` and reauthor clean-slate page stubs and templates.
Purpose: Ensure licensing compliance and a fresh foundation for Phase 2 layout work.
Output: Minimal Tera template shell and nine page stubs that build successfully.
</objective>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/REQUIREMENTS.md
@.gsd/phases/02-core-ux-navigation/02-RESEARCH.md
@site/README.md
@site/src/
@site/templates/
</context>

<tasks>
  <task type="auto">
    <name>Remove student-project HTML/CSS/JS from site/src</name>
    <files>site/src/*.html, site/src/*.css, site/src/*.js (if present)</files>
    <action>
      Delete existing page HTML/CSS/JS files in `site/src` that originated from the student project.
      Keep the directory structure, then create fresh minimal stubs (see next task) with no copied markup.
    </action>
    <verify>
      `site/src` contains only newly authored stub files; no legacy markup remains.
    </verify>
    <done>Student-project files removed and replaced with clean-slate versions.</done>
  </task>

  <task type="auto">
    <name>Create fresh base template and partial placeholders</name>
    <files>site/templates/base.html, site/templates/partials/{header,footer}.html</files>
    <action>
      Author a new `base.html` with minimal semantic structure and empty blocks for header, main, and footer.
      Create placeholder partials for header and footer (no navigation or styling yet).
    </action>
    <verify>
      Base template renders with empty placeholders and no copied layout.
    </verify>
    <done>Clean template shell exists for all page stubs.</done>
  </task>

  <task type="auto">
    <name>Create nine clean-slate page stubs</name>
    <files>site/src/index.html, preise.html, nachhaltig.html, fahrzeuge.html, geschaeftskunden.html, ueber-uns.html, mitglied-werden.html, datenschutz.html, impressum.html</files>
    <action>
      Create minimal stub pages that extend `base.html`, set a unique title, and include a simple placeholder heading per page.
      Do not add phone number CTA yet.
    </action>
    <verify>
      Each page renders and contains a unique H1 placeholder only.
    </verify>
    <done>All required pages exist as minimal stubs.</done>
  </task>

  <task type="auto">
    <name>Confirm build output from clean-slate stubs</name>
    <files>site/build/dist/*</files>
    <action>Run `npm run build` from `site/` to ensure the new stubs compile.</action>
    <verify>`site/build/dist` contains rendered HTML for all nine pages.</verify>
    <done>Clean-slate stubs build successfully.</done>
  </task>
</tasks>

<verification>
- `cd site && npm run build`
- Verify `site/build/dist/index.html` and all page outputs exist
</verification>

<must_haves>
- Student-project markup removed from `site/src`
- Base template and page stubs reauthored from scratch
- All nine pages render without copied layout
</must_haves>

<success_criteria>
- Clean template shell and stubs exist
- Build completes without errors
- No student-project HTML/CSS remains in `site/src`
</success_criteria>

<output>
Plan produces a clean-slate template foundation and page stubs ready for layout work.
</output>
