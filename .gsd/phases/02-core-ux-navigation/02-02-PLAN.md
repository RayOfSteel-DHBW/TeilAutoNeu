---
phase: 02-core-ux-navigation
plan: 02-02
type: standard
wave: 2
depends_on:
  - 02-01
autonomous: true
files_modified:
  - site/templates/base.html
  - site/templates/partials/header.html
  - site/templates/partials/footer.html
  - site/public/js/nav.js
  - site/src/base.css
  - site/src/tailwind.css
---

<objective>
Implement the responsive base layout and navigation shell for mobile and desktop.
Purpose: Provide a usable site structure with a hamburger nav for small screens.
Output: Accessible header/nav, responsive layout, and basic footer structure.
</objective>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/REQUIREMENTS.md
@.gsd/phases/02-core-ux-navigation/02-RESEARCH.md
@site/templates/base.html
@site/src/base.css
@site/src/tailwind.css
</context>

<tasks>
  <task type="auto">
    <name>Build responsive layout shell</name>
    <files>site/templates/base.html, site/templates/partials/footer.html</files>
    <action>
      Implement semantic layout with header, main, and footer in `base.html`.
      Add a responsive container/grid system using Tailwind classes, with consistent spacing and max width.
      Add a simple footer shell with placeholders for address and legal links.
    </action>
    <verify>Layout renders on desktop and mobile without horizontal scroll.</verify>
    <done>Responsive layout shell is in place.</done>
  </task>

  <task type="auto">
    <name>Implement accessible hamburger navigation</name>
    <files>site/templates/partials/header.html, site/public/js/nav.js</files>
    <action>
      Create a header with logo text and primary nav links.
      Add a hamburger button for small screens with `aria-controls` and `aria-expanded`.
      Implement a minimal JS toggle that opens/closes the nav and updates aria state.
      Do not place the phone number in the global header.
    </action>
    <verify>
      On small screens, the nav toggles open/closed and keyboard focus works.
    </verify>
    <done>Hamburger navigation works on mobile.</done>
  </task>

  <task type="auto">
    <name>Wire base styles for layout primitives</name>
    <files>site/src/base.css, site/src/tailwind.css</files>
    <action>
      Add minimal base styles for body background, text color defaults, and focus states.
      Keep styling neutral; brand palette arrives in 02-03.
    </action>
    <verify>Base styles render without layout regressions.</verify>
    <done>Layout primitives are styled for usability.</done>
  </task>

  <task type="auto">
    <name>Verify responsive navigation in build output</name>
    <files>site/build/dist/*</files>
    <action>Run `npm run build` and open `build/dist/index.html` to check nav behavior.</action>
    <verify>Hamburger toggles on mobile width and nav renders on desktop width.</verify>
    <done>Responsive navigation verified in built output.</done>
  </task>
</tasks>

<verification>
- `cd site && npm run build`
- Open `site/build/dist/index.html` at mobile and desktop widths
</verification>

<must_haves>
- Hamburger navigation exists and is accessible
- Base layout is mobile-first and responsive
- Global header omits phone number
</must_haves>

<success_criteria>
- Navigation works on small screens with JS toggle
- Layout shell is consistent across pages
- Build output renders header, main, footer
</success_criteria>

<output>
Plan delivers the navigation shell and responsive layout foundation.
</output>
