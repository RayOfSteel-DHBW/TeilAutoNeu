---
phase: 05-fleet-locations
plan: 05-03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - site/src/base.css
  - site/src/fahrzeuge.html
  - site/public/js/fleet-map.js
autonomous: true
must_haves:
  truths:
    - OSM attribution is visible on the map at all breakpoints.
    - Map container has a fixed height so it renders reliably.
    - Planned marker styling is visually distinct from active locations.
  artifacts:
    - site/src/base.css
    - site/public/js/fleet-map.js
  key_links:
    - MapLibre attribution control is enabled and not hidden by CSS.
---

<objective>
Polish the fleet map UI and ensure OSM attribution compliance.
Purpose: Make the map reliable, readable, and license-compliant across devices.
Output: Styled markers, stable map layout, and always-visible attribution.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.gsd/templates/summary.md
</execution_context>

<context>
@.gsd/PROJECT.md
@.gsd/ROADMAP.md
@.gsd/REQUIREMENTS.md
@.gsd/phases/05-fleet-locations/05-CONTEXT.md
@site/src/base.css
@site/src/fahrzeuge.html
@site/public/js/fleet-map.js
</context>

<tasks>
  <task type="auto">
    <name>Style map container, markers, and popups</name>
    <files>site/src/base.css</files>
    <action>
      Add CSS for:
      - .fleet-map container height (e.g., 360-520px responsive range)
      - .fleet-marker button styling and focus states
      - .fleet-marker--planned variant (outline or muted color)
      - .fleet-popup layout (icon + text)
      Keep styles minimal and aligned with brand colors.
    </action>
    <verify>base.css defines .fleet-map, .fleet-marker, .fleet-popup styles.</verify>
    <done>Map UI renders reliably and markers are distinct.</done>
  </task>

  <task type="auto">
    <name>Ensure attribution and interaction polish</name>
    <files>site/public/js/fleet-map.js, site/src/fahrzeuge.html</files>
    <action>
      Update fleet-map.js to:
      - Use a non-compact attribution control
      - Disable scroll zoom by default, enable on map focus if needed
      - Keep planned marker class in sync with CSS naming (.fleet-marker--planned)
      Adjust fahrzeuge.html text to mention that the map uses OpenStreetMap data if needed.
    </action>
    <verify>Attribution control is enabled and planned marker class matches CSS.</verify>
    <done>Attribution is visible and map interactions are user-friendly.</done>
  </task>

  <task type="auto">
    <name>Verify attribution and layout in build output</name>
    <files>site/build/dist/fahrzeuge.html, site/build/dist/base.css, site/build/dist/js/fleet-map.js</files>
    <action>Run `cd site && npm run build` and confirm the built map includes attribution, marker styles, and a fixed height.</action>
    <verify>Built CSS includes fleet-map styles and map JS includes attribution control.</verify>
    <done>Map polish and attribution compliance are present in the static build.</done>
  </task>
</tasks>

<verification>
- `cd site && npm run build`
- Review build/dist/fahrzeuge.html to confirm attribution container renders
</verification>

<success_criteria>

- OSM attribution is visible in the map UI at all sizes
- Map container has reliable height and markers are readable
  </success_criteria>

<output>
After completion, create `.gsd/phases/05-fleet-locations/05-03-SUMMARY.md`
</output>
