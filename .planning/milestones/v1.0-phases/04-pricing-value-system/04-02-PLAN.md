---
phase: 04-pricing-value-system
plan: 04-02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - site/src/preise.html
  - site/public/js/pricing.js
autonomous: true
must_haves:
  truths:
    - Preise page renders pricing values and examples from pricing.json at runtime.
    - Only XS and M classes are displayed, with Quernutzung referenced separately.
  artifacts:
    - site/src/preise.html
    - site/public/js/pricing.js
  key_links:
    - preise.html loads pricing.js and pricing.js fetches ./data/pricing.json.
---

<objective>
Render pricing values and sample calculations from JSON on the Preise page.
Purpose: Make pricing content data-driven without changing the build pipeline.
Output: Preise page layout with placeholders and a JS renderer for pricing.json.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.planning/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-pricing-value-system/04-RESEARCH.md
@site/src/preise.html
@site/templates/base.html
</context>

<tasks>
  <task type="auto">
    <name>Build Preise page structure for data-driven pricing</name>
    <files>site/src/preise.html</files>
    <action>
      Replace the placeholder page with a full layout that includes:
      - H1 "Preise" and a short lead-in paragraph (no exact prices)
      - A container with IDs for dynamic sections:
        #pricing-values, #pricing-classes, #pricing-examples, #pricing-disclaimer, #pricing-quernutzung
      - Keep the disclaimer section present but not the first block
      - Add a script tag for js/pricing.js via a head or main block
      Use semantic sections and Tailwind spacing utilities consistent with other pages.
    </action>
    <verify>preise.html contains all pricing section IDs and loads js/pricing.js.</verify>
    <done>Preise page has the layout scaffolding for runtime pricing content.</done>
  </task>

  <task type="auto">
    <name>Create pricing JSON renderer</name>
    <files>site/public/js/pricing.js</files>
    <action>
      Create pricing.js to:
      - Fetch ./data/pricing.json (relative path for GitHub Pages)
      - Populate the pricing section IDs with values from JSON
      - Render membership deposit + annual fee, usage items, class list, examples, disclaimer, and quernutzung
      - Show a simple fallback message if fetch fails
      Use DOM APIs (createElement/textContent) to avoid injecting raw HTML.
    </action>
    <verify>pricing.js fetches ./data/pricing.json and populates all target IDs.</verify>
    <done>Preise page can render pricing data at runtime from JSON.</done>
  </task>

  <task type="auto">
    <name>Verify Preise build output</name>
    <files>site/build/dist/preise.html</files>
    <action>Run `cd site && npm run build` and confirm build/dist/preise.html includes the new layout and pricing.js reference.</action>
    <verify>Built Preise page has the pricing containers and the js/pricing.js script tag.</verify>
    <done>Static output contains the runtime pricing scaffolding.</done>
  </task>
</tasks>

<verification>
- `cd site && npm run build`
- Review build/dist/preise.html for pricing containers and js/pricing.js reference
</verification>

<success_criteria>

- Preise page renders JSON-backed pricing sections at runtime
- Pricing examples appear via JSON without exact price values in HTML
  </success_criteria>

<output>
After completion, create `.planning/phases/04-pricing-value-system/04-02-SUMMARY.md`
</output>
