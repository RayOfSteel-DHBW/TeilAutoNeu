---
phase: 04-pricing-value-system
plan: 04-01
type: execute
wave: 1
depends_on:
  - 03-03
files_modified:
  - site/public/data/pricing.json
  - site/README.md
autonomous: true
must_haves:
  truths:
    - Pricing data exists as abstract values in JSON and only includes XS and M classes.
    - Owners can update pricing by editing JSON without touching HTML.
  artifacts:
    - site/public/data/pricing.json
    - site/README.md
  key_links:
    - pricing.json is under public/ so npm build copies it into build/dist/data/pricing.json.
---

<objective>
Create the pricing JSON source and document the owner update workflow.
Purpose: Make pricing data maintainable without editing templates or HTML.
Output: A public pricing.json with abstract values and a README update note.
</objective>

<execution_context>
../skills/execute-plan/SKILL.md
@.planning/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-pricing-value-system/04-RESEARCH.md
@site/README.md
</context>

<tasks>
  <task type="auto">
    <name>Create pricing JSON data source</name>
    <files>site/public/data/pricing.json</files>
    <action>
      Create site/public/data/pricing.json with the abstracted schema:
      - updated (YYYY-MM-DD), disclaimer, membership (deposit, annual_fee)
      - usage (booking_fee, time_rate, km_rate, includes array)
      - classes array with XS and M only
      - examples array with 2 abstract sample calculations (no exact EUR values)
      - quernutzung note for larger classes
      Use ASCII-only German (e.g., "persoenlich", "Jahresgebuehr").
    </action>
    <verify>pricing.json exists in public/data and contains XS and M only with no exact prices.</verify>
    <done>Abstract pricing data is available as JSON for runtime rendering.</done>
  </task>

  <task type="auto">
    <name>Document pricing JSON update workflow</name>
    <files>site/README.md</files>
    <action>
      Add a short "Pricing data" note explaining:
      - Edit site/public/data/pricing.json to update prices
      - Run `npm run build` (or `npm run watch` for preview)
      - Deploy build/dist as usual
      Keep the note concise and ASCII-only.
    </action>
    <verify>README mentions pricing.json location and the build step.</verify>
    <done>Owner-facing update steps are documented.</done>
  </task>
</tasks>

<verification>
- Confirm site/public/data/pricing.json exists and uses abstract values only
- Confirm README includes pricing JSON update steps
</verification>

<success_criteria>

- Pricing data is editable in a single JSON file under public/
- Update workflow is documented for non-technical owners
  </success_criteria>

<output>
After completion, create `.planning/phases/04-pricing-value-system/04-01-SUMMARY.md`
</output>
