---
phase: 01-foundation-deployment
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .github/workflows/static.yml
  - notes/Deployment.md
autonomous: false
user_setup: []

must_haves:
  truths:
    - GitHub Actions workflow builds the site and deploys it to GitHub Pages.
    - Preview URL can be shared for review.
  artifacts:
    - .github/workflows/static.yml
    - notes/Deployment.md
  key_links:
    - Workflow artifact path matches the build output directory
---

<objective>
Confirm and document the GitHub Pages preview pipeline so stakeholders have a reliable preview URL.

Purpose: Make review and iteration easy without local tooling.
Output: Verified GitHub Pages workflow and owner/dev-facing deployment notes.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.planning/templates/summary.md
@.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.github/workflows/static.yml
@site/package.json
</context>

<tasks>

<task type="auto">
  <name>Audit Pages workflow settings and ensure it matches the build output</name>
  <files>.github/workflows/static.yml</files>
  <action>
    Review the existing workflow for correctness:
    - SITE_DIR points at site
    - npm ci runs in site/
    - deployment artifact path is site/build/dist
    - workflow triggers are correct for the publish/preview branch strategy
    Apply only minimal fixes if inconsistencies are found.
  </action>
  <verify>grep -n "upload-pages-artifact" .github/workflows/static.yml</verify>
  <done>Workflow is consistent with current build output directory</done>
</task>

<task type="auto">
  <name>Write deployment notes for preview and day-to-day operation</name>
  <files>notes/Deployment.md</files>
  <action>
    Create concise documentation covering:
    - Which branch triggers the Pages deploy
    - Where the Pages URL can be found
    - Local build command (npm run build)
    - Where build output lands (site/build/dist)
    - Common failure modes (missing tera, node version mismatch)
    Keep it owner-friendly and Windows-oriented where relevant.
  </action>
  <verify>test -f notes/Deployment.md</verify>
  <done>Deployment notes exist and match the workflow configuration</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>GitHub Pages preview pipeline (via Actions workflow)</what-built>
  <how-to-verify>
    In GitHub:
    1. Open Actions and confirm the latest run of “Deploy marketing site to Pages” succeeded
    2. Open the Pages URL from the deploy step output
    3. Confirm homepage loads and at least one subpage loads (e.g., /preise.html)
  </how-to-verify>
  <resume-signal>Type "approved" or paste the failing job log excerpt</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Workflow still points at site/build/dist
- [ ] notes/Deployment.md exists
</verification>

<success_criteria>

- GitHub Pages preview deploy is understood and documented
- Human verification confirms the deployed site is reachable
  </success_criteria>

<output>
After completion, create .planning/phases/01-foundation-deployment/01-02-SUMMARY.md
</output>
