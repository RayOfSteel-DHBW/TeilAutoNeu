---
phase: 01-foundation-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/package.json
  - site/package-lock.json
  - site/scripts/build-site.ps1
  - site/tailwind.config.js
  - site/postcss.config.js
  - site/src/tailwind.css
  - site/templates/base.html
  - site/public/.nojekyll
autonomous: true
user_setup: []

must_haves:
  truths:
    - Site builds via npm scripts into static output without a server runtime.
    - Tailwind toolchain exists and produces a CSS artifact in the build output.
    - Base template includes the Tailwind output CSS so later phases can use utilities.
  artifacts:
    - site/tailwind.config.js
    - site/postcss.config.js
    - site/src/tailwind.css
    - site/public/.nojekyll
  key_links:
    - site/scripts/build-site.ps1 generates Tailwind CSS into site/build/dist
    - site/templates/base.html links to the generated Tailwind CSS
---

<objective>
Add Tailwind CSS to the existing static build pipeline so the site can be styled with Tailwind in later phases.

Purpose: Establish a reliable, repeatable build foundation early.
Output: Tailwind config + build output CSS + build script integration.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.planning/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@site/package.json
@site/scripts/build-site.ps1
@site/templates/base.html
</context>

<tasks>

<task type="auto">
  <name>Install Tailwind toolchain and create config files</name>
  <files>site/package.json, site/package-lock.json, site/tailwind.config.js, site/postcss.config.js, site/src/tailwind.css</files>
  <action>
    Add Tailwind dependencies to the site workspace:
    - Add devDependencies: tailwindcss, postcss, autoprefixer
    - Add Tailwind config scanning templates + source files
    - Add a Tailwind entry file with @tailwind base/components/utilities
    Keep the existing PowerShell-based build approach; do not introduce a server runtime.
  </action>
  <verify>cd site && npm install</verify>
  <done>Tailwind config exists and package-lock.json reflects the new dependencies</done>
</task>

<task type="auto">
  <name>Integrate Tailwind build into build-site.ps1 and emit CSS into dist</name>
  <files>site/scripts/build-site.ps1</files>
  <action>
    Extend the build script so that after templating completes it also compiles Tailwind CSS into the build output.
    Output location should be inside site/build/dist so GitHub Pages deploy includes it.
    Use the locally installed Tailwind CLI (via npx) so CI and local runs behave the same.
  </action>
  <verify>cd site && npm ci && npm run build</verify>
  <done>build/dist/tailwind.css is generated on build</done>
</task>

<task type="auto">
  <name>Wire Tailwind CSS into the base template and ensure Pages compatibility</name>
  <files>site/templates/base.html, site/public/.nojekyll</files>
  <action>
    Link the generated tailwind.css from the base template (in addition to existing CSS) so Phase 2 can begin using Tailwind utilities immediately.
    Add a .nojekyll file into site/public so it is deployed to Pages to avoid Jekyll processing edge cases.
  </action>
  <verify>cd site && npm run build && test -f build/dist/tailwind.css && test -f build/dist/.nojekyll</verify>
  <done>Base template output links tailwind.css and .nojekyll is present in dist</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd site && npm ci && npm run build
- [ ] site/build/dist contains HTML, CSS, and public assets
- [ ] site/build/dist/tailwind.css exists
</verification>

<success_criteria>

- Tailwind toolchain is installed and configured
- Build emits tailwind.css into dist
- Base HTML includes the Tailwind stylesheet
  </success_criteria>

<output>
After completion, create .planning/phases/01-foundation-deployment/01-01-SUMMARY.md
</output>
