---
phase: 03-homepage-membership-funnel
plan: 03-02
type: standard
wave: 1
depends_on:
  - 02-03
autonomous: false
files_modified:
  - site/src/mitglied-werden.html
---

<objective>
Build the membership page with the 3-step flow and phone-only CTA.
Purpose: Place the join path and phone number only where it belongs, with clear expectations.
Output: Mitglied-werden page with steps, expectations, and a confirmed phone CTA block.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-homepage-membership-funnel/03-CONTEXT.md
@.planning/phases/03-homepage-membership-funnel/03-RESEARCH.md
@site/src/mitglied-werden.html
@site/templates/base.html
</context>

<tasks>
  <task type="checkpoint:decision" gate="blocking">
    <decision>Confirm the phone CTA wording and call hours for the membership page.</decision>
    <context>
      The phone number must appear only on the membership page, and call hours should be shown near the CTA.
      We need the exact CTA text and hours before finalizing the copy.
    </context>
    <options>
      <option id="default-hours">
        <name>Use "Jetzt anrufen" with Mo-Fr 09:00-12:00 Uhr</name>
        <pros>Matches the earlier placeholder example; straightforward for visitors</pros>
        <cons>Must be confirmed accurate for the owners</cons>
      </option>
      <option id="custom">
        <name>Provide exact CTA wording and hours now</name>
        <pros>Accurate, owner-approved information</pros>
        <cons>Requires the final text and hours from the user</cons>
      </option>
    </options>
    <resume-signal>Select: default-hours or custom (and provide final CTA text + hours)</resume-signal>
  </task>

  <task type="auto">
    <name>Rebuild Mitglied-werden page with steps and phone-only CTA</name>
    <files>site/src/mitglied-werden.html</files>
    <action>
      Replace the placeholder HTML with a Tera template that extends base.html.
      Add:
      - H1 "Mitglied werden" and a short intro describing the phone-only join path.
      - A 3-step ordered list (call, short kennismachen/fragen, membership start).
      - A "Was Sie wissen sollten" section with clear expectations (membership-based, station-based, phone booking, minimum term if confirmed).
      - A CTA block with phone number 07473-922202 and the confirmed call hours.
      Use Sie-Ansprache, avoid moralizing, and do not include forms or email addresses.
    </action>
    <verify>Membership page shows the 3 steps and the phone-only CTA with hours.</verify>
    <done>Mitglied-werden page contains the join flow and phone CTA only in this context.</done>
  </task>

  <task type="auto">
    <name>Verify membership page build output</name>
    <files>site/build/dist/mitglied-werden.html</files>
    <action>Run `cd site && npm run build` and review build/dist/mitglied-werden.html.</action>
    <verify>Built page shows steps, expectations, phone CTA, and no email/form elements.</verify>
    <done>Membership page content is present in the static build output.</done>
  </task>
</tasks>

<verification>
- `cd site && npm run build`
- Review `site/build/dist/mitglied-werden.html` for steps and phone CTA only here
</verification>

<must_haves>
- 3-step flow appears only on Mitglied-werden page
- Phone number 07473-922202 and call hours appear only on Mitglied-werden
- No contact forms or email addresses anywhere on the membership page
</must_haves>

<success_criteria>
- Membership path is clear, short, and phone-only by design
- CTA wording and hours match the confirmed decision
- Page aligns with copy guardrails and no overclaims
</success_criteria>

<output>
Plan delivers the membership join flow and phone-only CTA in the correct context.
</output>
