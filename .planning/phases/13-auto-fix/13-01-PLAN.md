---
phase: 13
plan: 1
title: Fix Audit Issues — Iterative Per-Page Verification
wave: 1
autonomous: true
requirements: [AFIX-01, AFIX-02, AFIX-03, AFIX-04, AFIX-05, AFIX-06, AFIX-07, AFIX-08]
---

# Plan 13-01: Fix Audit Issues — Iterative Per-Page Verification

## Objective

Fix all issues from the Phase 12 audit, then verify each page is clean at both widths using Puppeteer before proceeding to the next page.

## Issues to Fix

1. **I1: FAQ accordion text leak** — `.accordion-content` has `pb-4` padding that leaks text when `max-height: 0`. Fix: toggle padding in JS alongside max-height. Also fix "+" icon not changing to "−".
2. **I2: Hero whitespace** — Deferred to owner review (Phase 14). No fix needed now.

## Tasks

### Task 1: Fix FAQ accordion collapse/expand
- Remove `pb-4` from `.accordion-content` in accordion.html template
- Wrap `<p>` content in inner div with padding, OR toggle padding in accordion.js
- Fix icon toggle: change "+" to "−" when expanded

### Task 2: Rebuild and verify index.html
- Run `npm run build`
- Screenshot index.html at 1280px — verify FAQ is fully collapsed
- Screenshot index.html at 375px — verify same
- Click a FAQ item — verify it expands properly with icon change

### Task 3: Verify all other pages (per-page)
- Screenshot each remaining page at both widths
- Confirm no regressions introduced by the fix
- Pages: fahrzeuge, preise, geschaeftskunden, ueber-uns, mitglied-werden, datenschutz, impressum

## Execution Model
Iterative: fix → build → verify index.html clean → verify remaining pages → done
