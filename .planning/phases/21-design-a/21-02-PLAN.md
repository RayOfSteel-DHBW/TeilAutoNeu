---
phase: 21-design-a
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - site/src/index.html
  - site/src/mitglied-werden.html
autonomous: true
requirements:
  - IMPL-03
  - CONV-01
  - CONV-02
  - CONV-03
  - CONV-04

must_haves:
  truths:
    - "Homepage hero is 85vh, left-aligned, Playfair Display 900 headline, warm off-white background with horizontal rule at ~60% height"
    - "Homepage headline speaks directly to Zweitwagen persona (not generic carsharing) — uses verified copy like 'Das Auto auf Abruf'"
    - "Membership gate statement appears immediately after hero, before benefit content — in Inter 500 italic"
    - "Homepage uses editorial separator-line layout (NO rounded cards) — content sections divided by thin horizontal rules"
    - "Phone CTA block appears before footer on homepage with large phone number in brand-accent"
    - "Mitglied-werden page has clear 3-step process with editorial styling, phone CTA prominent"
    - "CTA buttons are rounded-md (not pill/rounded-full), filled brand-accent with white text, left-aligned"
  artifacts:
    - path: "site/src/index.html"
      provides: "Direction A editorial homepage with Zweitwagen-optimized hero and separator-line content"
      contains: "Das Auto auf Abruf"
    - path: "site/src/mitglied-werden.html"
      provides: "Direction A membership page with editorial styling and clear conversion funnel"
      contains: "07473-922202"
  key_links:
    - from: "site/src/index.html"
      to: "site/src/mitglied-werden.html"
      via: "Primary CTA links from homepage hero to membership page"
      pattern: "mitglied-werden.html"
    - from: "site/src/index.html"
      to: "site/src/preise.html"
      via: "Homepage teaser section links to pricing"
      pattern: "preise.html"
---

<objective>
Restyle the two conversion-critical pages (homepage and mitglied-werden) to Direction A "Editorial Broadsheet" design. The homepage is the primary landing page and must speak directly to the Zweitwagen persona. Mitglied-werden is the conversion endpoint.

Purpose: These two pages form the primary conversion funnel: homepage (awareness + interest) -> mitglied-werden (action). Getting them right is the highest-priority design work.

Output: Two fully restyled pages with editorial layout, verified copy, and clear conversion path.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-research/20-DESIGN-DIRECTIONS.md (Direction A — sections 4, 5, 7 especially)
@.planning/phases/20-research/20-INSPIRATION-NOTES.md (verified content angles)
@.planning/phases/21-design-a/21-01-SUMMARY.md (design system foundation)

Key verified copy from 20-INSPIRATION-NOTES.md and 20-DESIGN-DIRECTIONS.md:
- "Das Auto auf Abruf" — old site tagline (VERIFIED)
- "Anrufen, fahren und sparen." — three-word CTA rhythm (VERIFIED)
- "Machen Sie mit — werden Sie Mitglied! Schneller Einstieg und Ausstieg möglich." — membership framing (VERIFIED)
- "Kein Callcenter — Sie sprechen direkt mit uns" — personal service angle (VERIFIED from existing site)
- "Sie fahren, wir kümmern uns" — existing v1.1 heading (VERIFIED)
- "Nur zahlen, wenn Sie fahren" — existing v1.1 heading (VERIFIED)
- "Das passende Auto für jede Fahrt" — existing v1.1 heading (VERIFIED)
- "Persönlich statt anonym" — existing v1.1 heading (VERIFIED)

DO NOT invent new claims, statistics, or features. Rewording for tone is OK; new facts are NOT.

<interfaces>
Direction A design tokens (from 21-01):
- Primary: #1a1a2e (deep navy)
- Accent: #c2703e (burnt amber)
- Surface: #faf8f5 (warm off-white)
- Ink: #2d2d3a (dark blue-gray body text)
- Muted: #e8e4de (warm tan dividers)
- CTA: #b85c2f (deep amber hover)
- Display font: Playfair Display (serif)
- Body font: Inter (sans-serif)

Direction A layout rules:
- NO cards (no rounded corners, no shadows, no bordered containers)
- Content sections divided by border-b border-brand-muted (thin horizontal rules)
- py-16 between sections, space-y-6 within sections
- Hero: 85vh, left-aligned, horizontal rule at ~60% viewport height
- CTA buttons: rounded-md, NOT rounded-full
- Two-column grids for benefits (no enclosing borders)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle homepage to Direction A editorial layout with Zweitwagen-optimized hero</name>
  <files>site/src/index.html</files>
  <action>
Rewrite `site/src/index.html` completely. Keep the Tera template structure (extends base.html, block overrides) but replace all content HTML with Direction A editorial design.

**Hero section (replaces current hero):**
- Full-width section (NOT inside a max-width container — base.html main block no longer wraps content)
- Height: `min-h-[85vh]` with `flex flex-col justify-center`
- Background: `bg-brand-surface` (warm off-white #faf8f5)
- Inner content container: `mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8`
- Content LEFT-aligned (not centered): `text-left`
- Remove the logo image from hero (logo is in the nav now)
- Headline: `<h1 class="font-display text-5xl font-black leading-tight text-brand-primary sm:text-6xl lg:text-7xl">Das Auto auf Abruf</h1>` — Playfair Display, 900 weight (font-black), large editorial size
- Subheading below: `<p class="mt-4 max-w-xl text-lg font-sans text-brand-ink/80 sm:text-xl">Anrufen, fahren und sparen.</p>` — Inter, regular weight
- CTA buttons below subtitle, LEFT-aligned (flex, gap-4, NOT justify-center):
  - Primary: `<a href="mitglied-werden.html" class="inline-flex rounded-md bg-brand-accent px-6 py-3 text-base font-semibold text-white shadow-sm transition hover:bg-brand-cta">Mitglied werden</a>`
  - Secondary: `<a href="#inhalt" class="inline-flex rounded-md border-2 border-brand-primary px-6 py-3 text-base font-semibold text-brand-primary transition hover:bg-brand-muted">Mehr erfahren</a>`
- Horizontal rule at bottom of hero: `<hr class="mt-12 border-t-2 border-brand-primary">` — the editorial "fold line"

**Membership gate (immediately after hero, before content):**
- Full-width section with `py-8 px-4`
- Inner: `mx-auto max-w-6xl`
- Single line in Inter 500 italic: `<p class="text-center font-sans text-base font-medium italic text-brand-ink/70 sm:text-lg">Machen Sie mit — werden Sie Mitglied! Schneller Einstieg und Ausstieg möglich.</p>`
- This ensures visitors understand the membership model BEFORE seeing benefits

**Content sections (replaces card grid):**
- Anchor: `<div id="inhalt"></div>`
- NO cards. Instead, use editorial text blocks separated by horizontal rules.
- Each section: full-width with `border-b border-brand-muted py-16`
- Inner: `mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8`

- Section 1: Two-column grid (`md:grid-cols-2 gap-12`) for the two primary benefits:
  - Left column: `<h2 class="font-display text-2xl font-bold text-brand-primary sm:text-3xl">Sie fahren, wir kümmern uns</h2>` + body text in Inter
  - Right column: `<h2 class="font-display text-2xl font-bold text-brand-primary sm:text-3xl">Nur zahlen, wenn Sie fahren</h2>` + body text in Inter
  - Body text uses existing verified copy from v1.1 (Steuern, Versicherung, Wartung... etc.)

- Section 2: Three-column grid (`md:grid-cols-3 gap-10`) for secondary benefits:
  - "Merklich günstiger" — keep existing copy with X%/Y% placeholders
  - "Das passende Auto für jede Fahrt" — keep existing verified copy
  - "Persönlich statt anonym" — keep existing verified copy
  - Each benefit: h3 heading in Playfair Display + body text in Inter. NO card wrapper, NO border, NO shadow, NO rounded corners.

- Section 3: Phone CTA block before footer:
  - Full-width `bg-brand-primary py-16`
  - Inner centered: `mx-auto max-w-6xl px-4 text-center`
  - Heading: `<h2 class="font-display text-3xl font-bold text-white sm:text-4xl">Bereit?</h2>`
  - Subtext: `<p class="mt-4 font-sans text-lg text-white/80">Rufen Sie uns an — persönlich und unkompliziert.</p>`
  - Phone number: `<a href="tel:+4974739222020" class="mt-6 inline-block font-display text-3xl font-bold text-brand-accent hover:text-white transition sm:text-4xl">07473-922202</a>`

- Remove the accordion include (`{% include "accordion.html" %}`) — the editorial design does not use FAQ accordion on homepage. If FAQ content is needed later, it goes on a dedicated page.

**Update meta tags:**
- Keep existing description and OG tags, they are fine for Direction A.

**Do NOT invent new copy.** Use existing verified text. Rewording for tone is acceptable (shorter sentences, more declarative) but must be grounded in existing content.
  </action>
  <verify>
    <automated>cd C:/Dev/Repos/TeilAuto/site && npm run build 2>&1 | tail -3 && grep -c "Das Auto auf Abruf" src/index.html && grep -c "07473-922202" src/index.html && grep -c "rounded-3xl" src/index.html</automated>
  </verify>
  <done>Homepage has 85vh left-aligned editorial hero with "Das Auto auf Abruf" headline, membership gate statement after hero, editorial separator-line content sections (zero rounded-3xl cards), phone CTA block before footer with 07473-922202, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Restyle mitglied-werden page to Direction A editorial conversion funnel</name>
  <files>site/src/mitglied-werden.html</files>
  <action>
Read current `site/src/mitglied-werden.html` first, then rewrite it for Direction A.

**Page structure (extends base.html):**
- Keep Tera template structure: `{% extends "base.html" %}`, block overrides
- Set `{% set current_page = "mitglied-werden" %}` in header block

**Hero/intro section:**
- Full-width, `py-16 sm:py-20`
- Inner: `mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8`
- Left-aligned (editorial style)
- h1: `<h1 class="font-display text-4xl font-bold text-brand-primary sm:text-5xl">Mitglied werden</h1>`
- Subtitle: `<p class="mt-4 max-w-2xl text-lg text-brand-ink/80">So einfach starten Sie mit teilAuto.</p>`
- Horizontal rule: `<hr class="mt-8 border-t-2 border-brand-primary max-w-24">`

**Membership gate reminder:**
- After intro, before steps: `<p class="mt-8 font-sans text-base font-medium italic text-brand-ink/70">Machen Sie mit — werden Sie Mitglied! Schneller Einstieg und Ausstieg möglich.</p>`

**3-Step process (editorial layout, NOT cards):**
- Section with `border-b border-brand-muted py-16`
- Inner: `mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8`
- Three-column grid on md+: `md:grid-cols-3 gap-12`
- Each step:
  - Step number: `<span class="font-display text-5xl font-black text-brand-accent">1</span>` (large Playfair number in amber)
  - Heading: `<h2 class="mt-4 font-display text-xl font-bold text-brand-primary">Anrufen</h2>`
  - Body: `<p class="mt-2 text-brand-ink/80">Rufen Sie uns an unter 07473-922202. Wir besprechen alles Persönliche mit Ihnen.</p>`
- Step 1: Anrufen — phone call (keep existing content, reword for tone)
- Step 2: Mitglied werden — sign up (keep existing content about membership, 3-month minimum framing as positive: "Schneller Einstieg und Ausstieg möglich")
- Step 3: Losfahren — start driving (keep existing content)

**Details section:**
- Section with `border-b border-brand-muted py-16`
- What membership includes, what costs apply — preserve existing verified content
- Use two-column editorial layout (md:grid-cols-2 gap-12), NOT cards
- Headings in Playfair Display, body in Inter

**Final CTA block:**
- Full-width `bg-brand-primary py-16`
- Centered: `text-center`
- h2: "Jetzt anrufen" in white Playfair Display
- Phone number large in amber: `07473-922202`
- Subtext: "Persönlich und unkompliziert — kein Callcenter, Sie sprechen direkt mit uns."

**Copy guidelines:**
- Reuse verified content from existing mitglied-werden page
- Can shorten and make more declarative (editorial tone)
- Do NOT invent new membership terms, pricing, or benefits
- The 3-month minimum is a FACT from PROJECT.md — state it positively
  </action>
  <verify>
    <automated>cd C:/Dev/Repos/TeilAuto/site && npm run build 2>&1 | tail -3 && grep -c "07473-922202" src/mitglied-werden.html && grep -c "rounded-3xl" src/mitglied-werden.html</automated>
  </verify>
  <done>Mitglied-werden page has editorial layout with 3-step process, membership gate framing, phone CTA prominent, zero rounded-3xl cards, build succeeds</done>
</task>

</tasks>

<verification>
1. `cd site && npm run build` succeeds
2. Homepage: "Das Auto auf Abruf" appears as h1, membership gate text appears before content sections, no rounded-3xl cards, phone number 07473-922202 appears in CTA block
3. Mitglied-werden: 3-step process with editorial layout, phone CTA block, no rounded-3xl cards
4. Both pages link correctly to each other (homepage -> mitglied-werden via CTA button)
5. `grep -c "rounded-3xl" site/src/index.html site/src/mitglied-werden.html` returns 0 for both files
</verification>

<success_criteria>
- Homepage hero is 85vh, left-aligned, "Das Auto auf Abruf" headline in Playfair Display
- Membership gate message appears immediately after hero
- Homepage uses editorial separator-line layout (no cards)
- Homepage phone CTA block shows 07473-922202 prominently before footer
- Mitglied-werden has editorial 3-step process
- Mitglied-werden has prominent phone CTA
- All copy is grounded in verified existing content
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/21-design-a/21-02-SUMMARY.md`
</output>
