---
phase: 21-design-a
plan: 03
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - site/src/preise.html
  - site/src/fahrzeuge.html
autonomous: true
requirements:
  - IMPL-03
  - IMPL-04
  - CONV-04

must_haves:
  truths:
    - "Preise page uses editorial layout with separator lines, NOT cards — pricing data still rendered by pricing.js from pricing.json"
    - "Preise page preserves the pricing.js + pricing.json data-driven pattern (no hardcoded prices)"
    - "Fahrzeuge page preserves MapLibre map container (420-520px height) with fleet-map class"
    - "Fahrzeuge page uses editorial layout for vehicle descriptions — separator lines, not rounded cards"
    - "Both pages use Direction A typography (Playfair Display headings, Inter body)"
    - "Both pages use Direction A colors (navy primary, amber accent, warm off-white surface)"
    - "Phone CTA appears on both pages"
  artifacts:
    - path: "site/src/preise.html"
      provides: "Direction A editorial pricing page with data-driven pricing display"
      contains: "pricing.js"
    - path: "site/src/fahrzeuge.html"
      provides: "Direction A editorial fleet/vehicles page with MapLibre map"
      contains: "fleet-map"
  key_links:
    - from: "site/src/preise.html"
      to: "site/public/js/pricing.js"
      via: "Script tag loads pricing.js which reads pricing.json"
      pattern: "pricing.js"
    - from: "site/src/fahrzeuge.html"
      to: "site/public/js/fleet-map.js"
      via: "Script tag loads fleet-map.js for MapLibre map"
      pattern: "fleet-map.js"
---

<objective>
Restyle the two data-driven pages (preise and fahrzeuge) to Direction A "Editorial Broadsheet" design. These pages have JS dependencies (pricing.js, fleet-map.js) that must be preserved.

Purpose: Preise is the second page in the conversion funnel (after homepage). Fahrzeuge shows the fleet and map. Both must maintain their JS-driven data rendering while adopting the editorial visual style.

Output: Two fully restyled pages with editorial layout, preserved JS integrations, and Direction A styling.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-research/20-DESIGN-DIRECTIONS.md (Direction A — sections 5, 7)
@.planning/phases/21-design-a/21-01-SUMMARY.md (design system)

Read the CURRENT versions of these files before modifying:
- site/src/preise.html
- site/src/fahrzeuge.html

Preserved JS patterns:
- pricing.js reads pricing.json and renders pricing tables into specific DOM elements
- fleet-map.js initializes MapLibre map in .fleet-map container
- Both scripts are loaded via <script> tags — DO NOT remove these

<interfaces>
Direction A layout rules:
- NO cards (no rounded corners, no shadows, no bordered containers)
- Content sections divided by border-b border-brand-muted
- py-16 between sections, space-y-6 within sections
- EXCEPTION for pricing/vehicle tables: use border-collapse with border-brand-muted cell borders for structured data
- CTA buttons: rounded-md, brand-accent fill

Direction A design tokens:
- Primary: #1a1a2e | Accent: #c2703e | Surface: #faf8f5 | Ink: #2d2d3a | Muted: #e8e4de | CTA: #b85c2f
- Display: Playfair Display | Body: Inter
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle preise page to Direction A editorial pricing layout</name>
  <files>site/src/preise.html</files>
  <action>
Read `site/src/preise.html` first. Identify all JS integration points (pricing.js script tag, DOM element IDs that pricing.js targets). Then rewrite the page template.

**Page structure:**
- `{% extends "base.html" %}`, set `current_page = "preise"`
- Keep all existing `{% block head %}` meta tags, update if needed

**Intro section:**
- Full-width, `py-16 sm:py-20`
- Inner: `mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8`
- Left-aligned editorial style
- h1: `<h1 class="font-display text-4xl font-bold text-brand-primary sm:text-5xl">Preise</h1>`
- Subtitle: brief description of pricing model in Inter
- Short horizontal rule: `<hr class="mt-8 border-t-2 border-brand-primary max-w-24">`

**Pricing tables section:**
- Section with `border-b border-brand-muted py-16`
- Inner: `mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8`
- PRESERVE all existing DOM element IDs and containers that pricing.js writes into
- Style the containers with editorial treatment:
  - Tables should use `border-collapse` with `border-brand-muted` cell borders (the Direction A EXCEPTION for structured data)
  - Table headings in Playfair Display (font-display), data cells in Inter (font-sans)
  - No rounded corners on table containers
  - Subtle `border border-brand-muted` around the table container
- Wrap pricing containers in a clean editorial grid: `md:grid-cols-2 gap-12` for side-by-side comparison if there are two tariff tables

**Cost comparison / explanation section:**
- Below pricing tables, separated by `border-b border-brand-muted py-16`
- Use editorial text block layout — heading in Playfair Display, explanatory text in Inter
- Keep any existing content about how pricing works, the booking fee, km rates etc.
- Do NOT invent new pricing claims

**CTA block:**
- Full-width `bg-brand-primary py-16` before footer
- "Fragen zu den Preisen?" heading in white
- Phone number in amber: 07473-922202
- Link to mitglied-werden.html

**CRITICAL:** Keep the `<script src="js/pricing.js"></script>` tag (or wherever it appears). Keep all DOM IDs that pricing.js targets. The JS rendering must still work after restyling.
  </action>
  <verify>
    <automated>cd C:/Dev/Repos/TeilAuto/site && npm run build 2>&1 | tail -3 && grep -c "pricing.js" src/preise.html && grep -c "rounded-3xl" src/preise.html</automated>
  </verify>
  <done>Preise page has editorial layout, pricing.js script tag preserved, pricing DOM containers preserved, no rounded-3xl cards, phone CTA present, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Restyle fahrzeuge page to Direction A editorial fleet layout with map preserved</name>
  <files>site/src/fahrzeuge.html</files>
  <action>
Read `site/src/fahrzeuge.html` first. Identify the MapLibre integration (fleet-map container, fleet-map.js script tag, maplibre-gl CSS link). Then rewrite the page template.

**Page structure:**
- `{% extends "base.html" %}`, set `current_page = "fahrzeuge"`
- Keep the MapLibre GL CSS link in `{% block head %}`: `<link href="https://unpkg.com/maplibre-gl/dist/maplibre-gl.css" rel="stylesheet">`

**Intro section:**
- Full-width, `py-16 sm:py-20`
- Inner: `mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8`
- Left-aligned editorial style
- h1: `<h1 class="font-display text-4xl font-bold text-brand-primary sm:text-5xl">Fahrzeuge & Standorte</h1>`
- Subtitle in Inter
- Short horizontal rule

**Map section:**
- Section with `border-b border-brand-muted py-16`
- Inner: `mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8`
- PRESERVE the map container exactly: `<div id="fleet-map" class="fleet-map"></div>` (or whatever ID/class fleet-map.js targets)
- The .fleet-map class in base.css already handles height (420-520px) — no changes needed
- Add a brief heading above the map: "Unsere Standorte" in Playfair Display
- Note about Belsen as planned expansion below map in muted text

**Vehicle descriptions section:**
- Section with `border-b border-brand-muted py-16`
- Inner: `mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8`
- Two-column grid on md+: `md:grid-cols-2 gap-12`
- Each vehicle as an editorial text block (NOT a card):
  - Vehicle name in Playfair Display h2
  - Specs in a simple table with `border-brand-muted` borders (structured data exception)
  - If vehicle images exist (adam.png, mokka.png), include them — they're in public/img/
  - Body description in Inter

**Cross-use note:**
- Keep existing Quernutzung (cross-use) information if present
- Editorial text block, not card

**CTA block:**
- Full-width `bg-brand-primary py-16`
- Phone number CTA in amber

**CRITICAL:** Keep `<script src="js/fleet-map.js"></script>` and the `<script src="https://unpkg.com/maplibre-gl/dist/maplibre-gl.js"></script>` tag. Keep the fleet-map container ID/class. The map must still render.
  </action>
  <verify>
    <automated>cd C:/Dev/Repos/TeilAuto/site && npm run build 2>&1 | tail -3 && grep -c "fleet-map" src/fahrzeuge.html && grep -c "maplibre" src/fahrzeuge.html && grep -c "rounded-3xl" src/fahrzeuge.html</automated>
  </verify>
  <done>Fahrzeuge page has editorial layout, MapLibre map container and JS preserved, fleet-map class present, vehicle descriptions in editorial style, no rounded-3xl cards, phone CTA present, build succeeds</done>
</task>

</tasks>

<verification>
1. `cd site && npm run build` succeeds
2. `grep "pricing.js" site/src/preise.html` returns match (JS preserved)
3. `grep "fleet-map" site/src/fahrzeuge.html` returns match (map container preserved)
4. `grep "maplibre" site/src/fahrzeuge.html` returns matches (MapLibre GL JS + CSS preserved)
5. `grep -c "rounded-3xl" site/src/preise.html site/src/fahrzeuge.html` returns 0 for both (no v1.1 cards)
6. Both pages have phone CTA blocks
</verification>

<success_criteria>
- Preise page has editorial layout with pricing.js integration intact
- Preise page shows pricing data via JS rendering (no hardcoded prices)
- Fahrzeuge page has editorial layout with MapLibre map intact
- Fahrzeuge page map container has fleet-map class (420-520px height)
- Both pages use Playfair Display headings and Inter body text
- Both pages use Direction A colors (navy, amber, warm off-white)
- No rounded-3xl cards on either page
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/21-design-a/21-03-SUMMARY.md`
</output>
