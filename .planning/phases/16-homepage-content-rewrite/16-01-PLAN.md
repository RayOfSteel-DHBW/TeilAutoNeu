---
phase: 16-homepage-content-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/index.html
  - site/public/img/talogo.svg
autonomous: false
requirements:
  - RFIX-02

must_haves:
  truths:
    - "Hero section displays TeilAuto car icon SVG, not text-only placeholder"
    - "Card 1 heading is 'Sie fahren, wir kuemmern uns' and body enumerates Steuern, Versicherung, Wartung, TUeV"
    - "Card 2 focuses on 'Sie zahlen nur wenn Sie fahren' without cost enumeration"
    - "Card 3 heading is 'Merklich guenstiger' with X%/Y% placeholder variables, no fabricated numbers"
    - "Card 4 does NOT lead with 'Zwei Fahrzeuge' and mentions Quernutzung + local availability"
    - "Card 5 either uses a strong alternative phrasing or is dropped entirely"
    - "Card count matches number of genuine selling propositions (no padding)"
    - "Homepage renders clean at 1280px and 375px"
  artifacts:
    - path: "site/public/img/talogo.svg"
      provides: "TeilAuto car icon logo SVG"
    - path: "site/src/index.html"
      provides: "Rewritten hero + value cards"
      contains: "talogo.svg"
  key_links:
    - from: "site/src/index.html"
      to: "site/public/img/talogo.svg"
      via: "img src attribute in hero section"
      pattern: 'src="img/talogo.svg"'
---

<objective>
Replace the text-only hero logo with the TeilAuto car icon and rewrite all value cards with messaging aligned to the Zweitwagen persona and owner's guidance from the joint review.

Purpose: The homepage is the primary landing page. Current cards use generic carsharing messaging that doesn't match the owner's specific value framing from the joint review (R1, R3-R8).

Output: Updated index.html with car icon hero + rewritten value cards, verified at both widths.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-homepage-content-rewrite/16-RESEARCH.md
@.planning/phases/14-joint-review/REVIEW-LOG.md
@site/src/index.html

<interfaces>
<!-- Current hero logo reference (line 19) -->
<img src="img/logo.svg" alt="teilAuto Moessingen" class="mx-auto mb-6 h-16 w-auto">

<!-- Current card structure pattern -->
<div class="rounded-3xl border border-brand-muted bg-white p-6 shadow-sm">
  <h2 class="text-xl font-display">[heading]</h2>
  <p class="mt-3 text-brand-ink/80">[body text]</p>
</div>

<!-- Current grid layout: 2-card section (lines 44-61) + 3-card section (lines 64-88) -->
<div class="grid gap-6 md:grid-cols-2"> <!-- first grid: 2 cards -->
<div class="grid gap-6 md:grid-cols-3"> <!-- second grid: 3 cards -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hero logo and rewrite all value cards</name>
  <files>site/public/img/talogo.svg, site/src/index.html</files>
  <action>
**Step 1 — Copy logo SVG (R1):**
Copy `references/old/student-project/TA/TA_Website/public/img/talogo.svg` to `site/public/img/talogo.svg`.

Note: The SVG is 18KB with Inkscape metadata. It contains a single black `<path>` element drawing the TeilAuto car icon + text. The fill is `#000000` (black). For the hero on a light background this works. Adjust the `<img>` height class from `h-16` to `h-20` or `h-24` since the car icon has different proportions than the text-only logo.

**Step 2 — Update hero section in `site/src/index.html` (R1):**
Change the img src from `img/logo.svg` to `img/talogo.svg`. Keep the alt text "teilAuto Moessingen". Adjust height class as needed for the car icon proportions (likely `h-20` to `h-24`).

**Step 3 — Rewrite Card 1 (R3) — "Sie fahren, wir kuemmern uns":**
Replace "Kurze Wege, klare Organisation" card. New content:
- Heading: "Sie fahren, wir kuemmern uns"
- Body: Enumerate what teilAuto handles — Steuern, Versicherung, Wartung, TUeV, Stellplatz. The message is: you just drive, we handle everything else. This is the "hassle-free ownership" card.
- Use proper German characters (ue -> ue is fine in templates, but check if existing cards use umlaut entities or raw characters and match the pattern).

**Step 4 — Rewrite Card 2 (R4) — "Sie zahlen nur wenn Sie fahren":**
Replace "Teilen statt besitzen" card. New content:
- Heading: A short heading capturing "pay only when you drive" (e.g., "Nur zahlen, wenn Sie fahren" or "Sie zahlen nur, wenn Sie fahren")
- Body: No monthly pressure to drive just because you're paying. Drop cost enumeration entirely (those items moved to Card 1). Do NOT mention biking more — owner explicitly said don't advertise that.
- Keep it short and punchy.

**Step 5 — Rewrite Card 3 (R5) — "Merklich guenstiger":**
Replace "Sparsam bleiben" card. New content:
- Heading: "Merklich guenstiger"
- Body: Use placeholder comparison variables — "X % guenstiger als eine Autovermietung" and "Y % guenstiger als ein eigener Zweitwagen". CRITICAL: Do NOT fabricate specific percentages. Use literal "X %" and "Y %" as template-style placeholders. Introduce the concept of carsharing as Zweitwagen alternative.

**Step 6 — Rewrite Card 4 (R6) — Quernutzung + local availability:**
Replace "Flexibel unterwegs" card. New content:
- Heading: Something that does NOT lead with "Zwei Fahrzeuge" (e.g., "Das passende Auto fuer jede Fahrt" or "Vor Ort und deutschlandweit")
- Body: Enough cars locally for daily needs. Plus: via Quernutzung access 200+ partner vehicles across Germany — the right car wherever you need it. Frame: local convenience + nationwide flexibility.
- Do NOT start the body with vehicle count. The fleet size should be secondary to the flexibility message.

**Step 7 — Handle Card 5 (R7) — Write best-guess alternative:**
The current "Lokal verankert" card has uncertain value. Write the card using the strongest alternative phrasing from R7: "Persoenlich statt anonym" — angle: no call center, real people, your neighbor runs this. This is the strongest differentiator vs. commercial carsharing.
- Heading: "Persoenlich statt anonym"
- Body: No call center, no app — you talk directly to the person who runs teilAuto. Personal, local, and approachable.
- Add an HTML comment above the card: `<!-- R7: Card 5 alternatives considered: "Persoenlich statt anonym" (chosen), "Ihr Ansprechpartner vor Ort", "Nachbarschaftlich mobil". Owner to confirm or drop. -->`

**Step 8 — Adjust grid layout (R8):**
Keep the current 2+3 grid since we still have 5 cards. If the owner decides to drop Card 5 during verification, the second grid changes from `md:grid-cols-3` to `md:grid-cols-2` (making it 2+2). For now, keep 2+3.

**Step 9 — Build and verify:**
Run `cd C:/Dev/Repos/TeilAuto/site && npm run build` to compile.

**Content rules (do NOT violate):**
- No fabricated cost numbers — only X%/Y% placeholders (R5)
- No cost enumeration overlap between Card 1 and Card 2 (R3 handles costs, R4 handles pay-per-use)
- Card 4 must NOT lead with "Zwei Fahrzeuge stehen bereit" (R6)
- Do NOT advertise that customers bike more (R4 note)
- Use "Sie" (formal German) consistently
- Match existing card HTML structure: `rounded-3xl border border-brand-muted bg-white p-6 shadow-sm`
  </action>
  <verify>
    <automated>cd C:/Dev/Repos/TeilAuto/site && npm run build 2>&1 | tail -5</automated>
    Verify manually in build output:
    - `grep -c "talogo.svg" C:/Dev/Repos/TeilAuto/site/build/dist/index.html` returns 1
    - `grep -c "logo.svg" C:/Dev/Repos/TeilAuto/site/build/dist/index.html` returns 0 (old logo reference removed)
    - `grep "Kurze Wege" C:/Dev/Repos/TeilAuto/site/build/dist/index.html` returns nothing (old card 1 gone)
    - `grep "Teilen statt besitzen" C:/Dev/Repos/TeilAuto/site/build/dist/index.html` returns nothing (old card 2 gone)
    - `grep "Sparsam bleiben" C:/Dev/Repos/TeilAuto/site/build/dist/index.html` returns nothing (old card 3 gone)
    - `grep "Zwei Fahrzeuge" C:/Dev/Repos/TeilAuto/site/build/dist/index.html` returns nothing (R6 violation check)
  </verify>
  <done>
    - talogo.svg copied to site/public/img/
    - Hero img src points to talogo.svg with appropriate sizing
    - All 5 cards rewritten per R3-R7 guidance
    - No fabricated cost numbers (X%/Y% placeholders only)
    - No cost enumeration overlap between cards
    - Card 4 does not lead with "Zwei Fahrzeuge"
    - Build succeeds without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification and Card 5 owner decision</name>
  <files>site/build/dist/index.html</files>
  <action>Owner visually verifies the homepage at http://127.0.0.1:5500/site/build/dist/index.html at both desktop (1280px) and mobile (375px) widths. Confirms card messaging and decides on Card 5 (R7): keep, replace phrasing, or drop.</action>
  <what-built>
    Hero logo replaced with TeilAuto car icon. All 5 value cards rewritten per joint review guidance (R1, R3-R7). Grid layout preserved as 2+3.
  </what-built>
  <how-to-verify>
    1. Open http://127.0.0.1:5500/site/build/dist/index.html in browser
    2. **Hero section:** Verify car icon logo displays properly (not text-only placeholder)
    3. **Desktop (1280px):** Check all cards render in 2+3 grid, text is readable, no overflow
    4. **Mobile (375px):** Check cards stack vertically, text wraps properly, no horizontal scroll
    5. **Card 1:** Confirms "Sie fahren, wir kuemmern uns" + hassle-free enumeration
    6. **Card 2:** Confirms pay-only-when-you-drive messaging, no cost overlap with Card 1
    7. **Card 3:** Confirms "Merklich guenstiger" with X%/Y% placeholders (no fake numbers)
    8. **Card 4:** Confirms Quernutzung + local framing, does NOT lead with "Zwei Fahrzeuge"
    9. **Card 5:** Review "Persoenlich statt anonym" phrasing — decide:
       - **Keep** this phrasing
       - **Replace** with "Ihr Ansprechpartner vor Ort" or "Nachbarschaftlich mobil"
       - **Drop** the card entirely (grid adjusts to 2+2)
  </how-to-verify>
  <resume-signal>Type "approved" to keep as-is, or describe changes needed (especially Card 5 decision: keep/replace/drop)</resume-signal>
  <verify>Owner confirms homepage renders correctly at both widths and approves card content</verify>
  <done>Owner has verified hero logo, all card messaging, and made Card 5 decision</done>
</task>

</tasks>

<verification>
- Build succeeds: `cd C:/Dev/Repos/TeilAuto/site && npm run build`
- Hero shows car icon logo (talogo.svg), not text placeholder
- All card headings match owner guidance from R3-R7
- No fabricated cost numbers anywhere on homepage
- No "Zwei Fahrzeuge stehen bereit" text
- Grid layout renders correctly at 1280px and 375px
- Card count reflects genuine propositions per R8
</verification>

<success_criteria>
1. Hero section displays TeilAuto car icon (not text-only placeholder)
2. Value cards rewritten per owner guidance — headings and body text match R3-R7 framing
3. No fabricated cost percentages — only X%/Y% placeholders in Card 3
4. Card count reflects actual strong propositions (no padding per R8)
5. Homepage renders clean at desktop (1280px) and mobile (375px) widths
6. Owner has confirmed or adjusted Card 5 phrasing
</success_criteria>

<output>
After completion, create `.planning/phases/16-homepage-content-rewrite/16-01-SUMMARY.md`
</output>
