---
phase: 15-site-wide-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/index.html
  - site/src/fahrzeuge.html
  - site/src/geschaeftskunden.html
  - site/src/mitglied-werden.html
  - site/src/ueber-uns.html
  - site/src/preise.html
  - site/src/impressum.html
  - site/public/data/pricing.json
  - site/public/js/pricing.js
  - site/templates/partials/header.html
autonomous: true
requirements:
  - RFIX-01

must_haves:
  truths:
    - "Card borders are visibly distinct from the background on all pages"
    - "No 'noch offen' text remains anywhere in source files — all replaced with 'TODO'"
    - "Mitglied werden link appears in navigation on every page as a styled CTA pill"
    - "Navigation CTA appears in both desktop and mobile nav menus"
  artifacts:
    - path: "site/src/index.html"
      provides: "Card borders using border-brand-primary/20"
      contains: "border-brand-primary/20"
    - path: "site/src/fahrzeuge.html"
      provides: "Card borders using border-brand-primary/20"
      contains: "border-brand-primary/20"
    - path: "site/src/geschaeftskunden.html"
      provides: "Card borders + TODO placeholders"
      contains: "border-brand-primary/20"
    - path: "site/src/mitglied-werden.html"
      provides: "Card borders using border-brand-primary/20"
      contains: "border-brand-primary/20"
    - path: "site/src/ueber-uns.html"
      provides: "Card borders using border-brand-primary/20"
      contains: "border-brand-primary/20"
    - path: "site/src/preise.html"
      provides: "Card borders using border-brand-primary/20"
      contains: "border-brand-primary/20"
    - path: "site/src/impressum.html"
      provides: "TODO placeholders replacing noch offen"
      contains: "TODO"
    - path: "site/public/data/pricing.json"
      provides: "TODO placeholders replacing noch offen"
      contains: "TODO"
    - path: "site/public/js/pricing.js"
      provides: "TODO detection and badge rendering"
      contains: "TODO"
    - path: "site/templates/partials/header.html"
      provides: "Mitglied werden CTA in desktop and mobile nav"
      contains: "mitglied-werden.html"
  key_links:
    - from: "site/templates/partials/header.html"
      to: "mitglied-werden.html"
      via: "anchor href in nav"
      pattern: "href=\"mitglied-werden.html\""
    - from: "site/public/js/pricing.js"
      to: "site/public/data/pricing.json"
      via: "fetch and placeholder detection"
      pattern: "TODO"
---

<objective>
Fix three site-wide issues from the joint review: (1) card border contrast, (2) placeholder standardization, and (3) navigation CTA for mitglied-werden.

Purpose: Resolve cross-cutting visual and content issues (R2, R23, R28) that affect multiple pages before tackling page-specific fixes in phases 16-19.
Output: All card borders visible, all placeholders standardized to TODO, mitglied-werden accessible from nav on every page.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-site-wide-fixes/15-RESEARCH.md

@site/templates/partials/header.html
@site/public/js/pricing.js
@site/public/data/pricing.json
</context>

<interfaces>
<!-- Existing nav structure in header.html (lines 16-37) — executor must add CTA after existing links -->
<!-- Desktop nav: the <nav> element contains 4 <a> links (Preise, Fahrzeuge, Fur Firmen, Uber uns) -->
<!-- Mobile nav: same <nav> element, responsive via md: breakpoints — one set of links for both -->

<!-- pricing.js placeholder detection (line 44-45): -->
<!-- function isNochOffen(val) { return typeof val === "string" && val.toLowerCase().trim() === "noch offen"; } -->
<!-- This function and all references must change from "noch offen" to "TODO" -->

<!-- pricing.js badge rendering (line 52-57): -->
<!-- function valueOrBadge(val, prefix) renders amber badge for placeholders -->
<!-- Badge text on line 57: "noch offen" must become "TODO" -->

<!-- Card border pattern used across all pages: -->
<!-- class="...border border-brand-muted..." must become "...border border-brand-primary/20..." -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace card border classes and standardize placeholders</name>
  <files>
    site/src/index.html
    site/src/fahrzeuge.html
    site/src/geschaeftskunden.html
    site/src/mitglied-werden.html
    site/src/ueber-uns.html
    site/src/preise.html
    site/src/impressum.html
    site/public/data/pricing.json
    site/public/js/pricing.js
  </files>
  <action>
    **R2 — Card border contrast fix:**
    In ALL six HTML source files (`site/src/index.html`, `site/src/fahrzeuge.html`, `site/src/geschaeftskunden.html`, `site/src/mitglied-werden.html`, `site/src/ueber-uns.html`, `site/src/preise.html`), find every occurrence of `border-brand-muted` in class attributes and replace with `border-brand-primary/20`.

    Exact counts to verify completeness:
    - index.html: 5 occurrences (value cards)
    - fahrzeuge.html: 10 occurrences (vehicle cards, placeholder images, Quernutzung list items, info sections)
    - geschaeftskunden.html: 6 occurrences (outer sections + inner benefit cards)
    - mitglied-werden.html: 8 occurrences (step cards + info section)
    - ueber-uns.html: 3 occurrences (content sections)
    - preise.html: 5 occurrences (pricing sections)

    IMPORTANT: Do NOT touch `border-brand-muted` inside `pricing.js` (line 129) — that is a CSS class on a dynamically created element and uses `border-brand-muted` as a Tailwind border-top utility for the Kaution footnote. Change that one too, for consistency — replace the `border-brand-muted` in the className string on line 129 of pricing.js with `border-brand-primary/20`.

    **R23 — Placeholder standardization ("noch offen" to "TODO"):**

    1. `site/src/impressum.html` — Find 3 `<span>` elements containing `[noch offen]` or `[noch offen &ndash; sofern vorhanden]`. Replace:
       - `[noch offen]` with `[TODO]`
       - `[noch offen &ndash; sofern vorhanden]` with `[TODO]`

    2. `site/src/geschaeftskunden.html` — Find text "Betrag noch offen" on line 96. Replace with "Betrag TODO".
       Actually, replace the full line text: `Betrag noch offen &ndash; Inhaber bestätigt vor Launch` with `TODO &ndash; Inhaber bestätigt vor Launch`

    3. `site/public/data/pricing.json` — Replace all 3 values of `"noch offen"` with `"TODO"`:
       - line 6: `"deposit": "TODO"`
       - line 7: `"annual_fee": "TODO"`
       - line 10: `"booking_fee": "TODO"`

    4. `site/public/js/pricing.js` — Update placeholder detection and rendering:
       - Line 7 comment: change `"noch offen"` to `"TODO"`
       - Line 43 JSDoc: change `"noch offen"` to `"TODO"`
       - Line 45: change comparison string from `"noch offen"` to `"todo"` (lowercase for case-insensitive match)
       - Rename function `isNochOffen` to `isTodo` everywhere it appears (lines 44, 53)
       - Line 49 JSDoc: change `"noch offen"` to `"TODO"`
       - Line 57: change badge text from `"noch offen"` to `"TODO"`
  </action>
  <verify>
    Run: `cd site && npm run build`
    Then grep to confirm zero remaining "noch offen" in source: `grep -r "noch offen" site/src/ site/public/`
    Then grep to confirm zero remaining "border-brand-muted" in source HTML: `grep -r "border-brand-muted" site/src/`
    Confirm `border-brand-primary/20` appears in all 6 HTML files: `grep -l "border-brand-primary/20" site/src/index.html site/src/fahrzeuge.html site/src/geschaeftskunden.html site/src/mitglied-werden.html site/src/ueber-uns.html site/src/preise.html`
  </verify>
  <done>
    All `border-brand-muted` on card elements replaced with `border-brand-primary/20` across 6 HTML pages and pricing.js. All "noch offen" placeholders replaced with "TODO" across impressum.html, geschaeftskunden.html, pricing.json, and pricing.js. Zero grep hits for either old pattern in source files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add mitglied-werden CTA to navigation</name>
  <files>site/templates/partials/header.html</files>
  <action>
    **R28 — Navigation CTA for mitglied-werden:**

    Edit `site/templates/partials/header.html` to add a "Mitglied werden" link as the final item in the nav element (after the "Uber uns" link on line 36).

    The nav currently has 4 links: Preise, Fahrzeuge, Fur Firmen, Uber uns. Add a 5th link after "Uber uns" with CTA pill styling.

    Add this element as the last child inside the `<nav>` element (before the closing `</nav>` tag on line 37):

    ```html
    <a
      href="mitglied-werden.html"
      {% if current_page == "mitglied-werden" %}aria-current="page"{% endif %}
      class="rounded-full bg-brand-primary text-white px-4 py-2 text-sm font-medium hover:bg-brand-primary/90 transition-colors text-center"
    >Mitglied werden</a>
    ```

    Key details:
    - Use `rounded-full bg-brand-primary text-white` for the pill CTA style (stands out from regular nav links)
    - Include `px-4 py-2` for comfortable touch target
    - Include `hover:bg-brand-primary/90` for hover feedback
    - Include `text-center` so it looks good in the mobile stacked layout
    - Include the Tera `{% if current_page == "mitglied-werden" %}aria-current="page"{% endif %}` conditional for active state — though the CTA style already stands out, this maintains pattern consistency
    - The CTA does NOT need `aria-[current=page]:` styling overrides because its base style (white text on green bg) is already distinct — adding the gray active styles would actually make it less visible

    This single `<nav>` element serves both desktop (horizontal `md:flex-row`) and mobile (vertical stacked `flex-col`) via responsive classes already on the nav. No duplicate markup needed.

    After editing, rebuild: `cd site && npm run build`
  </action>
  <verify>
    Run: `cd site && npm run build`
    Then verify: `grep "mitglied-werden.html" site/templates/partials/header.html` returns the new CTA link.
    Then verify in built output: `grep "mitglied-werden.html" site/build/dist/index.html` confirms it appears in the rendered page.
  </verify>
  <done>
    "Mitglied werden" CTA pill link appears in the nav on every page (via shared header partial). Visible in both desktop horizontal nav and mobile stacked nav. Build succeeds without errors.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Build: `cd site && npm run build` completes without errors
2. Zero hits: `grep -r "noch offen" site/src/ site/public/` returns nothing
3. Zero hits: `grep -r "border-brand-muted" site/src/` returns nothing (pricing.js also updated)
4. Positive hits: `grep -l "border-brand-primary/20" site/src/index.html site/src/fahrzeuge.html site/src/geschaeftskunden.html site/src/mitglied-werden.html site/src/ueber-uns.html site/src/preise.html` returns all 6 files
5. Nav CTA: `grep "mitglied-werden" site/templates/partials/header.html` confirms link present
6. Built output: `grep "mitglied-werden" site/build/dist/index.html` confirms link in rendered HTML
7. Puppeteer screenshots at 1280px and 375px confirm: card borders visible, nav CTA present, no "noch offen" badges remain
</verification>

<success_criteria>
- Card borders use `border-brand-primary/20` on all card elements across all 6 pages with cards
- All "noch offen" text replaced with "TODO" in impressum.html, geschaeftskunden.html, pricing.json, and pricing.js
- pricing.js `isNochOffen` renamed to `isTodo` and detects "TODO" instead of "noch offen"
- "Mitglied werden" CTA pill visible in header nav on every page
- CTA appears correctly in both desktop (horizontal) and mobile (stacked) nav layouts
- `npm run build` succeeds
- Puppeteer screenshots at both widths confirm all three fixes visually
</success_criteria>

<output>
After completion, create `.planning/phases/15-site-wide-fixes/15-01-SUMMARY.md`
</output>
