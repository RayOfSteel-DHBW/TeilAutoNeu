---
phase: 18-preise-page-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/public/js/pricing.js
  - site/public/data/pricing.json
  - site/src/preise.html
autonomous: true
requirements: [RFIX-04]

must_haves:
  truths:
    - "Rate tables section (Unsere Fahrzeugklassen) is completely gone from the page"
    - "Groessere Fahrzeuge section is completely gone from the page"
    - "Kaution line shows a soft descriptive sentence with no specific amount"
    - "siehe unten text no longer appears anywhere on the page"
    - "Disclaimer is a single short orientation sentence, not a multi-line legal block"
    - "Page renders without JS console errors at both widths"
  artifacts:
    - path: "site/public/js/pricing.js"
      provides: "Simplified pricing renderer without renderClasses and renderQuernutzung"
      contains: "renderValues"
    - path: "site/public/data/pricing.json"
      provides: "Shortened disclaimer text"
      contains: "disclaimer"
    - path: "site/src/preise.html"
      provides: "Pricing template without classes and quernutzung container divs"
  key_links:
    - from: "site/public/js/pricing.js"
      to: "site/src/preise.html"
      via: "getElementById for remaining containers only"
      pattern: "getElementById.*pricing-(values|examples|disclaimer)"
    - from: "site/public/js/pricing.js"
      to: "site/public/data/pricing.json"
      via: "fetch and render"
      pattern: "fetch.*PRICING_URL"
---

<objective>
Simplify the Preise page by removing redundant rate tables and Quernutzung sections, rephrasing the Kaution line to a soft description, and shortening the disclaimer. This addresses review issues R17-R21 from the joint review.

Purpose: The pricing page should center on example calculations, not detailed rate tables. Redundant sections (rate tables, larger vehicles) clutter the page and duplicate content already on fahrzeuge.html.

Output: A cleaner preise.html with only three dynamic sections (values, examples, disclaimer) and simplified data.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-preise-page-restructure/18-RESEARCH.md

@site/public/js/pricing.js
@site/public/data/pricing.json
@site/src/preise.html
</context>

<interfaces>
<!-- Key renderer-to-container mapping. Each JS function renders into a matching HTML container by ID. -->

From site/public/js/pricing.js (sections object, line 278):
```javascript
var sections = {
  values: document.getElementById("pricing-values"),
  classes: document.getElementById("pricing-classes"),       // DELETE
  examples: document.getElementById("pricing-examples"),
  quernutzung: document.getElementById("pricing-quernutzung"), // DELETE
  disclaimer: document.getElementById("pricing-disclaimer")
};
```

From site/public/js/pricing.js (.then() chain, line 294):
```javascript
.then(function (data) {
  renderValues(sections.values, data);
  renderClasses(sections.classes, data);       // DELETE
  renderExamples(sections.examples, data);
  renderQuernutzung(sections.quernutzung, data); // DELETE
  renderDisclaimer(sections.disclaimer, data);
})
```

From site/src/preise.html (container divs):
```html
<!-- Vehicle class cards (XS, M) -->
<div id="pricing-classes" ...>...</div>    <!-- DELETE entire block -->

<!-- Larger vehicles via partner network -->
<div id="pricing-quernutzung" ...>...</div> <!-- DELETE entire block -->
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove sections, rephrase Kaution, shorten disclaimer</name>
  <files>
    site/public/js/pricing.js
    site/public/data/pricing.json
    site/src/preise.html
  </files>
  <action>
Make all five changes across the three source files. The changes are tightly coupled (renderer-to-container lockstep) and must all land together.

**R19 -- Remove rate tables (also resolves R17 "siehe unten"):**
1. In `site/public/js/pricing.js`:
   - DELETE the entire `renderClasses()` function (lines 137-184, from the JSDoc comment through the closing brace)
   - DELETE the `renderClasses(sections.classes, data);` call from the `.then()` chain (line 296)
   - DELETE the `classes: document.getElementById("pricing-classes"),` line from the `sections` object (line 280)
2. In `site/src/preise.html`:
   - DELETE the comment `<!-- Vehicle class cards (XS, M) -->` and the entire `div#pricing-classes` block (lines 43-46)

**R20 -- Remove "Groessere Fahrzeuge" section:**
1. In `site/public/js/pricing.js`:
   - DELETE the entire `renderQuernutzung()` function (lines 239-248, from JSDoc comment through closing brace)
   - DELETE the `renderQuernutzung(sections.quernutzung, data);` call from the `.then()` chain (line 298)
   - DELETE the `quernutzung: document.getElementById("pricing-quernutzung"),` line from the `sections` object (line 283)
2. In `site/src/preise.html`:
   - DELETE the comment `<!-- Larger vehicles via partner network -->` and the entire `div#pricing-quernutzung` block (lines 53-56)

**R18 -- Rephrase Kaution:**
In `site/public/js/pricing.js`, in the `renderValues()` function, replace the Kaution footnote block (lines 128-134). The current code builds the note with three appendChild calls including a `valueOrBadge(data.membership.deposit)` call. Replace the entire block (from `var kautionNote` through `container.appendChild(kautionNote)`) with:
```javascript
    container.appendChild(
      el("p", {
        className: "mt-4 text-xs text-brand-ink/50 border-t border-brand-muted pt-4"
      }, "Zus\u00e4tzlich wird bei Abschluss einer Mitgliedschaft eine kleine Kaution f\u00e4llig. Diese wird bei Austritt inkl. Zinsen zur\u00fcckgezahlt.")
    );
```
This uses the `el()` helper with a string child (sets textContent), keeping the same styling classes. No specific amount, no valueOrBadge() call.

**R21 -- Shorten disclaimer:**
1. In `site/public/data/pricing.json`:
   - Change `"disclaimer"` value to: `"Alle Preise sind Richtwerte und k\u00f6nnen sich \u00e4ndern. Genaue Konditionen erhalten Sie im pers\u00f6nlichen Gespr\u00e4ch."`
   - Change `"source_note"` value to: `""`
   - Keep `"updated"` value as-is (`"2026-02-09"`)
2. In `site/public/js/pricing.js`, in `renderDisclaimer()` function:
   - Wrap the `source_note` paragraph (line 263-265) in an `if (data.source_note)` guard so empty strings are suppressed
   - Wrap the `updated` paragraph (line 266-268) in an `if (data.updated)` guard so empty strings don't render "Stand: "

**After all edits, verify consistency:**
- The `sections` object should have exactly 3 keys: `values`, `examples`, `disclaimer`
- The `.then()` chain should call exactly 3 render functions: `renderValues`, `renderExamples`, `renderDisclaimer`
- The `preise.html` template should have exactly 3 container divs: `pricing-values`, `pricing-examples`, `pricing-disclaimer`
- The `.catch()` handler iterates `Object.keys(sections)` which will automatically adjust to the reduced set
  </action>
  <verify>
    <automated>cd /c/Dev/Repos/TeilAuto/site &amp;&amp; npm run build</automated>
  </verify>
  <done>
    - renderClasses() function, call, and sections entry deleted from pricing.js
    - renderQuernutzung() function, call, and sections entry deleted from pricing.js
    - pricing-classes and pricing-quernutzung container divs deleted from preise.html
    - Kaution line shows static descriptive sentence, no valueOrBadge() call
    - Disclaimer shortened in pricing.json, renderDisclaimer() guards empty fields
    - Build succeeds without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Build verification and Puppeteer visual check</name>
  <files></files>
  <action>
After the build from Task 1 completes, verify the output is correct both programmatically and visually.

**Programmatic checks (run these first):**
1. Verify build output does NOT contain deleted content:
   - `pricing-classes` should not appear in `site/build/dist/preise.html`
   - `pricing-quernutzung` should not appear in `site/build/dist/preise.html`
   - `siehe unten` should not appear in `site/build/dist/js/pricing.js`
   - `renderClasses` should not appear in `site/build/dist/js/pricing.js`
   - `renderQuernutzung` should not appear in `site/build/dist/js/pricing.js`
2. Verify build output DOES contain expected content:
   - `pricing-values` appears in `site/build/dist/preise.html`
   - `pricing-examples` appears in `site/build/dist/preise.html`
   - `pricing-disclaimer` appears in `site/build/dist/preise.html`
   - `Kaution` appears in `site/build/dist/js/pricing.js` (the new descriptive text)

**Puppeteer visual verification:**
Pre-condition: Site must be running at `http://127.0.0.1:5500/site/build/dist/`

1. Navigate to `http://127.0.0.1:5500/site/build/dist/preise.html`
2. Screenshot at 1280px width (desktop)
3. Screenshot at 375px width (mobile)
4. Check browser console for JS errors -- there should be none

**Verify visually:**
- Rate tables section ("Unsere Fahrzeugklassen") is completely gone
- "Groessere Fahrzeuge" section is completely gone
- Kaution footnote shows soft descriptive text, no badge, no amount
- Disclaimer section is short -- one sentence plus date line
- Page flows: intro, membership/usage, example calculations, short disclaimer
- No empty white boxes or orphaned containers
- Layout is clean at both widths
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const html=fs.readFileSync('C:/Dev/Repos/TeilAuto/site/build/dist/preise.html','utf8'); const js=fs.readFileSync('C:/Dev/Repos/TeilAuto/site/build/dist/js/pricing.js','utf8'); const bad=['pricing-classes','pricing-quernutzung']; const badJs=['renderClasses','renderQuernutzung','siehe unten']; const f1=bad.filter(c=>html.includes(c)); const f2=badJs.filter(c=>js.includes(c)); if(f1.length||f2.length){console.error('FAIL:',f1.concat(f2));process.exit(1)}else{console.log('PASS: all deleted content removed from build output')}"</automated>
  </verify>
  <done>
    - Build output confirmed free of deleted sections (no pricing-classes, pricing-quernutzung, renderClasses, renderQuernutzung, "siehe unten")
    - Build output retains required sections (pricing-values, pricing-examples, pricing-disclaimer)
    - Puppeteer screenshots confirm clean rendering at 1280px and 375px
    - No JS console errors
  </done>
</task>

</tasks>

<verification>
1. Build completes without errors: `cd site && npm run build`
2. Build output does not contain: `pricing-classes`, `pricing-quernutzung`, `renderClasses`, `renderQuernutzung`, `siehe unten`
3. Build output does contain: `pricing-values`, `pricing-examples`, `pricing-disclaimer`
4. Puppeteer screenshots at 1280px and 375px show clean page with no orphaned sections
5. No JS console errors when loading preise.html
</verification>

<success_criteria>
- R17 resolved: "siehe unten" gone (auto-resolved by R19 deletion of renderClasses)
- R18 resolved: Kaution shows "Zusaetzlich wird bei Abschluss einer Mitgliedschaft eine kleine Kaution faellig..." -- no specific amount
- R19 resolved: Rate tables section completely removed (JS function + HTML container)
- R20 resolved: Groessere Fahrzeuge section completely removed (JS function + HTML container)
- R21 resolved: Disclaimer shortened to one orientation sentence, source_note suppressed
- Page renders cleanly at desktop (1280px) and mobile (375px)
</success_criteria>

<output>
After completion, create `.planning/phases/18-preise-page-restructure/18-01-SUMMARY.md`
</output>
