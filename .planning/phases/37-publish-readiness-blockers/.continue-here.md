---
phase: 37-publish-readiness-blockers
task: 1
total_tasks: 2
status: blocked
last_updated: 2026-03-11T20:40:17.083Z
---

<current_state>
Phase 37 planning is complete and checker-passed. The next concrete work item is Plan 37-01, but execution was intentionally paused before any code changes because `init execute-phase 37` resolved a branch target (`gsd/v1.3-epic-skins`) that does not match the current milestone state (`v1.5`) or the current branch (`gsd/v1.4-epic-skin-polish`).
</current_state>

<completed_work>

- Phase directory created at `.planning/phases/37-publish-readiness-blockers/`.
- Proceeded without `37-CONTEXT.md` after the workflow-required decision.
- Skipped research because project config has research disabled for planning.
- Created `37-01-PLAN.md` for the blocker-fix wave: all Design B phone CTA target corrections plus the shared fleet-map icon-path fix.
- Created `37-02-PLAN.md` for the serial regression-verification wave from the shared A/B/C comparison picker.
- Ran a plan-check pass, found one real blocker in `37-01-PLAN.md`, and revised it.
- Corrected the fleet-map path expectation to the repo-consistent `../../public/img/cars/...` convention already used by Design A and Design C.
- Re-ran the checker and got a pass for full Phase 37 coverage (`GAP-01`, `GAP-02`, `GAP-03`).

</completed_work>

<remaining_work>

- Task 1: Reconcile GSD branch/state metadata before execution so `execute-phase` does not switch or commit onto the wrong branch.
- Task 1: Run `$gsd-execute-phase 37` once the branch target is safe.
- Task 1 / Plan 37-01: Update all stale Design B `tel:+4974739222020` links to `tel:+497473922202`, fix `site/public/js/fleet-map.js`, and write `37-01-SUMMARY.md`.
- Task 2 / Plan 37-02: Run serial browser verification from `site/epic/index.html`, confirm A/B/C routing plus the Design B phone and map fixes, then write `37-02-SUMMARY.md`.
- After execution: run phase verification and only then move on to Phase 38.

</remaining_work>

<decisions_made>

- Continued planning without a phase-specific `CONTEXT.md` because the workflow allowed it and the roadmap/requirements already described the blockers clearly.
- Kept planning in two waves: code fixes first, browser regression second. That preserves the serial-browser constraint already established for the epic comparison surface.
- Locked the fleet-map fix to `../../public/img/cars/...` rather than `/public/img/cars/...` because popup image URLs resolve relative to `site/epic/b/fahrzeuge.html`, and the working Design A/C map scripts already prove that convention.
- Stopped short of auto-executing even though `workflow.auto_advance` is enabled, because the branch metadata mismatch made automatic execution unsafe.

</decisions_made>

<blockers>
- Execute-phase branch mismatch: `node ./.codex/get-shit-done/bin/gsd-tools.cjs init execute-phase 37` resolved `branch_name: gsd/v1.3-epic-skins` while `.planning/STATE.md` says milestone `v1.5` and Git is currently on `gsd/v1.4-epic-skin-polish`.
- Dirty worktree: existing unrelated changes were already present before pausing, including a modified `.planning/phases/36-comparison-publish-review-pack/36-01-PLAN.md` and untracked audit scripts under `.planning/phases/35-epic-skin-c-polish-loop/`.

</blockers>

<files_modified>
- New phase planning files: `.planning/phases/37-publish-readiness-blockers/37-01-PLAN.md`
- New phase planning files: `.planning/phases/37-publish-readiness-blockers/37-02-PLAN.md`
- New handoff file: `.planning/phases/37-publish-readiness-blockers/.continue-here.md`
- Pre-existing unrelated dirty files still in worktree: `.planning/phases/36-comparison-publish-review-pack/36-01-PLAN.md`
- Pre-existing unrelated dirty files still in worktree: `.planning/phases/35-epic-skin-c-polish-loop/audit-checks.js`
- Pre-existing unrelated dirty files still in worktree: `.planning/phases/35-epic-skin-c-polish-loop/audit-iter1.js`
- Pre-existing unrelated dirty files still in worktree: `.planning/phases/35-epic-skin-c-polish-loop/audit-verify1.js`
</files_modified>

<context>
The important mental model is that Phase 37 is not exploratory work anymore. The concrete defects are already known: Design B still dials the wrong number in multiple review pages, and the shared fleet-map script still points popup icons at the wrong asset path. The plan set is intentionally narrow and should stay narrow. Do not broaden this into more Design B polish or comparison redesign.

Execution should begin only after resolving why GSD thinks this phase belongs on `gsd/v1.3-epic-skins`. The safest resume path is to inspect branch-generation/state metadata first, then run `execute-phase` once the branch target matches the active milestone and current worktree expectations.
</context>

<next_action>
Start with: inspect the branch metadata mismatch by comparing `.planning/STATE.md`, current Git branch, and the output of `node ./.codex/get-shit-done/bin/gsd-tools.cjs init execute-phase 37`, then decide whether to fix the metadata or intentionally execute on the current branch before running `$gsd-execute-phase 37`.
</next_action>
