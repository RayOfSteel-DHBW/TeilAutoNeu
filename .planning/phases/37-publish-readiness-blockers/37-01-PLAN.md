---
phase: 37-publish-readiness-blockers
plan: 01
type: execute
wave: 1
depends_on:
  - 36-01
files_modified:
  - site/epic/b/index.html
  - site/epic/b/preise.html
  - site/epic/b/fahrzeuge.html
  - site/epic/b/geschaeftskunden.html
  - site/epic/b/ueber-uns.html
  - site/epic/b/mitglied-werden.html
  - site/epic/b/impressum.html
  - site/epic/b/datenschutz.html
  - site/public/js/fleet-map.js
  - .planning/phases/37-publish-readiness-blockers/37-01-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - GAP-01
  - GAP-02

must_haves:
  truths:
    - "Every visible Design B phone CTA that renders `07473-922202` dials the matching `tel:+497473922202` target"
    - "No Design B review page still contains the stale `tel:+4974739222020` target after this plan completes"
    - "Design B homepage and membership pages still render the same visible phone number while dialing the corrected target"
    - "Fleet-map popup icons resolve through the working epic-skin `../../public/img/cars/` document-relative convention instead of the broken `/img/cars/` path"
    - "The shared fleet-map script still provides popup icons for each listed vehicle location after switching to the repo-consistent relative path convention used by Designs A and C"
  key_links:
    - from: "site/epic/b/fahrzeuge.html"
      to: "site/public/js/fleet-map.js"
      via: "shared script include ../../public/js/fleet-map.js"
      pattern: "fleet-map"
    - from: "site/public/js/fleet-map.js"
      to: "site/public/img/cars/mokka-icon.svg"
      via: "popup iconUrl relative asset path"
      pattern: "../../public/img/cars/"
  artifacts:
    - path: "site/epic/b/index.html"
      provides: "Corrected Design B homepage CTA targets for the shared review entry path"
      min_lines: 60
    - path: "site/epic/b/mitglied-werden.html"
      provides: "Corrected membership CTA targets on the other audited review-critical page"
      min_lines: 80
    - path: "site/public/js/fleet-map.js"
      provides: "Shared popup icon paths rewritten to the repo-consistent epic-skin relative asset convention"
      min_lines: 40
    - path: ".planning/phases/37-publish-readiness-blockers/37-01-SUMMARY.md"
      provides: "Exact blocker fixes, file list, and post-fix verification notes"
      min_lines: 10
---

<objective>
Remove the two concrete publish blockers recorded for Phase 37: normalize Design B's live phone CTA targets to the displayed `07473-922202` number, and correct the shared fleet-map popup icon paths so Design B `fahrzeuge` no longer loads broken car-icon assets.

Purpose: v1.4 shipped a comparison-ready surface with accepted blocker debt. Phase 37 starts by fixing the actual code defects before any publish recommendation or owner review capture continues.
Output: corrected Design B CTA links, corrected shared fleet-map icon paths, and `37-01-SUMMARY.md` documenting what changed.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.4-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize Design B phone CTA targets on every review page</name>
  <files>site/epic/b/index.html, site/epic/b/preise.html, site/epic/b/fahrzeuge.html, site/epic/b/geschaeftskunden.html, site/epic/b/ueber-uns.html, site/epic/b/mitglied-werden.html, site/epic/b/impressum.html, site/epic/b/datenschutz.html</files>
  <action>
Audit all eight Design B HTML pages for `href` values that still dial `tel:+4974739222020`.

Update every live phone CTA that renders `07473-922202` so it instead uses `tel:+497473922202`. This includes the repeated nav phone link, the in-flow CTA blocks, the footer phone link, and the mobile bottom-bar CTA where present.

Do not rewrite visible copy, page structure, or CTA placement. This is a correctness pass only.

After editing, run a repo-local search to confirm the stale target is gone from `site/epic/b/`:
`rg -n "tel:\\+4974739222020" site/epic/b`
  </action>
  <verify>
Confirm `rg -n "tel:\\+4974739222020" site/epic/b` returns no matches, and `rg -n "tel:\\+497473922202" site/epic/b -g "*.html"` returns the expected CTA links across the Design B pages.
  </verify>
  <done>Design B no longer has mismatched phone CTA targets.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the shared fleet-map popup icon asset paths</name>
  <files>site/public/js/fleet-map.js</files>
  <action>
Fix the shared popup icon paths inside `site/public/js/fleet-map.js` so they match the working epic-skin convention already used in `site/epic/a/js/fleet-map.js` and `site/epic/c/js/fleet-map.js`.

Replace the broken root-relative `/img/cars/...` icon URLs with `../../public/img/cars/...`.

Reason for this exact convention: `site/public/js/fleet-map.js` is loaded by `site/epic/b/fahrzeuge.html`, but the popup `<img>` URLs resolve relative to the document URL, not to the script file path. The working Design A and Design C copies already prove that `../../public/img/cars/...` is the repo-consistent fix for epic-skin pages.

After editing, confirm there are no remaining root-relative `iconUrl: "/img/cars/...` references in `site/public/js/fleet-map.js`.
  </action>
  <verify>
Confirm `rg -n 'iconUrl: "/img/cars/' site/public/js/fleet-map.js` returns no matches, and `rg -n '\.\./\.\./public/img/cars/' site/public/js/fleet-map.js` shows the corrected relative paths.
  </verify>
  <done>Fleet-map popup icons now point at the real car asset directory.</done>
</task>

<task type="auto">
  <name>Task 3: Write blocker-fix summary with exact scope and residual risks</name>
  <files>.planning/phases/37-publish-readiness-blockers/37-01-SUMMARY.md</files>
  <action>
Write `37-01-SUMMARY.md` after the fixes land.

The summary must capture:
- which Design B pages had phone CTA targets corrected
- which fleet-map icon paths changed, including the move to the `../../public/img/cars/` convention
- the exact grep-based checks that passed
- whether any blocker remains that should stop the next verification wave

If any expected CTA or icon-path fix could not be completed cleanly, state the blocker explicitly and stop the wave cleanly instead of hand-waving readiness.
  </action>
  <verify>
`37-01-SUMMARY.md` names the touched files, the completed checks, and either declares the blocker-fix pass complete or names a concrete remaining stop condition.
  </verify>
  <done>The implementation wave ends with a usable written handoff for regression verification.</done>
</task>

</tasks>

<verification>
- `rg -n "tel:\\+4974739222020" site/epic/b` returns no matches
- `rg -n "tel:\\+497473922202" site/epic/b -g "*.html"` confirms the corrected target appears on the Design B review pages
- `rg -n 'iconUrl: "/img/cars/' site/public/js/fleet-map.js` returns no matches
- `rg -n '\.\./\.\./public/img/cars/' site/public/js/fleet-map.js` shows the corrected relative-path references
- `37-01-SUMMARY.md` records the exact blocker fixes and any residual stop condition truthfully
</verification>

<success_criteria>
1. Design B no longer dials the wrong phone number anywhere a reviewer can reach a visible phone CTA
2. Design B `fahrzeuge` can consume fleet-map popup icons from a non-broken asset path
3. The code fix scope stays narrow and does not redesign Design B or the comparison surface
4. The next wave can focus on serial regression verification instead of rediscovering which code changed
</success_criteria>

<output>
After completion, create `.planning/phases/37-publish-readiness-blockers/37-01-SUMMARY.md`
</output>
