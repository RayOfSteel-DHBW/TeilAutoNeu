---
phase: 37-publish-readiness-blockers
plan: 02
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - site/epic/index.html
  - site/epic/b/index.html
  - site/epic/b/mitglied-werden.html
  - site/epic/b/fahrzeuge.html
  - site/public/js/fleet-map.js
  - .planning/phases/37-publish-readiness-blockers/37-02-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - GAP-01
  - GAP-02
  - GAP-03

must_haves:
  truths:
    - "Browser verification runs after the blocker-fix wave, not in parallel with other browser-driven work"
    - "From the shared comparison picker, Design A, Design B, and Design C still open from their canonical paths without dead links or misrouting"
    - "During regression verification, Design B exposes corrected phone CTA targets and `fahrzeuge` popups without broken car-icon requests"
    - "If the publish-readiness fixes introduce a small shared-surface regression, it is fixed and re-verified in the same wave"
    - "Phase 37 ends with an explicit ready-or-blocked verdict for publish readiness, not just screenshots or partial notes"
  key_links:
    - from: "site/epic/index.html"
      to: "site/epic/a/index.html"
      via: "direct picker link"
    - from: "site/epic/index.html"
      to: "site/epic/b/index.html"
      via: "direct picker link"
    - from: "site/epic/index.html"
      to: "site/epic/c/index.html"
      via: "direct picker link"
    - from: "site/epic/b/fahrzeuge.html"
      to: "site/public/js/fleet-map.js"
      via: "shared map script"
      pattern: "fleet-map"
  artifacts:
    - path: "site/epic/index.html"
      provides: "Shared comparison picker that remains the canonical A/B/C entry point"
      min_lines: 40
    - path: ".planning/phases/37-publish-readiness-blockers/37-02-SUMMARY.md"
      provides: "Serial regression verification result and publish-readiness verdict"
      min_lines: 12
---

<objective>
Serially verify that the Phase 37 blocker fixes did not break the shared A/B/C comparison surface: the shared picker must still route correctly, Design B must now dial the correct number, and Design B `fahrzeuge` must load popup icons without broken asset requests.

Purpose: Phase 37 is only complete when the fixed code is proven in the real reviewer flow, not just patched at the file level.
Output: browser-verified publish-readiness evidence and `37-02-SUMMARY.md`, plus any small truthful shared-surface fix needed to keep the picker clean.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.4-MILESTONE-AUDIT.md
@.planning/phases/36-comparison-publish-review-pack/36-OWNER-INSTRUCTIONS.md
@.planning/phases/37-publish-readiness-blockers/37-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run serial shared-flow regression verification</name>
  <files>site/epic/index.html, .planning/phases/37-publish-readiness-blockers/37-02-SUMMARY.md</files>
  <action>
Serve the `site/` directory at a stable local URL that preserves direct `.html` paths, then verify the reviewer flow serially:

1. Open `site/epic/index.html`.
2. Confirm the picker exposes direct entry links for Designs A, B, and C.
3. Follow each picker link once to confirm it reaches the intended homepage.
4. On Design B homepage, verify the visible nav/footer/mobile CTA paths dial `tel:+497473922202`.
5. Open Design B `mitglied-werden.html` and confirm its in-flow phone CTA also dials `tel:+497473922202`.
6. Open Design B `fahrzeuge`, trigger at least one map popup, and confirm its icon loads from the corrected asset path without a 404 or broken-image state.
7. Watch for any dead links, console-level routing issues, or regressions introduced by the blocker fixes.

Do not run this verification in parallel with other browser-using work.
  </action>
  <verify>
The shared picker loads, all three designs open from it, Design B homepage and `mitglied-werden` CTAs are confirmed to dial `tel:+497473922202`, and the Design B fleet popup icon loads without a broken asset request.
  </verify>
  <done>The real reviewer path is verified after the fixes, not assumed.</done>
</task>

<task type="auto">
  <name>Task 2: Fix and re-verify any small regression discovered during verification</name>
  <files>site/epic/index.html, site/epic/b/index.html, site/epic/b/mitglied-werden.html, site/epic/b/fahrzeuge.html, site/public/js/fleet-map.js</files>
  <action>
If Task 1 uncovers a small truthful regression in the shared comparison picker, a verified Design B CTA path, or the shared fleet-map behavior, fix it immediately in the actual affected file and repeat the relevant browser check.

Scope boundary:
- allowed: small routing, CTA-target, or fleet-map-fix work needed to preserve the Phase 36 comparison flow and clear the documented Phase 37 blockers
- not allowed: redesigning the picker, changing the canonical A/B/C directories, or absorbing unrelated polish work

If the issue is larger than a small truthful fix, do not guess. Record it as a blocker in the summary and stop the wave cleanly.
  </action>
  <verify>
If any listed file changes, the same routing, CTA, or popup-icon check that previously failed now passes in browser.
  </verify>
  <done>Any regression caused by the blocker fixes is either resolved immediately or recorded as an explicit blocker.</done>
</task>

<task type="auto">
  <name>Task 3: Write publish-readiness verdict and evidence summary</name>
  <files>.planning/phases/37-publish-readiness-blockers/37-02-SUMMARY.md</files>
  <action>
Write `37-02-SUMMARY.md` with:
- served URL used for verification
- picker routes checked for A, B, and C
- Design B phone CTA verification result
- Design B `fahrzeuge` popup icon verification result
- whether any shared-surface fix was needed
- one explicit verdict: `publish blockers cleared` or `still blocked`

If verification exposed a remaining defect, state the exact blocker, where it occurs, and why Phase 38 should not assume publish readiness yet.
  </action>
  <verify>
`37-02-SUMMARY.md` contains enough evidence for a later verifier to understand whether GAP-01, GAP-02, and GAP-03 are actually cleared.
  </verify>
  <done>Phase 37 closes with a concrete publish-readiness verdict instead of loose notes.</done>
</task>

</tasks>

<verification>
- Browser verification is executed serially after the code-fix wave
- `site/epic/index.html` still routes to Design A, B, and C correctly
- Design B exposes corrected `tel:+497473922202` CTA targets in the reviewer flow
- Design B `fahrzeuge` popups load their car icons without broken asset requests
- `37-02-SUMMARY.md` records a clear cleared-or-blocked verdict with supporting evidence
</verification>

<success_criteria>
1. A reviewer can still enter A, B, and C from the shared picker after the blocker fixes land
2. The core Design B publish blockers are proven fixed in the browser-visible comparison flow
3. Any regression introduced by the blocker fixes is either corrected immediately or recorded as a blocker
4. Phase 37 produces an execution record that Phase 38 can trust
</success_criteria>

<output>
After completion, create `.planning/phases/37-publish-readiness-blockers/37-02-SUMMARY.md`
</output>
