---
phase: 23-design-c
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - site/src/index.html
  - site/src/mitglied-werden.html
autonomous: true
requirements:
  - IMPL-03
  - CONV-01
  - CONV-02
  - CONV-03
  - CONV-04

must_haves:
  truths:
    - "Homepage hero is 70vh (min-h-[70vh] or min-h-[calc(70vh-4rem)]), centered, Nunito 800 headline, warm cream-to-sand gradient background with organic radial-gradient shapes"
    - "Homepage headline is 'Ihr Zweitwagen wartet schon' -- addresses Simone/Zweitwagen persona directly, NOT generic carsharing"
    - "Homepage has TWO pill-shaped CTA buttons (rounded-full): primary terracotta 'Mitglied werden' and secondary sand 'Mehr erfahren'"
    - "Warm membership gate card (rounded-2xl bg-brand-muted p-8) with 'So einfach geht's' heading appears after hero -- NOT a colored band, a warm card"
    - "Homepage uses rounded-2xl sand cards (bg-brand-muted) on cream background for content sections -- NOT alternating bands, NOT separator lines"
    - "Cards have shadow-md with warm terracotta-tinted shadow (shadow-brand-primary/8 or equivalent)"
    - "Phone CTA appears in mid-page warm terracotta section and before footer"
    - "Mitglied-werden page has personal 3-step flow with Direction C warm card styling"
    - "All content uses max-w-5xl containers"
    - "All copy is grounded in verified existing content -- no invented claims"
  artifacts:
    - path: "site/src/index.html"
      provides: "Direction C Nachbarschaftlich homepage with 70vh warm hero, pill CTAs, sand card content sections, warm membership gate card"
      contains: "Ihr Zweitwagen wartet schon"
    - path: "site/src/mitglied-werden.html"
      provides: "Direction C membership page with warm 3-step flow and terracotta CTA"
      contains: "07473-922202"
  key_links:
    - from: "site/src/index.html"
      to: "site/src/mitglied-werden.html"
      via: "Primary CTA link from hero button"
      pattern: "mitglied-werden.html"
    - from: "site/src/index.html"
      to: "site/src/preise.html"
      via: "Content section link to pricing"
      pattern: "preise.html"
---

<objective>
Restyle the two conversion-critical pages (homepage and mitglied-werden) to Direction C "Nachbarschaftlich" design. The homepage features a 70vh warm hero with organic shapes and a warm membership gate card. Mitglied-werden is the conversion endpoint with a personal 3-step process.

Purpose: These two pages form the primary conversion funnel: homepage (warm welcome + membership framing) -> mitglied-werden ("So einfach geht's" 3-step with personal tone). Direction C's approach is warm and neighborly -- two pill CTAs, sand cards, personal contact foregrounded, "Kein Callcenter" trust signal.

Output: Two fully restyled pages with Nachbarschaftlich layout, verified copy, and clear conversion path.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-research/20-DESIGN-DIRECTIONS.md (Direction C specification -- hero, membership gate, conversion)
@.planning/phases/20-research/20-INSPIRATION-NOTES.md (verified content angles)
@.planning/phases/23-design-c/23-01-SUMMARY.md (design system foundation)
@.planning/phases/23-design-c/23-RESEARCH.md (Direction C implementation patterns)

Key verified copy from 20-INSPIRATION-NOTES.md and 20-DESIGN-DIRECTIONS.md:
- "Ihr Zweitwagen wartet schon" -- Direction C headline addressing Simone directly (VERIFIED framing)
- "Das Auto auf Abruf" -- old site tagline (VERIFIED)
- "Anrufen, einsteigen, losfahren." -- three-action CTA rhythm (VERIFIED)
- "Machen Sie mit -- werden Sie Mitglied! Schneller Einstieg und Ausstieg moglich." -- membership framing (VERIFIED)
- "Kein Callcenter -- Sie sprechen direkt mit uns" -- personal service angle (VERIFIED)
- "Lernen Sie uns kennen" -- warm invitation (VERIFIED)
- "Wir freuen uns auf Ihren Anruf" -- neighborly closing (VERIFIED)
- "Sie fahren, wir kummern uns" -- existing v1.1 heading (VERIFIED)
- "Nur zahlen, wenn Sie fahren" -- existing v1.1 heading (VERIFIED)
- "Personlich statt anonym" -- existing v1.1 heading (VERIFIED)

DO NOT invent new claims, statistics, or features. Rewording for Direction C's warm, personal tone is OK; new facts are NOT.

Direction C tone: "Warm, inviting, personal. Sie-Ansprache with friendly sentence structures."

<interfaces>
Direction C design tokens (from 23-01):
- Primary: #b5541a (terracotta)
- Accent: #d4853a (amber-ochre hover)
- Surface: #fdf6ee (warm cream)
- Ink: #33261a (dark warm brown)
- Muted: #f0e6d6 (sand)
- Display font: Nunito (headings)
- Body font: DM Sans

Direction C layout rules:
- USES CARDS: rounded-2xl bg-brand-muted p-8 shadow-md shadow-brand-primary/8
- No visible borders on cards -- separation through background color difference + shadow
- py-14 between sections (spec value)
- Content width: max-w-5xl (64rem)
- CTA buttons: rounded-full (pill), bg-brand-primary, hover:bg-brand-accent
- Hero: 70vh, centered, gradient bg + organic shapes, TWO CTA buttons
- Cards: rounded-2xl, sand bg-brand-muted on cream bg-brand-surface
- Section bg: cream bg-brand-surface (default), no alternating bands

CRITICAL DIFFERENCES FROM DIRECTION B:
- TWO CTA buttons in hero (not one)
- 70vh hero (not 100vh)
- Cards with warm shadows (not alternating bands or bare text)
- rounded-full buttons (not rounded-lg)
- rounded-2xl cards (B has no cards)
- Phone shown in nav (B hides phone from nav)
- Membership gate is a warm card (B uses a high-contrast band)
- Nunito headings + DM Sans body (B uses Outfit single-font)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle homepage to Direction C Nachbarschaftlich with 70vh warm hero and sand card sections</name>
  <files>site/src/index.html</files>
  <action>
Rewrite `site/src/index.html` completely. Keep the Tera template structure (extends base.html, block overrides) but replace all content HTML with Direction C Nachbarschaftlich design.

**Hero section (70vh centered, warm gradient with organic shapes):**
- Full-width section, NOT inside a max-width container
- Height: `min-h-[calc(70vh-4rem)]` (accounts for sticky nav height) with `relative flex flex-col items-center justify-center text-center px-4`
- Background: CSS inline style or Tailwind classes: `background: linear-gradient(160deg, #fdf6ee 0%, #f0e6d6 40%, #fdf6ee 100%);` -- warm cream-to-sand-to-cream sweep. Use `style="..."` attribute for the complex gradient.
- Organic accent shapes (2-3 soft CSS circles at low opacity, pointer-events-none):
  ```html
  <div class="pointer-events-none absolute inset-0 overflow-hidden">
    <div class="absolute -bottom-32 -left-32 h-96 w-96 rounded-full bg-brand-primary/8"></div>
    <div class="absolute -top-16 -right-16 h-48 w-48 rounded-full bg-brand-primary/6"></div>
  </div>
  ```
  Keep simple. These create gentle warmth without imagery.
- Headline: `<h1 class="relative font-display text-4xl font-extrabold leading-tight text-brand-ink sm:text-5xl">Ihr Zweitwagen wartet schon</h1>` -- Nunito 800, addresses Simone directly
- Subtext: `<p class="relative mt-4 max-w-lg text-lg text-brand-ink/70">Das Auto auf Abruf &mdash; anrufen, einsteigen, losfahren.</p>` -- combines verified old-site phrases
- TWO CTA buttons (both rounded-full pill):
  ```html
  <div class="relative mt-8 flex flex-wrap items-center justify-center gap-4">
    <a href="mitglied-werden.html"
       class="inline-flex items-center rounded-full bg-brand-primary px-8 py-3 text-base font-semibold text-white shadow-md transition hover:bg-brand-accent">
      Mitglied werden &rarr;
    </a>
    <a href="#vorteile"
       class="inline-flex items-center rounded-full bg-brand-muted px-8 py-3 text-base font-semibold text-brand-ink transition hover:bg-brand-muted/80">
      Mehr erfahren
    </a>
  </div>
  ```
- Optional: CSS clip-path wave divider at bottom of hero:
  ```html
  <div class="absolute bottom-0 left-0 right-0 h-16 bg-brand-surface" style="clip-path: ellipse(60% 100% at 50% 100%);"></div>
  ```
  Only add if it looks good -- Direction C spec mentions organic curved divider but it's not required.

**Warm membership gate card (immediately after hero):**
- `<section id="vorteile" class="py-14">`
- Inner: `<div class="mx-auto max-w-5xl px-4 sm:px-6 lg:px-8">`
- Single warm card: `<div class="rounded-2xl bg-brand-muted p-8 text-center shadow-md">`
  - Heading: `<h2 class="font-display text-2xl font-bold text-brand-ink">So einfach geht's</h2>`
  - Text: `<p class="mt-4 text-lg text-brand-ink/80">Machen Sie mit &mdash; werden Sie Mitglied! Schneller Einstieg und Ausstieg m&ouml;glich.</p>`
- This is a FRIENDLY INVITATION card, NOT a high-contrast band. Direction C membership gate is warm and welcoming.

**Content sections (sand cards on cream background):**

- Section 1 (benefits): `<section class="py-14">`
  - Inner: `mx-auto max-w-5xl px-4 sm:px-6 lg:px-8`
  - Grid: `grid gap-6 md:grid-cols-2`
  - Each benefit in a sand card:
    ```html
    <div class="rounded-2xl bg-brand-muted p-8 shadow-md">
      <h3 class="font-display text-xl font-bold text-brand-ink">Sie fahren, wir k&uuml;mmern uns</h3>
      <p class="mt-3 text-brand-ink/80">Steuern, Versicherung, Wartung &mdash; alles inklusive...</p>
    </div>
    ```
  - Benefits to include (verified existing copy, tightened for warm tone):
    - "Sie fahren, wir kummern uns" -- maintenance/insurance included
    - "Nur zahlen, wenn Sie fahren" -- cost efficiency
    - "Personlich statt anonym" -- direct human contact ("Kein Callcenter -- Sie sprechen direkt mit uns")
    - "Das passende Auto fur jede Fahrt" -- fleet variety

- Section 2 (secondary benefits): `<section class="py-14">`
  - Three-column grid on lg+: `grid gap-6 md:grid-cols-2 lg:grid-cols-3`
  - Smaller sand cards with focused benefits
  - "Merklich gunstiger" -- savings angle
  - "Uber 200 Partnerfahrzeuge" -- cross-use network
  - "Elektromobil unterwegs" -- EV angle (Mokka E)

- Section 3 (warm CTA): `<section class="bg-brand-primary py-16">`
  - Inner centered: `mx-auto max-w-5xl px-4 text-center`
  - Heading: `<h2 class="font-display text-3xl font-bold text-white sm:text-4xl">Lernen Sie uns kennen</h2>`
  - Subtext: `<p class="mt-4 text-lg text-white/80">Wir freuen uns auf Ihren Anruf.</p>`
  - Phone number: `<a href="tel:+4974739222020" class="mt-6 inline-block text-3xl font-bold text-white/90 transition hover:text-white sm:text-4xl">07473-922202</a>`
  - Secondary text: `<p class="mt-4 text-sm text-white/60">Pers&ouml;nlich und unkompliziert &mdash; kein Callcenter, Sie sprechen direkt mit uns.</p>`

- Remove the accordion include (`{% include "accordion.html" %}`) -- Direction C's card-based aesthetic should not have a separate FAQ widget on the landing page.

**Meta tags:** Keep existing description and OG tags.

**Do NOT invent new copy.** Use existing verified text. Rewording for Direction C's warm, personal tone is acceptable (friendlier sentences, personal address, inviting language) but must be grounded in existing content.
  </action>
  <verify>
    <automated>cd C:/Dev/Repos/TeilAuto/site && npm run build 2>&1 | tail -3 && grep -c "Ihr Zweitwagen wartet schon" src/index.html && grep -c "07473-922202" src/index.html && grep -c "rounded-3xl" src/index.html</automated>
  </verify>
  <done>Homepage has 70vh centered hero with "Ihr Zweitwagen wartet schon" headline, two pill CTA buttons, warm membership gate card after hero, sand card content sections (zero rounded-3xl v1.1 cards), terracotta phone CTA section before footer, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Restyle mitglied-werden page to Direction C warm personal conversion funnel</name>
  <files>site/src/mitglied-werden.html</files>
  <action>
Read current `site/src/mitglied-werden.html` first, then rewrite it for Direction C.

**Page structure (extends base.html):**
- Keep Tera template structure: `{% extends "base.html" %}`, block overrides
- Set `{% set current_page = "mitglied-werden" %}` in header block

**Hero/intro section:**
- `<section class="py-16 sm:py-20">`
- Inner: `mx-auto max-w-5xl px-4 sm:px-6 lg:px-8 text-center`
- h1: `<h1 class="font-display text-4xl font-extrabold text-brand-ink sm:text-5xl">Mitglied werden</h1>` -- Nunito 800
- Subtitle: `<p class="mt-4 max-w-2xl mx-auto text-lg text-brand-ink/70">So einfach starten Sie mit teilAuto &mdash; pers&ouml;nlich und unkompliziert.</p>`

**3-Step process (in sand cards):**
- `<section class="py-14">`
- Inner: `mx-auto max-w-5xl px-4 sm:px-6 lg:px-8`
- Three-column grid on md+: `grid gap-6 md:grid-cols-3`
- Each step in a sand card:
  ```html
  <div class="rounded-2xl bg-brand-muted p-8 text-center shadow-md">
    <span class="font-display text-5xl font-extrabold text-brand-primary">1</span>
    <h2 class="mt-4 font-display text-xl font-bold text-brand-ink">Anrufen</h2>
    <p class="mt-2 text-brand-ink/70">Rufen Sie uns an unter 07473-922202. Wir besprechen alles Pers&ouml;nliche mit Ihnen.</p>
  </div>
  ```
- Step 1: Anrufen -- phone call
- Step 2: Kennenlernen -- personal meeting (in-person)
- Step 3: Losfahren -- start driving
- Step numbers in large Nunito 800 terracotta: prominent and warm

**Details section (what membership includes):**
- `<section class="py-14">`
- Inner: `mx-auto max-w-5xl px-4 sm:px-6 lg:px-8`
- Two-column grid (`grid gap-6 md:grid-cols-2`) of sand cards
- Card 1: What's included (Versicherung, Wartung, etc.) -- verified content
- Card 2: How it works (3-month minimum stated positively as "ab drei Monaten, danach monatlich kuendbar")
- Each in `rounded-2xl bg-brand-muted p-8 shadow-md` card
- Headings in Nunito 700, body in DM Sans 400

**Final CTA block (terracotta band):**
- `<section class="bg-brand-primary py-16">`
- Centered: `text-center`
- h2: `<h2 class="font-display text-3xl font-bold text-white sm:text-4xl">Jetzt anrufen</h2>`
- Phone number large: `<a href="tel:+4974739222020" class="mt-6 inline-block text-3xl font-bold text-white/90 transition hover:text-white sm:text-4xl">07473-922202</a>`
- Subtext: `<p class="mt-4 text-lg text-white/80">Pers&ouml;nlich und unkompliziert &mdash; kein Callcenter, Sie sprechen direkt mit uns.</p>`

**Copy guidelines:**
- Reuse verified content from existing mitglied-werden page
- Direction C tone: warm, inviting, personal. "Lernen Sie uns kennen," "Wir freuen uns auf Ihren Anruf."
- Do NOT invent new membership terms, pricing, or benefits
- The 3-month minimum is a FACT from PROJECT.md -- state it positively and warmly
  </action>
  <verify>
    <automated>cd C:/Dev/Repos/TeilAuto/site && npm run build 2>&1 | tail -3 && grep -c "07473-922202" src/mitglied-werden.html && grep -c "rounded-3xl" src/mitglied-werden.html && grep -c "rounded-2xl" src/mitglied-werden.html</automated>
  </verify>
  <done>Mitglied-werden page has Direction C layout with sand cards, 3-step process with terracotta step numbers in Nunito, phone CTA prominent, zero rounded-3xl v1.1 cards, rounded-2xl Direction C cards present, warm personal tone, build succeeds</done>
</task>

</tasks>

<verification>
1. `cd site && npm run build` succeeds
2. Homepage: "Ihr Zweitwagen wartet schon" appears as h1, warm membership gate card after hero, sand card content sections, phone number 07473-922202 in CTA block
3. Mitglied-werden: 3-step process in sand cards with terracotta numbers, warm tone, phone CTA
4. Both pages link correctly (homepage -> mitglied-werden via primary pill CTA button)
5. `grep -c "rounded-3xl" site/src/index.html site/src/mitglied-werden.html` returns 0 for both
6. `grep -c "rounded-2xl" site/src/index.html` returns at least 1 (Direction C cards used)
7. Homepage has TWO CTA buttons in hero (both rounded-full pills)
8. `grep "Nunito" site/src/base.css` confirms Nunito is set as heading font
</verification>

<success_criteria>
- Homepage hero is 70vh, centered, "Ihr Zweitwagen wartet schon" headline in Nunito 800
- Homepage has TWO pill-shaped CTA buttons (not one, not angular)
- Warm membership gate card (not band) appears after hero
- Homepage uses sand cards (rounded-2xl bg-brand-muted) for content
- Homepage terracotta phone CTA section shows 07473-922202 before footer
- Mitglied-werden has 3-step process in sand cards with terracotta step numbers
- Mitglied-werden has warm personal tone and prominent phone CTA
- All copy grounded in verified existing content
- No rounded-3xl v1.1 cards on either page
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/23-design-c/23-02-SUMMARY.md`
</output>
