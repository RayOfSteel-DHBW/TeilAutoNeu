---
phase: 29-epic-skin-c
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - site/epic/c/preise.html
  - site/epic/c/fahrzeuge.html
autonomous: true
requirements: [IMPL-03, IMPL-04, VIS-02, VIS-03]

must_haves:
  truths:
    - "Preise page loads pricing data dynamically from pricing.js"
    - "Fahrzeuge page renders interactive MapLibre map with vehicle markers"
    - "Both pages share Design C's visual identity (terracotta/sage, DM Serif Display, organic blobs)"
    - "Navigation and footer are consistent with homepage"
  artifacts:
    - path: "site/epic/c/preise.html"
      provides: "Pricing page with dynamic pricing.js integration"
      min_lines: 200
    - path: "site/epic/c/fahrzeuge.html"
      provides: "Vehicles page with MapLibre map and fleet details"
      min_lines: 250
  key_links:
    - from: "site/epic/c/preise.html"
      to: "site/epic/c/js/pricing.js"
      via: "script tag"
      pattern: "js/pricing\\.js"
    - from: "site/epic/c/fahrzeuge.html"
      to: "site/epic/c/js/fleet-map.js"
      via: "script tag"
      pattern: "js/fleet-map\\.js"
    - from: "site/epic/c/fahrzeuge.html"
      to: "maplibre-gl@5.17.0"
      via: "CDN script tag"
      pattern: "maplibre-gl@5\\.17\\.0"
---

<objective>
Build preise.html and fahrzeuge.html — the two data-driven pages that integrate shared JS (pricing.js, fleet-map.js).

Purpose: These pages test the JS integration paths and establish the data-display patterns for Design C.
Output: Two fully functional pages with dynamic data rendering.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-epic-skin-c/29-RESEARCH.md
@.planning/phases/29-epic-skin-c/29-01-SUMMARY.md

Read the homepage for template pattern, nav, footer, @theme block:
@site/epic/c/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build preise.html with pricing.js integration</name>
  <files>site/epic/c/preise.html</files>
  <action>
Create `site/epic/c/preise.html` — standalone HTML with Tailwind CDN.

**Template:** Copy the exact `<head>` block (Tailwind CDN, @theme, Google Fonts, c-blob CSS, fleet-map CSS), nav, footer, mobile bottom bar, and skip link from `site/epic/c/index.html`. Update:
- `<title>Preise | teilAuto Moessingen</title>`
- `aria-current="page"` on Preise link (remove from Startseite)

**Content structure:**
1. **Page header** — Simple section with serif heading "Unsere Preise" + subtitle explaining the membership cost model. NOT a full hero — compact, light background, with small organic blob decoration. Use warm copy: "Fair und transparent — Sie zahlen nur, was Sie wirklich nutzen."

2. **Pricing display section** — Container divs for pricing.js output:
   - `<div id="pricing-values"></div>` — membership and usage costs
   - `<div id="pricing-examples"></div>` — example trip calculations
   - `<div id="pricing-disclaimer"></div>` — disclaimer text
   Note: pricing.js renders its own HTML into these containers. Style the containers with appropriate spacing and the c-blob visual signature.

3. **Membership gate reminder** — Brief paragraph reinforcing that teilAuto is membership-based. "Als Mitglied profitieren Sie von guenstigen Konditionen — ohne versteckte Gebuehren." Link to mitglied-werden.html.

4. **CTA section** — "Fragen zu unseren Preisen?" + phone number + Mitglied werden button.

**Scripts at bottom:**
```html
<script src="js/nav.js" defer></script>
<script src="js/pricing.js" defer></script>
```

Add organic blob shapes in at least 2 sections for visual signature consistency.
  </action>
  <verify>
    <automated>wc -l C:/Dev/Repos/TeilAuto/site/epic/c/preise.html && grep -c "pricing-values" C:/Dev/Repos/TeilAuto/site/epic/c/preise.html && grep -c "pricing.js" C:/Dev/Repos/TeilAuto/site/epic/c/preise.html</automated>
  </verify>
  <done>preise.html exists with 200+ lines, contains pricing-values/pricing-examples/pricing-disclaimer divs, loads pricing.js. Nav has aria-current="page" on Preise.</done>
</task>

<task type="auto">
  <name>Task 2: Build fahrzeuge.html with MapLibre map integration</name>
  <files>site/epic/c/fahrzeuge.html</files>
  <action>
Create `site/epic/c/fahrzeuge.html` — standalone HTML with Tailwind CDN.

**Template:** Same head/nav/footer/mobile-bar pattern as preise.html. Update:
- `<title>Fahrzeuge | teilAuto Moessingen</title>`
- `aria-current="page"` on Fahrzeuge link

**Head additions for MapLibre:**
```html
<link href="https://unpkg.com/maplibre-gl@5.17.0/dist/maplibre-gl.css" rel="stylesheet" />
<script src="https://unpkg.com/maplibre-gl@5.17.0/dist/maplibre-gl.js" defer></script>
<script src="js/fleet-map.js" defer></script>
```

**Content structure:**
1. **Page header** — "Unsere Fahrzeuge" + subtitle. Compact, not full hero. Organic blob decoration.

2. **Vehicle details** — Show the 2 vehicles (Opel Mokka, Opel Adam) with specs. Use definition lists (`dl/dt/dd`) for vehicle specs (this is Design C's approach — Design A used dl too, but Design C frames it with warm community copy). Include vehicle images from `../../public/img/mokka.png` and `../../public/img/adam.png`.

3. **Map section** — "Wo stehen unsere Fahrzeuge?"
   - Map container: `<div id="fleet-map" class="fleet-map"></div>`
   - The fleet-map CSS is already in the `<style>` block (copied from index.html @theme block pattern from research)
   - Height: 420px mobile, 480px sm, 520px lg

4. **Quernutzung section** — "Mehr als nur Moessingen" — explain cross-network access to 200+ vehicles. Organic blob decoration.

5. **CTA section** — "Fahrzeug ausprobieren?" + phone number + mitglied-werden link.

**Scripts at bottom:**
```html
<script src="js/nav.js" defer></script>
```
(MapLibre and fleet-map.js loaded in head with defer)

Add organic blob shapes in at least 2 sections.
  </action>
  <verify>
    <automated>wc -l C:/Dev/Repos/TeilAuto/site/epic/c/fahrzeuge.html && grep -c "fleet-map" C:/Dev/Repos/TeilAuto/site/epic/c/fahrzeuge.html && grep -c "maplibre-gl@5.17.0" C:/Dev/Repos/TeilAuto/site/epic/c/fahrzeuge.html && grep -c "../../public/img" C:/Dev/Repos/TeilAuto/site/epic/c/fahrzeuge.html</automated>
  </verify>
  <done>fahrzeuge.html exists with 250+ lines, contains fleet-map container with correct ID, loads MapLibre 5.17.0 CSS and JS, references vehicle images from ../../public/img/. Nav has aria-current on Fahrzeuge.</done>
</task>

</tasks>

<verification>
- Both pages use identical @theme block and nav/footer as index.html
- preise.html pricing.js loads and targets correct container IDs
- fahrzeuge.html map container has `id="fleet-map"` (exact match required by fleet-map.js)
- Both pages have organic blob visual signature elements
- Both pages have pb-20 lg:pb-0 spacer for mobile bottom bar
</verification>

<success_criteria>
1. preise.html renders pricing data when served via local server
2. fahrzeuge.html renders MapLibre map with vehicle markers
3. Both pages maintain Design C visual identity (terracotta/sage, organic blobs)
4. No horizontal overflow at 375px on either page
</success_criteria>

<output>
After completion, create `.planning/phases/29-epic-skin-c/29-02-SUMMARY.md`
</output>
