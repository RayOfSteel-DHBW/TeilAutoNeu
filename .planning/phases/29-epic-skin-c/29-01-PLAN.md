---
phase: 29-epic-skin-c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/epic/c/index.html
  - site/epic/c/js/nav.js
  - site/epic/c/js/pricing.js
  - site/epic/c/js/fleet-map.js
  - site/epic/c/js/accordion.js
autonomous: true
requirements: [IMPL-01, IMPL-02, IMPL-03, VIS-01, VIS-04, CONV-01, CONV-02, CONV-03, CONV-04]

must_haves:
  truths:
    - "Homepage loads in browser with stacked editorial hero composition visually distinct from A's full-bleed and B's asymmetric split"
    - "Navigation uses a minimal header with expandable full-screen menu — NOT sticky dark bar (A) or floating pill (B)"
    - "Homepage section ordering leads with community/trust — different from A's convenience-first and B's cost-first"
    - "Organic blob shapes (clip-path or border-radius) appear as visual signature — NOT stripes (A) or diagonals (B)"
    - "Phone CTA 07473-922202 appears in hero, nav, and footer"
  artifacts:
    - path: "site/epic/c/index.html"
      provides: "Homepage with stacked editorial hero and community-first storytelling arc"
      min_lines: 350
    - path: "site/epic/c/js/nav.js"
      provides: "Minimal header nav toggle with full-screen overlay"
    - path: "site/epic/c/js/pricing.js"
      provides: "Pricing renderer with corrected data path"
    - path: "site/epic/c/js/fleet-map.js"
      provides: "Fleet map with corrected icon paths"
    - path: "site/epic/c/js/accordion.js"
      provides: "FAQ accordion"
  key_links:
    - from: "site/epic/c/index.html"
      to: "site/epic/c/js/nav.js"
      via: "script tag"
      pattern: "src=\"js/nav\\.js\""
    - from: "site/epic/c/index.html"
      to: "@tailwindcss/browser@4"
      via: "CDN script tag"
      pattern: "cdn\\.jsdelivr\\.net.*tailwindcss/browser@4"
---

<objective>
Create Design C foundation (JS files, nav pattern) and build the homepage with a stacked editorial hero, organic blob visual signature, and community-first storytelling arc.

Purpose: Establish Design C's structural identity — distinct from A (editorial broadsheet, dark sticky nav) and B (floating pill, asymmetric split hero). Design C uses a minimal header with expandable mega-menu, stacked editorial hero with floating stat badges, and warm terracotta/sage earth-tone palette with DM Serif Display + Figtree fonts. All styles inline via Tailwind CDN — no separate CSS files.

Output: Working homepage at site/epic/c/index.html + all JS dependencies in site/epic/c/js/
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-epic-skin-c/29-RESEARCH.md

Reference existing designs for structural distinctness (read but do NOT modify):
@site/epic/a/index.html (Design A: sticky dark nav, full-bleed hero, editorial broadsheet)
@site/epic/b/index.html (Design B: floating pill nav, asymmetric split hero, cost-forward)

Shared JS to copy+adapt:
@site/public/js/nav.js
@site/public/js/pricing.js
@site/public/js/fleet-map.js
@site/public/js/accordion.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Design C JS files and directory structure</name>
  <files>site/epic/c/js/nav.js, site/epic/c/js/pricing.js, site/epic/c/js/fleet-map.js, site/epic/c/js/accordion.js</files>
  <action>
Create directory `site/epic/c/js/`.

**nav.js** — Write a custom nav toggle for Design C's minimal header pattern. Design C uses a minimal horizontal header (NOT sticky, NOT floating pill) with a hamburger that opens a full-screen overlay on all breakpoints below `lg`. The overlay uses id="mobile-nav". The script must:
- Toggle `hidden`/`flex` classes on `#mobile-nav` when hamburger button `[aria-controls="mobile-nav"]` is clicked
- Toggle `aria-expanded` on the hamburger button
- Lock body scroll when overlay is open (`document.body.style.overflow = 'hidden'/'auto'`)
- Close on Escape key press
- Close button inside overlay triggers close

Do NOT use the shared `public/js/nav.js` (it targets `#primary-nav`). Write fresh, like Design B did.

**pricing.js** — Copy from `site/public/js/pricing.js`. Change the `PRICING_URL` variable from `"./data/pricing.json"` to `"../../public/data/pricing.json"`. This ensures pricing data loads correctly from the epic/c/ directory without needing a local copy of pricing.json.

**fleet-map.js** — Copy from `site/public/js/fleet-map.js`. Update all `iconUrl` paths that reference `/img/cars/` to use `../../public/img/cars/` as relative paths (e.g., `iconUrl: "../../public/img/cars/mokka-icon.svg"`). This fixes icon resolution when served from the epic/c/ directory.

**accordion.js** — Direct copy from `site/public/js/accordion.js`. No modifications needed.
  </action>
  <verify>
    <automated>ls -la C:/Dev/Repos/TeilAuto/site/epic/c/js/ && grep -c "PRICING_URL" C:/Dev/Repos/TeilAuto/site/epic/c/js/pricing.js && grep -c "mobile-nav" C:/Dev/Repos/TeilAuto/site/epic/c/js/nav.js</automated>
  </verify>
  <done>All 4 JS files exist in site/epic/c/js/. pricing.js references ../../public/data/pricing.json. fleet-map.js references ../../public/img/cars/. nav.js targets #mobile-nav with scroll lock and Escape key handler. accordion.js is a direct copy.</done>
</task>

<task type="auto">
  <name>Task 2: Build homepage with stacked editorial hero, organic blobs, and community-first arc</name>
  <files>site/epic/c/index.html</files>
  <action>
Create `site/epic/c/index.html` — a complete standalone HTML page using Tailwind CDN. This is the structural heart of Design C.

**Head block:**
- `<script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>`
- `<style type="text/tailwindcss">` block with @theme:
  ```
  @theme {
    --color-brand-primary: #5B4A3F;    /* warm brown */
    --color-brand-accent: #C4704B;     /* terracotta */
    --color-brand-surface: #FAF7F4;    /* warm cream */
    --color-brand-ink: #2C2420;        /* dark brown */
    --color-brand-muted: #E8E0D8;     /* light taupe */
    --color-brand-sage: #7A9E8E;      /* sage green accent */
    --font-sans: "Figtree", system-ui, sans-serif;
    --font-display: "DM Serif Display", "Georgia", serif;
  }
  ```
- Google Fonts: DM Serif Display (wght@400) + Figtree (wght@400;500;600;700)
- Add CSS for organic blob shapes (visual signature):
  ```css
  .c-blob {
    position: absolute;
    border-radius: 60% 40% 70% 30% / 50% 60% 40% 50%;
    pointer-events: none;
  }
  .c-blob--alt {
    border-radius: 40% 60% 30% 70% / 60% 40% 50% 50%;
  }
  ```
- Add fleet-map CSS (`.fleet-map`, `.fleet-marker`, `.fleet-marker--planned`, `.maplibregl-popup-content`) using standard CSS properties (NOT @apply).

**Skip link:** `<a href="#main-content" class="sr-only focus:not-sr-only ...">Zum Inhalt springen</a>`

**Navigation — Minimal header (NOT sticky, NOT floating pill):**
- Simple horizontal header with logo left, desktop links center/right, hamburger right
- NOT position:fixed or position:sticky — flows with the page
- Uses `lg:` breakpoint for desktop/mobile split (7 links too many for 768px)
- Desktop: `teilAuto` logo + `Preise | Fahrzeuge | Fuer Firmen | Ueber uns` + `Mitglied werden` button + phone number
- Mobile: hamburger opens full-screen overlay (`id="mobile-nav"`)
- `aria-current="page"` on Startseite link
- Mobile overlay: full-screen bg-brand-primary with large centered nav links, close button top-right
- Organic blob decoration in mobile overlay (small, subtle, opacity-10)

**Hero — Stacked editorial composition:**
Use CSS Grid with `grid-template-areas: 'hero'` so headline and floating badges overlap in the same grid cell. This is NOT a centered h1+subtitle+CTA (that was v1.2) and NOT full-bleed (Design A) and NOT asymmetric split (Design B).

Structure:
- Light background (brand-surface), NOT dark — this is the key visual difference from A and B which both use dark heroes
- Large serif headline (text-5xl lg:text-7xl): "Ihre Nachbarn fahren schon mit." (community-first framing)
- Subtitle: "teilAuto Moessingen — Carsharing von Nachbarn fuer Nachbarn. Seit 2000."
- CTA buttons: "Mitglied werden" (primary) + "Mehr erfahren" (secondary outline)
- Phone CTA below buttons
- Floating stat badges (positioned via grid overlap with `justify-self`/`align-self`):
  - Top-right: "60+" members badge (rounded, bg-brand-sage/10, font-display text-5xl)
  - Bottom-right: "Seit 2000" badge
- Organic blob shapes as decorative background elements (c-blob class, bg-brand-sage/8 and bg-brand-accent/6, various sizes)
- Min-height: 85vh on desktop, auto on mobile

**Homepage section ordering (community/trust-first — different from A and B):**

1. **Hero** — Stacked editorial with community message (described above)
2. **Social proof / community section** — "Ueber 60 Mitglieder in Moessingen" — lead with community trust, NOT benefits or costs. Use oversized typographic number "60+" as decorative background element. Short testimonial-style copy about the community feel. Organic blob decoration.
3. **Membership gate** — "Mitglied einer Gemeinschaft" — warm, neighborly framing. Explain membership model as joining a community, not signing up for a service. Three key points: Gemeinschaft, Flexibilitaet, Kostenteilung. Use organic blob accent.
4. **How it works** — "So einfach geht's" — 3 steps (Anrufen, Kennenlernen, Losfahren). Use oversized step numbers as decorative elements (font-display text-6xl font-bold text-brand-accent/10).
5. **Benefits** — "Was Sie sich sparen" — list concrete savings (Versicherung, Wartung, TUEV, Stellplatz inklusive). Brief, not a cost comparison table (that's Design B's territory). Focus on freedom from hassle, not money math.
6. **CTA block** — "Wir freuen uns auf Sie" — warm, personal closing. Large phone number, Mitglied werden button. Organic blob decoration.

**Footer:**
- bg-brand-primary text-white
- 3-column grid: Brand info | Navigation links | Contact (phone + legal links)
- Organic blob decoration (subtle, opacity-5)
- Copyright line with Impressum + Datenschutz links

**Mobile fixed bottom phone bar:**
- Fixed bottom, z-40, rounded-full button style
- `pb-20 lg:pb-0` on a spacer div after footer to prevent content overlap

**Copy strategy:** Community/trust-first, warm storytelling, neighborly framing. Same facts as A and B but framed as "your neighbors already drive with us" rather than "save money" (B) or "car on demand" (A). Use Sie-Ansprache throughout. Sachlich + freundlich tone. German HTML entities for umlauts.

**Scripts at bottom of body:**
```html
<script src="js/nav.js" defer></script>
```
(pricing.js and fleet-map.js only on their respective pages)

The page should be 400-500 lines. Every section must include at least one organic blob shape (the c-blob visual signature) to establish Design C's identity consistently.
  </action>
  <verify>
    <automated>wc -l C:/Dev/Repos/TeilAuto/site/epic/c/index.html && grep -c "c-blob" C:/Dev/Repos/TeilAuto/site/epic/c/index.html && grep -c "tailwindcss/browser@4" C:/Dev/Repos/TeilAuto/site/epic/c/index.html && grep -c "07473" C:/Dev/Repos/TeilAuto/site/epic/c/index.html && grep -c "DM Serif Display" C:/Dev/Repos/TeilAuto/site/epic/c/index.html</automated>
  </verify>
  <done>Homepage exists with 350+ lines. Contains multiple c-blob visual signature elements. Uses Tailwind CDN v4. Phone CTA appears at least 3 times (hero, nav/mobile, footer). DM Serif Display font loaded. Stacked editorial hero with grid overlay pattern is visually distinct from A and B. Section ordering is community-first (social proof before gate before benefits).</done>
</task>

</tasks>

<verification>
- `site/epic/c/index.html` opens in browser with Tailwind CDN rendering styles correctly
- Hero is NOT dark (unlike A and B) — light background with serif headline
- Navigation is a simple horizontal header, not sticky and not floating pill
- Organic blob shapes visible as decorative elements across sections
- Phone number 07473-922202 appears in hero, footer, and mobile bottom bar
- Section order: hero -> social proof -> membership gate -> how it works -> benefits -> CTA
- All JS files present in site/epic/c/js/
</verification>

<success_criteria>
1. Homepage renders correctly at 375px, 768px, and 1280px with no horizontal overflow
2. Hero composition (stacked editorial with floating badges) is visually distinct from A's full-bleed and B's asymmetric split
3. Nav pattern (minimal header) is distinct from A's sticky dark bar and B's floating pill
4. At least 4 organic blob shapes visible across the page sections
5. Section ordering differs from both A and B
6. Copy arc uses community/neighborly framing, not convenience-first (A) or cost-first (B)
</success_criteria>

<output>
After completion, create `.planning/phases/29-epic-skin-c/29-01-SUMMARY.md`
</output>
