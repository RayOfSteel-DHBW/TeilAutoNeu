---
phase: 28-epic-skin-b
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - site/epic/b/preise.html
  - site/epic/b/fahrzeuge.html
autonomous: true
requirements: [IMPL-03, VIS-03, VIS-04, CONV-03]

must_haves:
  truths:
    - "Preise page displays pricing data loaded from pricing.json (not hardcoded)"
    - "Fahrzeuge page shows MapLibre map with fleet markers at correct height (420-520px)"
    - "Both pages have floating pill nav, footer, mobile bottom phone bar consistent with homepage"
    - "Diagonal accent lines appear as visual signature on both pages"
    - "Both pages are responsive at 375px, 768px, 1280px without overflow"
  artifacts:
    - path: "site/epic/b/preise.html"
      provides: "Pricing page with pricing.js integration"
      contains: "pricing-values"
      min_lines: 100
    - path: "site/epic/b/fahrzeuge.html"
      provides: "Fleet page with MapLibre map"
      contains: "fleet-map"
      min_lines: 100
  key_links:
    - from: "site/epic/b/preise.html"
      to: "site/epic/b/js/pricing.js"
      via: "script src"
      pattern: 'src="js/pricing.js"'
    - from: "site/epic/b/fahrzeuge.html"
      to: "site/public/js/fleet-map.js"
      via: "script src"
      pattern: 'src="../public/js/fleet-map.js"'
    - from: "site/epic/b/fahrzeuge.html"
      to: "MapLibre GL JS CDN"
      via: "script src"
      pattern: "maplibre-gl"
---

<objective>
Build the two data-driven pages -- Preise (pricing from JSON) and Fahrzeuge (fleet + MapLibre map) -- using Design B's visual language.

Purpose: These pages integrate external data sources (pricing.json, fleet-map.js) and prove the design system works beyond the homepage. Preise reinforces the cost-forward narrative; Fahrzeuge shows the practical fleet.
Output: Two working pages with live data integration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-epic-skin-b/28-RESEARCH.md
@.planning/phases/28-epic-skin-b/28-01-SUMMARY.md

@site/public/js/pricing.js
@site/public/js/fleet-map.js
@site/public/data/pricing.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build preise.html with pricing.js integration</name>
  <files>site/epic/b/preise.html</files>
  <action>
Create `site/epic/b/preise.html` -- the Design B pricing page. This page is data-driven via `js/pricing.js` (adapted copy with updated PRICING_URL).

**Page structure:**

1. Same `<head>` as index.html (same fonts CDN link, tailwind-out.css, base.css). Title: "Preise | teilAuto Moessingen"

2. Skip-to-content link, floating pill nav (copy from index.html, add `aria-current="page"` to Preise link), mobile nav overlay

3. **Hero banner** (NOT the asymmetric split -- that's homepage-only). Use a compact hero: `bg-brand-primary`, `py-24 pt-28` (extra top padding for floating nav clearance). Centered content with `font-display` heading "Transparent und fair" and subtitle "Nur zahlen, wenn Sie fahren. Keine versteckten Kosten." Include diagonal accent line decoration.

4. **Pricing sections** -- Three container divs that pricing.js populates:
   - `<div id="pricing-values" class="...">` -- Membership fees and usage overview
   - `<div id="pricing-examples" class="...">` -- Sample trip calculations
   - `<div id="pricing-disclaimer" class="...">` -- Legal disclaimer

   Wrap these in a `max-w-4xl mx-auto px-4 py-16` container. Each section div should have appropriate spacing (`mt-12`). Use `bg-brand-surface` for the main area.

   Style the sections with Design B's component language: data-forward cards with diagonal accent corner decorations on the example cards. The pricing.js renders into these containers using DOM APIs with class names like `rounded-2xl bg-brand-surface p-4`, `font-display`, `text-brand-primary` -- these classes are already in the Tailwind output because they match Design B's @theme tokens.

5. **CTA section** before footer: "Fragen zu unseren Preisen?" with phone number and link to mitglied-werden.html. Use `bg-brand-muted` with diagonal accent.

6. Footer (identical to index.html)

7. Mobile bottom phone bar

8. Scripts: `<script src="js/pricing.js" defer></script>` and `<script src="js/nav.js" defer></script>`

**Critical**: The pricing.js `renderValues` function looks for `id="pricing-values"`, `renderExamples` looks for `id="pricing-examples"`, `renderDisclaimer` looks for `id="pricing-disclaimer"`. These IDs MUST be present exactly as spelled. The pricing.js uses class names from its own createElement calls -- these will render correctly because the Tailwind output includes all utility classes used across all HTML files.

After creation, recompile Tailwind:
```bash
cd site && npx @tailwindcss/cli -i epic/b/style.css -o epic/b/tailwind-out.css
```
  </action>
  <verify>
    <automated>cd C:/Dev/Repos/TeilAuto/site && npx @tailwindcss/cli -i epic/b/style.css -o epic/b/tailwind-out.css && grep -c "pricing-values" epic/b/preise.html && grep -c "pricing.js" epic/b/preise.html</automated>
  </verify>
  <done>
- preise.html exists with pricing-values, pricing-examples, pricing-disclaimer container divs
- pricing.js referenced via script src="js/pricing.js"
- Compact dark hero (not asymmetric split) with "Transparent und fair"
- Floating pill nav present with aria-current on Preise link
- Footer and mobile phone bar present
- Diagonal accent decoration present
- Tailwind compiles including preise.html classes
  </done>
</task>

<task type="auto">
  <name>Task 2: Build fahrzeuge.html with MapLibre map</name>
  <files>site/epic/b/fahrzeuge.html</files>
  <action>
Create `site/epic/b/fahrzeuge.html` -- the Design B fleet and locations page with interactive MapLibre map.

**Page structure:**

1. Same `<head>` as other pages plus MapLibre CSS: `<link href="https://unpkg.com/maplibre-gl@5.17.0/dist/maplibre-gl.css" rel="stylesheet" />`. Title: "Fahrzeuge & Standorte | teilAuto Moessingen"

2. Skip-to-content link, floating pill nav (aria-current on Fahrzeuge link), mobile nav overlay

3. **Compact hero**: `bg-brand-primary`, heading "Unsere Fahrzeuge" with subtitle about 2 vehicles in Moessingen and Quernutzung network. Diagonal accent decoration. `pt-28` for nav clearance.

4. **Fleet vehicles section**: Show the two Moessingen vehicles -- Opel Mokka-e and VW up! (verified fleet data from v1.2). Use Design B's data-forward card style with large typographic details. Each vehicle card: name, type (Elektro/Benzin), station location, key specs. Cards have diagonal accent corner decoration. Use `bg-brand-surface` section.

5. **Quernutzung section**: Brief explanation that members can use vehicles in other cities (Tuebingen, Reutlingen, etc.). Compact info block. `bg-brand-muted`.

6. **Map section**:
   ```html
   <div id="fleet-map" class="fleet-map"></div>
   ```
   The `fleet-map` class is defined in base.css with responsive heights (420/480/520px). Wrap in a container with appropriate padding. Section title "Standorte in Moessingen".

7. **CTA section**: Link to mitglied-werden.html. Phone number.

8. Footer, mobile bottom phone bar

9. Scripts (order matters):
   ```html
   <script src="https://unpkg.com/maplibre-gl@5.17.0/dist/maplibre-gl.js" defer></script>
   <script src="../public/js/fleet-map.js" defer></script>
   <script src="js/nav.js" defer></script>
   ```

**Critical notes**:
- The map container MUST have both `id="fleet-map"` and `class="fleet-map"` -- fleet-map.js targets the ID, base.css styles the class
- MapLibre GL JS and CSS from CDN at version 5.17.0
- fleet-map.js references icons with absolute paths like `/img/cars/mokka-icon.svg` -- these resolve from the server root. If serving from a subfolder, the icons may not load, but this is acceptable for local development (the map markers will still render as circles)
- fleet-map.js is referenced directly from `../public/js/fleet-map.js` (NOT copied, unlike pricing.js, because fleet-map.js doesn't have path issues)

After creation, recompile Tailwind:
```bash
cd site && npx @tailwindcss/cli -i epic/b/style.css -o epic/b/tailwind-out.css
```
  </action>
  <verify>
    <automated>cd C:/Dev/Repos/TeilAuto/site && npx @tailwindcss/cli -i epic/b/style.css -o epic/b/tailwind-out.css && grep -c "fleet-map" epic/b/fahrzeuge.html && grep -c "maplibre" epic/b/fahrzeuge.html</automated>
  </verify>
  <done>
- fahrzeuge.html exists with fleet-map container (id + class)
- MapLibre GL JS 5.17.0 referenced from CDN (both CSS and JS)
- fleet-map.js referenced from ../public/js/fleet-map.js
- Vehicle cards for Opel Mokka-e and VW up! with Design B data-forward style
- Quernutzung section present
- Compact dark hero (not split), floating nav, footer, mobile phone bar
- Diagonal accent decorations present
- Tailwind compiles without errors
  </done>
</task>

</tasks>

<verification>
- Both pages compile with Tailwind without errors
- preise.html has the three pricing container IDs
- fahrzeuge.html has fleet-map container with correct ID and class
- Both pages share consistent nav, footer, and visual signature with index.html
</verification>

<success_criteria>
Preise page loads pricing data from pricing.json via adapted pricing.js. Fahrzeuge page renders MapLibre map at correct container height. Both pages maintain Design B's visual identity (floating nav, diagonal accents, lime on green palette) and are responsive.
</success_criteria>

<output>
After completion, create `.planning/phases/28-epic-skin-b/28-02-SUMMARY.md`
</output>
