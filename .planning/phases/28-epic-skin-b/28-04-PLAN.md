---
phase: 28-epic-skin-b
plan: 04
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - site/epic/b/impressum.html
  - site/epic/b/datenschutz.html
autonomous: true
requirements: [IMPL-03, IMPL-04, VIS-03]

must_haves:
  truths:
    - "Impressum page displays full legal text in readable long-form layout"
    - "Datenschutz page displays full privacy policy in readable long-form layout"
    - "Both pages use full-width text layout (not split or multi-column) for legal readability"
    - "Both pages have floating pill nav, footer, mobile phone bar consistent with other pages"
  artifacts:
    - path: "site/epic/b/impressum.html"
      provides: "Legal impressum page"
      min_lines: 60
    - path: "site/epic/b/datenschutz.html"
      provides: "Privacy policy page"
      min_lines: 60
  key_links:
    - from: "site/epic/b/impressum.html"
      to: "site/epic/b/datenschutz.html"
      via: "footer link"
      pattern: 'href="datenschutz.html"'
---

<objective>
Build the two legal pages -- Impressum and Datenschutz -- with full-width readable long-form text layout.

Purpose: Legal compliance pages. Must accommodate long German legal text while maintaining Design B's visual identity in nav, footer, and subtle decorative touches.
Output: Two legal pages ready for content.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-epic-skin-b/28-RESEARCH.md
@.planning/phases/28-epic-skin-b/28-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build impressum.html and datenschutz.html legal pages</name>
  <files>site/epic/b/impressum.html, site/epic/b/datenschutz.html</files>
  <action>
Create both legal pages. These are simpler than content pages -- full-width text with minimal decoration. Same head, nav, footer pattern as all other pages.

**impressum.html** -- Legal imprint page:

1. Same `<head>` (fonts, tailwind-out.css, base.css). Title: "Impressum | teilAuto Moessingen"

2. Skip-to-content link, floating pill nav (no aria-current on main nav items -- legal pages aren't in primary nav), mobile nav overlay

3. Compact dark hero: `bg-brand-primary`, `pt-28 pb-12`. Heading "Impressum". Single diagonal accent line.

4. **Legal content section**: Full-width readable layout. `max-w-3xl mx-auto px-4 py-12`. Use `prose`-style typography:
   - `text-base leading-relaxed text-brand-ink/80`
   - Headings in `font-display font-bold text-brand-ink`
   - Sections: Angaben gemaess 5 TMG, Kontakt, Verantwortlich fuer den Inhalt

   Content (verified from v1.2 impressum):
   ```
   teilAuto Moessingen e.K.
   [Address from verified content]

   Kontakt:
   Telefon: 07473-922202

   Verantwortlich fuer den Inhalt nach 55 Abs. 2 RStV:
   [Owner name from verified content]
   ```

   Use the actual legal text from the existing v1.2 site's impressum page. If exact text isn't available, use the standard German Impressum structure with teilAuto Moessingen e.K. as the entity.

5. Footer, mobile bottom phone bar, nav.js

**datenschutz.html** -- Privacy policy page:

1. Same `<head>`. Title: "Datenschutz | teilAuto Moessingen"

2. Same nav pattern (no aria-current on main items)

3. Compact dark hero: heading "Datenschutzerklaerung". Diagonal accent.

4. **Privacy content section**: Same full-width readable layout as impressum. `max-w-3xl mx-auto px-4 py-12`.

   Content: Use the simplified Datenschutzerklaerung from Phase 11 (v1.0). Key sections:
   - Verantwortliche Stelle (teilAuto Moessingen e.K.)
   - Hosting (statische Website, kein Tracking)
   - Externe Dienste (Google Fonts, MapLibre)
   - Kontakt (Telefon)
   - Ihre Rechte (DSGVO rights)
   - Note: No analytics/tracking currently implemented (per v1.0 Phase 11 simplified version)

   Long-form text with appropriate headings (h2, h3) and paragraphs. Use `break-words` / `overflow-wrap: break-word` on the content container to handle long German compound words like "Datenschutzerklaerung" at narrow widths.

5. Footer, mobile bottom phone bar, nav.js

**Design B touches on legal pages** (subtle, not overwhelming):
- Compact dark hero with diagonal accent (consistent brand entry)
- `focus-visible` outline in `brand-accent` color
- Footer with diagonal accent line
- Section dividers between major content blocks using subtle diagonal rule

**Critical**: Legal pages must use full-width layout (`max-w-3xl`), NOT the split layout or dense card grid used elsewhere. Legal text needs maximum readability. Add `hyphens: auto` CSS to the text container to help with German word breaks.

After creating both files, recompile Tailwind:
```bash
cd site && npx @tailwindcss/cli -i epic/b/style.css -o epic/b/tailwind-out.css
```
  </action>
  <verify>
    <automated>cd C:/Dev/Repos/TeilAuto/site && npx @tailwindcss/cli -i epic/b/style.css -o epic/b/tailwind-out.css && test -f epic/b/impressum.html && test -f epic/b/datenschutz.html && echo "Both legal pages exist"</automated>
  </verify>
  <done>
- impressum.html exists with full legal text in max-w-3xl readable layout
- datenschutz.html exists with privacy policy in max-w-3xl readable layout
- Both pages have compact dark hero, floating nav, footer, mobile phone bar
- Legal text uses full-width layout (not split columns)
- Long German words don't cause horizontal overflow (break-words / hyphens)
- Tailwind compiles without errors
  </done>
</task>

</tasks>

<verification>
- Both pages compile with Tailwind
- Both pages have full-width text layout for legal readability
- Nav and footer consistent with all other pages
- No horizontal overflow from long German legal terms
</verification>

<success_criteria>
Both legal pages exist with proper legal content, full-width readable layout, and consistent Design B chrome (nav, footer, hero, accent elements). Legal text accommodates long German compound words without overflow.
</success_criteria>

<output>
After completion, create `.planning/phases/28-epic-skin-b/28-04-SUMMARY.md`
</output>
