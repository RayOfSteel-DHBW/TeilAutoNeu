---
phase: 34-epic-skin-b-polish-loop
plan: 01
type: execute
wave: 1
depends_on:
  - 33-01
files_modified:
  - site/epic/b/index.html
  - site/epic/b/preise.html
  - site/epic/b/fahrzeuge.html
  - site/epic/b/geschaeftskunden.html
  - site/epic/b/ueber-uns.html
  - site/epic/b/mitglied-werden.html
  - site/epic/b/impressum.html
  - site/epic/b/datenschutz.html
  - site/epic/b/style.css
  - site/epic/b/base.css
  - site/epic/b/js/nav.js
  - site/epic/b/js/pricing.js
  - .planning/phases/34-epic-skin-b-polish-loop/34-ITERATION-LOG.md
  - .planning/phases/34-epic-skin-b-polish-loop/34-01-SUMMARY.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - AUDIT-03
  - LOOP-01
  - LOOP-02
  - LOOP-03
  - READY-B-01

must_haves:
  truths:
    - "Execution does not start if another browser-driven phase is active or the shared local browser is already in use"
    - "Every executed iteration reviews all 8 Design B pages at 375px, 768px, and 1280px with Playwright"
    - "Every fix pass starts with a written TODO list naming page, location, and issue before any edits begin"
    - "All issues from one pass are fixed in one batch before the verification step begins"
    - "Audit checks both visual quality and page-owned adherence to .planning/milestones/v1.0-REQUIREMENTS.md"
    - "All edits stay inside site/epic/b/ unless a genuinely shared blocker is reported instead of being fixed locally"
    - "Design B structural identity remains intact; this phase polishes, not redesigns"
    - "The loop stops on a clean pass, a hard blocker, missing implementation, or after 5 iterations with unresolved non-nitpick issues"
    - "A clean finish records readiness for Phase 35, while blockers or iteration-cap holds stop the chain explicitly"
  artifacts:
    - path: ".planning/phases/34-epic-skin-b-polish-loop/34-ITERATION-LOG.md"
      provides: "Preflight notes plus per-iteration TODO, fix, and verification records"
      min_lines: 12
    - path: ".planning/phases/34-epic-skin-b-polish-loop/34-01-SUMMARY.md"
      provides: "Final status with iteration count, blockers, fixes, verification result, and next-step readiness"
      min_lines: 10
---

<objective>
Run a bounded sequential polish loop for `site/epic/b/` only: review every page with Playwright at `375px`, `768px`, and `1280px`, record a TODO list before each fix pass, batch-fix all issues from that pass, and verify until clean or the 5-iteration cap is reached.

Purpose: Bring Design B to review-ready quality after v1.3 implementation by checking both visual quality and the v1.0 requirements floor without leaking into shared review-surface work or later skin phases.
Output: `34-ITERATION-LOG.md`, any local Design B fixes, and `34-01-SUMMARY.md` with either a clean finish or an explicit stop state.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/v1.4-MILESTONE-BRIEF.md
@.planning/v1.4-REQUIREMENTS-DRAFT.md
@.planning/v1.4-ROADMAP-DRAFT.md
@.planning/v1.4-EXECUTION-PROMPTS.md
@.planning/milestones/v1.0-REQUIREMENTS.md
@.planning/milestones/v1.0-ROADMAP.md
@.planning/phases/28-epic-skin-b/28-RESEARCH.md
@.planning/phases/28-epic-skin-b/28-05-SUMMARY.md
@.planning/phases/34-epic-skin-b-polish-loop/34-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight shared-browser gate and local-only scope</name>
  <files>.planning/phases/34-epic-skin-b-polish-loop/34-ITERATION-LOG.md, .planning/phases/34-epic-skin-b-polish-loop/34-01-SUMMARY.md</files>
  <action>
Before opening Playwright, perform a hard preflight:

1. Read `.planning/STATE.md`, `.planning/current-agent-id.txt` if present, and the current terminal/app context.
2. If another browser-driven phase is actively running, or the shared browser session is already in use, STOP immediately. Do not launch Playwright and do not touch Design B source files. Write a short blocker note in `34-01-SUMMARY.md` instead.
3. Confirm all 8 Design B pages exist in `site/epic/b/`.
4. Confirm local support files exist and remain Design-B-local: `style.css`, `base.css`, `tailwind-out.css`, `js/nav.js`, `js/pricing.js`.
5. Serve only the `site/` directory at a stable local URL that preserves direct `.html` paths.
6. If you discover a genuinely shared blocker, report it in `34-01-SUMMARY.md` and stop. Do not absorb shared work into this phase.
7. Initialize `34-ITERATION-LOG.md` with a preflight section that records timestamp, served URL, scope boundary, and whether execution is cleared to begin.
  </action>
  <verify>
Confirm the preflight section exists in `34-ITERATION-LOG.md`, the served URL resolves to `http://127.0.0.1:5500/epic/b/index.html` or an equivalent local path, and all 8 target HTML files exist before any fix loop begins.
  </verify>
  <done>Preflight either clears execution safely or stops the phase without opening a conflicting browser session.</done>
</task>

<task type="auto">
  <name>Task 2: Iterative Playwright audit loop with per-pass TODO log</name>
  <files>site/epic/b/index.html, site/epic/b/preise.html, site/epic/b/fahrzeuge.html, site/epic/b/geschaeftskunden.html, site/epic/b/ueber-uns.html, site/epic/b/mitglied-werden.html, site/epic/b/impressum.html, site/epic/b/datenschutz.html, site/epic/b/style.css, site/epic/b/base.css, site/epic/b/js/nav.js, site/epic/b/js/pricing.js, .planning/phases/34-epic-skin-b-polish-loop/34-ITERATION-LOG.md</files>
  <action>
Run up to 5 full iterations in the exact order `review -> TODO -> batch fix -> verify`.

- Review all 8 pages at `375px`, `768px`, and `1280px`.
- Check both visual quality and page-owned v1.0 requirement adherence.
- Before making changes, append `## Iteration N TODO` with a table of page, location, issue, requirement/gate, severity, and planned fix.
- Fix every issue from that iteration in one batch inside `site/epic/b/`.
- Rebuild Tailwind after HTML/CSS changes:
  `cd site && npx @tailwindcss/cli -i epic/b/style.css -o epic/b/tailwind-out.css --content "epic/b/**/*.html"`
- Re-verify every touched page at all three breakpoints and append the result under the same iteration.
- Stop on the first clean pass, a hard blocker, missing implementation, or the 5-iteration cap.
  </action>
  <verify>
For every executed iteration, `34-ITERATION-LOG.md` shows a TODO section before fixes begin, touched pages are re-verified at `375px`, `768px`, and `1280px`, and no fix pass is split into per-issue mini-runs.
  </verify>
  <done>The loop ends on a clean pass or on an explicit stop state after Iteration 5.</done>
</task>

<task type="auto">
  <name>Task 3: Final verification, summary, and next-phase handoff</name>
  <files>.planning/phases/34-epic-skin-b-polish-loop/34-ITERATION-LOG.md, .planning/phases/34-epic-skin-b-polish-loop/34-01-SUMMARY.md</files>
  <action>
After the loop ends, write `34-01-SUMMARY.md` with:
- preflight outcome
- number of iterations executed
- high-level issues fixed
- any blockers or remaining nitpicks
- whether the phase finished clean, hit the cap, or stopped on a blocker
- explicit statement that a clean pass is ready for Phase 35, while blockers or cap holds stop continuation
  </action>
  <verify>
`34-01-SUMMARY.md` clearly states one of three outcomes: `blocked before start`, `clean pass complete`, or `iteration cap reached - waiting for user`. It also makes the next-phase readiness explicit.
  </verify>
  <done>Phase 34 ends with a clear written status and either a ready-for-35 handoff or an explicit stop boundary.</done>
</task>

</tasks>

<verification>
- Preflight confirms the browser is free or stops before Playwright opens
- `34-ITERATION-LOG.md` contains preflight notes plus a TODO section before each executed fix pass
- Every executed iteration reviews all 8 Design B pages at `375px`, `768px`, and `1280px`
- Audit covers both visual quality and page-owned v1.0 requirement adherence
- All source edits remain inside `site/epic/b/` except phase-local notes and summary files
- The loop stops on a clean pass or at the 5-iteration cap with a wait state
- `34-01-SUMMARY.md` makes readiness for Phase 35 or an explicit stop condition unambiguous
</verification>

<success_criteria>
1. Design B reaches review-ready quality or an explicit user-approved hold state after the 5-iteration cap
2. Every fix batch is preceded by a written TODO list with page, location, and issue
3. No shared review-surface work is pulled into this phase
4. No browser conflict is created by starting while another browser-driven phase is active
5. The final written status makes the next action unambiguous: ready for Phase 35 on clean pass, otherwise stop and review
</success_criteria>

<output>
After completion, create `.planning/phases/34-epic-skin-b-polish-loop/34-01-SUMMARY.md`
</output>
