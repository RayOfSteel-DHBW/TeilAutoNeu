---
phase: 27-epic-skin-a
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - site/epic/a/preise.html
  - site/epic/a/fahrzeuge.html
autonomous: true
requirements: [IMPL-03, IMPL-04, VIS-02, VIS-03, CONV-03]

must_haves:
  truths:
    - "Preise page renders pricing data from pricing.js into editorial ruled layout"
    - "Fahrzeuge page renders MapLibre map at 420-520px height with fleet markers"
    - "Both pages use sticky dark nav bar and footer consistent with homepage"
    - "Both pages use horizontal rule separators between sections (no cards)"
    - "Phone CTA appears in nav and mobile fixed bottom bar on both pages"
  artifacts:
    - path: "site/epic/a/preise.html"
      provides: "Pricing page with editorial ruled layout and pricing.js integration"
      min_lines: 150
    - path: "site/epic/a/fahrzeuge.html"
      provides: "Fleet page with MapLibre map, vehicle info, and editorial layout"
      min_lines: 200
  key_links:
    - from: "site/epic/a/preise.html"
      to: "site/epic/a/js/pricing.js"
      via: "script src"
      pattern: 'src="js/pricing.js"'
    - from: "site/epic/a/fahrzeuge.html"
      to: "site/epic/a/js/fleet-map.js"
      via: "script src"
      pattern: 'src="js/fleet-map.js"'
    - from: "site/epic/a/fahrzeuge.html"
      to: "MapLibre GL JS CDN"
      via: "script src"
      pattern: "maplibre-gl@5.17.0"
---

<objective>
Build the two data-driven pages (Preise and Fahrzeuge) with their JavaScript integrations in the editorial broadsheet style.

Purpose: These are the most technically complex pages -- pricing.js renders dynamic pricing tables and fleet-map.js powers the interactive map. Both must work correctly with the corrected relative paths while maintaining the editorial ruled layout identity.
Output: Working preise.html and fahrzeuge.html in site/epic/a/.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-epic-skin-a/27-RESEARCH.md
@.planning/phases/27-epic-skin-a/27-01-SUMMARY.md

Source content reference (v1.2 -- extract German copy, do NOT copy structure):
@site/src/preise.html
@site/src/fahrzeuge.html

JS integration patterns:
@site/epic/a/js/pricing.js
@site/epic/a/js/fleet-map.js

Design A identity reference:
@site/epic/a/index.html
@site/epic/a/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Preise page with pricing.js integration and editorial ruled layout</name>
  <files>site/epic/a/preise.html</files>
  <action>
Create `site/epic/a/preise.html` as a standalone HTML file.

**Structure:**
- Same head section as index.html (fonts, tailwind-out.css, charset, viewport)
- Title: "Preise | teilAuto Moessingen"
- Meta description about transparent carsharing pricing
- Include `<script src="js/pricing.js" defer></script>` in head

**Sticky dark nav** -- Copy nav from index.html. Update active page indicator: `border-b-2 border-brand-accent` on "Preise" link (remove from Startseite).

**Page content sections (all with `border-b border-brand-muted py-16` separators):**

1. **Page header section:**
   - Playfair Display h1: "Transparente Preise"
   - Subline explaining the membership-based pricing model
   - Left-aligned, max-w-5xl container

2. **Pricing values section** (Pattern 9 from research):
   - `<div id="pricing-values"></div>` inside max-w-5xl container
   - pricing.js will render tariff values into this div
   - The JS-rendered content uses Tailwind classes that pick up @theme tokens

3. **Pricing examples section:**
   - `<div id="pricing-examples"></div>` inside max-w-5xl container
   - pricing.js renders example trip calculations here

4. **Pricing disclaimer section:**
   - `<div id="pricing-disclaimer"></div>` inside max-w-5xl container
   - pricing.js renders legal disclaimer text

5. **Membership info teaser:**
   - Brief editorial text about how to become a member
   - Link to mitglied-werden.html
   - Amber accent rule before this section

6. **CTA block** -- phone number + "Mitglied werden" link

**Footer** -- Same as index.html (dark navy, 3-col grid).

**Mobile fixed bottom bar** -- Same as index.html.

**IMPORTANT:** Accept pricing.js output styling as-is (it renders with `rounded-2xl` card-like containers). This is the one acceptable exception to the no-cards rule since pricing data is structured tabular content. The editorial identity is maintained through the page-level ruled sections.

**After creating, rebuild Tailwind:**
```bash
cd site && npx @tailwindcss/cli -i epic/a/style.css -o epic/a/tailwind-out.css --content "epic/a/**/*.html"
```
  </action>
  <verify>
    <automated>cd C:/Dev/Repos/TeilAuto/site && test -f epic/a/preise.html && grep -q "pricing-values" epic/a/preise.html && grep -q "pricing.js" epic/a/preise.html && grep -q "bg-brand-primary" epic/a/preise.html && echo "preise.html OK"</automated>
  </verify>
  <done>preise.html exists with pricing.js script tag. Contains #pricing-values, #pricing-examples, #pricing-disclaimer divs. Sticky dark nav with Preise marked active. Horizontal rule separators between sections. Footer matches homepage. Mobile fixed bottom bar present.</done>
</task>

<task type="auto">
  <name>Task 2: Build Fahrzeuge page with MapLibre map and editorial fleet display</name>
  <files>site/epic/a/fahrzeuge.html</files>
  <action>
Create `site/epic/a/fahrzeuge.html` as a standalone HTML file.

**Head section:**
- Same as other pages plus MapLibre dependencies:
  ```html
  <link href="https://unpkg.com/maplibre-gl@5.17.0/dist/maplibre-gl.css" rel="stylesheet" />
  <script src="https://unpkg.com/maplibre-gl@5.17.0/dist/maplibre-gl.js" defer></script>
  <script src="js/fleet-map.js" defer></script>
  ```
- Title: "Fahrzeuge &amp; Standorte | teilAuto Moessingen"

**Sticky dark nav** -- Copy from index.html. Active indicator on "Fahrzeuge".

**Page content sections (editorial ruled layout):**

1. **Page header:**
   - Playfair Display h1: "Unsere Fahrzeuge &amp; Standorte"
   - Subline about the fleet and locations
   - Left-aligned, max-w-5xl

2. **Interactive map section** (Pattern 8 from research):
   - `<div id="fleet-map" class="fleet-map mt-8"></div>`
   - Inside max-w-5xl container with border-b separator
   - fleet-map.js initializes MapLibre into this div
   - The .fleet-map CSS class in base.css handles responsive heights (420-520px)

3. **Vehicle details section:**
   - Editorial text blocks for each vehicle (Opel Mokka-e, Opel Adam)
   - Two-column grid on desktop (md:grid-cols-2), single column mobile
   - Vehicle specs: type, seats, fuel/electric, trunk size
   - NO card containers -- text directly on surface with ruled separators
   - Reference vehicle images: `<img src="../../public/img/cars/mokka-placeholder.svg" ...>` (or similar path pattern from fleet-map.js)
   - Content sourced from v1.2 fahrzeuge.html German copy

4. **Parking locations text section:**
   - List of active locations with addresses
   - Mention of planned locations (Don Bosco)
   - Left-aligned editorial layout

5. **Quernutzung/BCS network brief:**
   - "Fahrzeuge von ueber 200 Partnern" -- brief mention of cross-use network
   - NOT a headline feature, just a notable benefit
   - Link for more info

6. **CTA block** -- phone number + membership link

**Footer + Mobile fixed bottom bar** -- consistent with all pages.

**Rebuild Tailwind after creating.**
  </action>
  <verify>
    <automated>cd C:/Dev/Repos/TeilAuto/site && test -f epic/a/fahrzeuge.html && grep -q "fleet-map" epic/a/fahrzeuge.html && grep -q "maplibre-gl" epic/a/fahrzeuge.html && grep -q "fleet-map.js" epic/a/fahrzeuge.html && echo "fahrzeuge.html OK"</automated>
  </verify>
  <done>fahrzeuge.html exists with MapLibre CSS/JS CDN links and fleet-map.js script tag. Contains #fleet-map div with fleet-map class. Vehicle details in editorial layout (no cards). Sticky dark nav with Fahrzeuge active. Horizontal rule separators. Footer and mobile bottom bar present.</done>
</task>

</tasks>

<verification>
- Open preise.html in browser -- pricing.js should fetch from ../../public/data/pricing.json and render pricing tables
- Open fahrzeuge.html in browser -- MapLibre map should render with fleet markers at correct locations
- Both pages maintain editorial ruled layout (border-b separators, not cards)
- Nav bar is sticky dark navy with correct active page indicators
- Phone CTA visible on both pages
</verification>

<success_criteria>
Both data-driven pages render correctly with their JavaScript integrations. Pricing data loads from pricing.json. Map renders with markers. Editorial broadsheet identity maintained -- horizontal rules, left-aligned text, no card creep.
</success_criteria>

<output>
After completion, create `.planning/phases/27-epic-skin-a/27-02-SUMMARY.md`
</output>
