---
phase: 27-epic-skin-a
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - site/epic/a/impressum.html
autonomous: true
gap_closure: true
requirements:
  - IMPL-04

must_haves:
  truths:
    - "impressum.html contains no [TODO] placeholders"
    - "Handelsregister shows Amtsgericht Stuttgart and HRA 737813"
    - "Umsatzsteuer-Identifikationsnummer section is either filled or removed to match Design B"
  artifacts:
    - path: "site/epic/a/impressum.html"
      provides: "Legal page with correct business registration data"
      contains: "Amtsgericht Stuttgart"
  key_links: []
---

<objective>
Fix [TODO] placeholders in Design A's impressum.html by copying the correct legal data from Design B.

Purpose: Design A impressum.html has 3 [TODO] placeholders (lines 94, 96, 103) for Handelsregister and USt-ID data. Design B already has the real data. This is a content copy oversight that makes Design A not production-ready.
Output: impressum.html with correct legal data, no [TODO] badges.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-epic-skin-a/27-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace [TODO] placeholders in impressum.html with real legal data</name>
  <files>site/epic/a/impressum.html</files>
  <action>
Edit site/epic/a/impressum.html to fix the 3 [TODO] placeholders:

1. **Line 93-94** (Registergericht): Replace the [TODO] span with plain text "Stuttgart" so the line reads:
   `Registergericht: Amtsgericht Stuttgart`
   Remove the entire `<span class="inline-flex items-center rounded bg-amber-100...">` element.

2. **Line 95-96** (Registernummer): Replace the [TODO] span with plain text "HRA 737813" so the line reads:
   `Registernummer: HRA 737813`
   Remove the entire `<span>` element.

3. **Lines 100-104** (Umsatzsteuer-Identifikationsnummer): Design B omits this section entirely (the USt-ID is not publicly available for this business). Remove the entire `<div class="space-y-3">` block containing the USt-ID heading and [TODO] span (lines 100-105).

Data source: Design B's impressum.html (site/epic/b/impressum.html) lines 154-155 has "Amtsgericht Stuttgart" and "HRA 737813". Design B omits USt-ID section.

After editing, rebuild Tailwind CSS:
```bash
cd site && npx @tailwindcss/cli -i epic/a/style.css -o epic/a/tailwind-out.css --content "epic/a/**/*.html"
```
  </action>
  <verify>
    <automated>grep -c "\[TODO\]" site/epic/a/impressum.html | grep "^0$" && grep "Amtsgericht Stuttgart" site/epic/a/impressum.html && grep "HRA 737813" site/epic/a/impressum.html && echo "PASS"</automated>
  </verify>
  <done>impressum.html has zero [TODO] placeholders, shows "Amtsgericht Stuttgart" and "HRA 737813", USt-ID section removed</done>
</task>

</tasks>

<verification>
- `grep "\[TODO\]" site/epic/a/impressum.html` returns no matches
- `grep "Amtsgericht Stuttgart" site/epic/a/impressum.html` returns a match
- `grep "HRA 737813" site/epic/a/impressum.html` returns a match
- `grep "Umsatzsteuer" site/epic/a/impressum.html` returns no matches
</verification>

<success_criteria>
Design A's impressum.html contains the same legal registration data as Design B, with no [TODO] placeholders remaining.
</success_criteria>

<output>
After completion, create `.planning/phases/27-epic-skin-a/27-06-SUMMARY.md`
</output>
