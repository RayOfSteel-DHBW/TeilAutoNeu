---
phase: 27-epic-skin-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/epic/a/style.css
  - site/epic/a/base.css
  - site/epic/a/js/nav.js
  - site/epic/a/js/pricing.js
  - site/epic/a/js/fleet-map.js
  - site/epic/a/js/accordion.js
  - site/epic/a/index.html
autonomous: true
requirements: [IMPL-01, IMPL-02, IMPL-03, VIS-01, VIS-04, CONV-01, CONV-02, CONV-03, CONV-04]

must_haves:
  truths:
    - "Design A directory exists at site/epic/a/ with its own @theme tokens (navy/amber palette)"
    - "Homepage renders a full-bleed immersive hero (85vh, dark navy bg, content layered bottom-left)"
    - "Homepage section ordering follows storytelling arc: hero -> amber accent rule -> membership gate -> convenience benefits -> trust proof -> cost teaser -> how-it-works -> final CTA -> footer"
    - "Sticky dark navy nav bar renders with phone CTA visible on desktop"
    - "Phone CTA 07473-922202 appears in nav bar (desktop) and fixed bottom bar (mobile)"
    - "Horizontal typographic rules (visual signature) separate content sections"
    - "Left-aligned reading flow throughout -- no centered content blocks"
    - "Playfair Display serif headings + Inter sans body creates editorial broadsheet identity"
  artifacts:
    - path: "site/epic/a/style.css"
      provides: "Tailwind v4 @import + @source + @theme block with Design A tokens"
      contains: "@theme"
    - path: "site/epic/a/base.css"
      provides: "Base styles, fleet-map container, heading clamps, focus-visible, fleet-marker/popup styles"
      contains: "fleet-map"
    - path: "site/epic/a/js/nav.js"
      provides: "Adapted nav.js for sticky dark nav bar with hamburger toggle"
    - path: "site/epic/a/js/pricing.js"
      provides: "Adapted pricing.js with PRICING_URL pointing to ../../public/data/pricing.json"
      contains: "PRICING_URL"
    - path: "site/epic/a/js/fleet-map.js"
      provides: "Adapted fleet-map.js with iconUrl paths pointing to ../../public/img/cars/"
      contains: "iconUrl"
    - path: "site/epic/a/js/accordion.js"
      provides: "Copy of accordion.js for FAQ sections"
    - path: "site/epic/a/index.html"
      provides: "Homepage with full-bleed immersive hero and storytelling section ordering"
      min_lines: 300
  key_links:
    - from: "site/epic/a/index.html"
      to: "site/epic/a/tailwind-out.css"
      via: "link rel stylesheet"
      pattern: 'href="tailwind-out.css"'
    - from: "site/epic/a/index.html"
      to: "site/epic/a/js/nav.js"
      via: "script src"
      pattern: 'src="js/nav.js"'
    - from: "site/epic/a/index.html"
      to: "mitglied-werden.html"
      via: "CTA link"
      pattern: 'href="mitglied-werden.html"'
    - from: "site/epic/a/js/pricing.js"
      to: "site/public/data/pricing.json"
      via: "fetch URL"
      pattern: "../../public/data/pricing.json"
---

<objective>
Create the Design A foundation (theme tokens, base styles, shared JS) and the homepage with its structurally distinct full-bleed immersive hero and editorial broadsheet layout.

Purpose: Establishes the entire Design A identity -- dark navy/amber palette, Playfair Display serif headings, horizontal typographic rules, left-aligned editorial reading flow, sticky dark nav bar -- and builds the homepage with a storytelling section ordering (convenience -> trust -> cost -> action) that differs from all other designs.
Output: Working homepage at site/epic/a/index.html with all supporting CSS and JS files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-epic-skin-a/27-RESEARCH.md

Source content reference (v1.2 Tera templates -- extract German copy, do NOT copy structure):
@site/src/index.html

Shared JS files to copy and adapt:
@site/public/js/nav.js
@site/public/js/pricing.js
@site/public/js/fleet-map.js
@site/public/js/accordion.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Design A foundation -- style.css, base.css, JS copies</name>
  <files>site/epic/a/style.css, site/epic/a/base.css, site/epic/a/js/nav.js, site/epic/a/js/pricing.js, site/epic/a/js/fleet-map.js, site/epic/a/js/accordion.js</files>
  <action>
Create the `site/epic/a/` directory and all foundation files.

**style.css** -- Tailwind v4 entry point with Design A tokens:
```css
@import "tailwindcss";
@source "*.html";

@theme {
  --color-brand-primary: #1a1a2e;
  --color-brand-accent: #c2703e;
  --color-brand-surface: #faf8f5;
  --color-brand-ink: #2d2d3a;
  --color-brand-muted: #e8e4de;
  --color-brand-cta: #b85c2f;
  --font-sans: "Inter", system-ui, sans-serif;
  --font-display: "Playfair Display", "Georgia", serif;
}
```

**base.css** -- Base styles adapted for Design A editorial identity. Use the code example from 27-RESEARCH.md "base.css for Design A" section verbatim. This includes:
- `@layer base` with body font, heading font-family (Playfair Display), clamp() fluid sizes, link colors (amber), focus-visible (amber)
- `.fleet-map` container (420-520px responsive heights, 0.5rem border-radius, `brand-muted` border)
- `.fleet-marker` and `.fleet-marker--planned` styles with amber accent colors
- `.fleet-popup` styles and `.maplibregl-popup-content` overrides

Import base.css from style.css by adding `@import "./base.css";` AFTER the `@theme` block.

**JS copies** -- Copy all 4 files from `site/public/js/` to `site/epic/a/js/`:
1. `pricing.js`: Change `PRICING_URL` (or equivalent variable holding the data URL) to `"../../public/data/pricing.json"`
2. `fleet-map.js`: Change all `iconUrl` paths from absolute (`/img/cars/...`) to relative (`../../public/img/cars/...`)
3. `nav.js`: Copy as-is. The nav.js handles hamburger toggle with aria-expanded, Escape key close, and active page detection. It should work with the sticky dark nav structure.
4. `accordion.js`: Copy as-is.

**Compile Tailwind** to verify foundation works:
```bash
cd site && npx @tailwindcss/cli -i epic/a/style.css -o epic/a/tailwind-out.css --content "epic/a/**/*.html"
```
Note: This will produce minimal output until HTML files exist, but should compile without errors.
  </action>
  <verify>
    <automated>cd C:/Dev/Repos/TeilAuto/site && npx @tailwindcss/cli -i epic/a/style.css -o epic/a/tailwind-out.css --content "epic/a/**/*.html" 2>&1 | head -5</automated>
  </verify>
  <done>style.css compiles without errors. base.css exists with fleet-map styles. All 4 JS files exist in site/epic/a/js/ with corrected paths. pricing.js contains "../../public/data/pricing.json". fleet-map.js contains "../../public/img/cars/".</done>
</task>

<task type="auto">
  <name>Task 2: Build homepage with full-bleed immersive hero and editorial broadsheet layout</name>
  <files>site/epic/a/index.html</files>
  <action>
Create `site/epic/a/index.html` as a standalone HTML file (no template inheritance). Use the page template pattern from 27-RESEARCH.md.

**Head section:**
- `<!DOCTYPE html>`, `<html lang="de">`, `<meta charset="UTF-8">`, viewport meta
- Title: "teilAuto Moessingen | Carsharing fuer Moessingen und Umgebung"
- Meta description targeting Simone persona
- Google Fonts preconnect + link for Inter (400,500,600) and Playfair Display (500,700,900)
- `<link rel="stylesheet" href="tailwind-out.css">`

**Sticky dark nav bar** -- Use the Pattern 6 from research verbatim:
- `sticky top-0 z-50 bg-brand-primary shadow-md`
- Logo "teilAuto" in white, Inter 600
- Desktop nav links: Startseite, Preise, Fahrzeuge, Fuer Firmen, Ueber uns, Mitglied werden
- Phone number `07473-922202` as amber `tel:` link on desktop
- Mobile hamburger button with aria-controls, aria-expanded, aria-label
- Mobile nav overlay with full-height links + amber phone CTA button
- Active page: `border-b-2 border-brand-accent` on "Startseite" link
- Use umlauts via HTML entities: `&uuml;` for ue, `&ouml;` for oe, `&auml;` for ae, `&szlig;` for ss

**Homepage sections in THIS EXACT ORDER (Design A storytelling arc):**

1. **Full-bleed immersive hero** (Pattern 3 from research):
   - `min-h-[85vh]`, dark navy gradient background, subtle geometric overlay lines
   - Content at bottom-left via `items-end pb-20`
   - Playfair Display headline: "Das Auto auf Abruf" (or similar Simone-targeting headline)
   - Subline in `text-white/80`: brief value prop about practical carsharing
   - Two CTAs: "Mitglied werden" (amber bg) + "Mehr erfahren" (white border) -- `rounded-md` NOT `rounded-full`
   - All text explicitly `text-white` or `text-white/80` (do NOT inherit body color)

2. **Amber accent rule** -- `<hr class="border-t-2 border-brand-accent">` in max-w-5xl container

3. **Membership gate** -- Single editorial line:
   - `<p class="font-sans text-base italic text-brand-ink/70 font-medium">`
   - Text: "Machen Sie mit -- werden Sie Mitglied bei teilAuto und sparen Sie bares Geld."
   - Brief note that teilAuto requires membership (not one-time rental)

4. **Convenience benefits** (editorial two-column grid, Pattern 5):
   - `border-b border-brand-muted py-16`
   - Playfair Display h2: "Ihr Auto, wenn Sie es brauchen"
   - 4 benefits in 2-col grid (md:grid-cols-2): practical daily use, no maintenance, no insurance costs, flexibility
   - NO cards, NO rounded containers -- text directly on brand-surface
   - Content sourced from v1.2 index.html, reframed for convenience-first storytelling

5. **Trust proof** -- "Persoenlich statt anonym" section:
   - `border-b border-brand-muted py-16`
   - Family-run emphasis, 60+ members, local roots in Moessingen
   - Left-aligned editorial text block

6. **Cost teaser** -- Brief pricing preview:
   - `border-b border-brand-muted py-16`
   - "Was kostet Carsharing?" heading
   - 2-3 sentence teaser with link to preise.html
   - NO full pricing table -- just enough to intrigue

7. **How it works** -- 3-step process:
   - `border-b border-brand-muted py-16`
   - Steps: Anrufen (07473-922202), Kennenlernen (Probefahrt), Losfahren
   - Can be centered for this specific element (exception to left-aligned rule)

8. **Final CTA block**:
   - Amber accent rule above
   - Phone number at large size, amber color
   - "Mitglied werden" button link

9. **Footer** (Pattern from research):
   - `bg-brand-primary py-12 text-white/80`
   - 3-col grid: brand info, navigation links, contact + legal links
   - Phone number as amber `tel:` link

**Mobile fixed bottom bar:**
- Fixed position bottom bar on mobile (below md) with phone CTA `tel:` link
- `fixed bottom-0 left-0 right-0 z-40 md:hidden bg-brand-primary py-3 px-4`
- "Jetzt anrufen: 07473-922202" in amber

**After creating index.html, rebuild Tailwind:**
```bash
cd site && npx @tailwindcss/cli -i epic/a/style.css -o epic/a/tailwind-out.css --content "epic/a/**/*.html"
```

**ANTI-PATTERNS TO AVOID:**
- Do NOT use `rounded-full` buttons (use `rounded-md`)
- Do NOT add card containers (`rounded-* bg-* shadow-*`) around content sections
- Do NOT center all content -- left-aligned reading flow is a structural differentiator
- Do NOT use a light/white nav bar -- dark navy sticky bar is Design A's identity
- Do NOT use the v1.2 section ordering (hero -> gate -> benefits -> pricing -> CTA). Use the storytelling arc above.
  </action>
  <verify>
    <automated>cd C:/Dev/Repos/TeilAuto/site && npx @tailwindcss/cli -i epic/a/style.css -o epic/a/tailwind-out.css --content "epic/a/**/*.html" 2>&1 | head -3 && test -f epic/a/index.html && echo "index.html exists" && grep -c "border-brand-muted" epic/a/index.html | xargs -I{} echo "horizontal rules: {}" && grep -c "rounded-full" epic/a/index.html | xargs -I{} echo "rounded-full (should be 0): {}"</automated>
  </verify>
  <done>index.html exists with 300+ lines. Full-bleed immersive hero at 85vh with dark navy background. Storytelling section ordering matches spec (hero -> gate -> convenience -> trust -> cost -> how-it-works -> CTA -> footer). Horizontal rules separate sections. No rounded-full buttons. No card containers. Left-aligned reading flow. Sticky dark nav with phone CTA. Mobile fixed bottom bar with tel: link. Tailwind compiles successfully.</done>
</task>

</tasks>

<verification>
- `site/epic/a/style.css` contains `@theme` with navy/amber tokens
- `site/epic/a/base.css` contains fleet-map styles with Design A colors
- `site/epic/a/js/pricing.js` contains `../../public/data/pricing.json`
- `site/epic/a/js/fleet-map.js` contains `../../public/img/cars/`
- `site/epic/a/index.html` renders without Tailwind compilation errors
- Homepage has full-bleed hero (not split, not stacked)
- Homepage sections follow storytelling arc order
- Horizontal rules between sections (not cards)
- Nav is sticky dark navy (not floating pill, not light)
</verification>

<success_criteria>
Design A foundation is complete and homepage demonstrates all 5 structural differentiators: (1) full-bleed immersive hero, (2) editorial broadsheet layout with horizontal rules, (3) left-aligned reading flow, (4) sticky dark nav bar, (5) storytelling content arc.
</success_criteria>

<output>
After completion, create `.planning/phases/27-epic-skin-a/27-01-SUMMARY.md`
</output>
