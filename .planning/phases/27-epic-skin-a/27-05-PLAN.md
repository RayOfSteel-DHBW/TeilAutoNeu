---
phase: 27-epic-skin-a
plan: 05
type: execute
wave: 3
depends_on: ["27-01", "27-02", "27-03", "27-04"]
files_modified:
  - site/epic/a/index.html
  - site/epic/a/preise.html
  - site/epic/a/fahrzeuge.html
  - site/epic/a/geschaeftskunden.html
  - site/epic/a/ueber-uns.html
  - site/epic/a/mitglied-werden.html
  - site/epic/a/impressum.html
  - site/epic/a/datenschutz.html
  - site/epic/a/style.css
  - site/epic/a/base.css
autonomous: true
requirements: [IMPL-04, IMPL-05, IMPL-06, VIS-02, VIS-03, STRUCT-01, STRUCT-02, STRUCT-03, STRUCT-05]

must_haves:
  truths:
    - "All 8 pages render without layout breaks at 375px, 768px, and 1280px"
    - "No horizontal overflow on any page at any breakpoint"
    - "Full-bleed hero renders correctly on mobile (text visible, not clipped)"
    - "Navigation works on all breakpoints (hamburger on mobile, links on desktop)"
    - "Phone CTA is accessible on all breakpoints (nav on desktop, fixed bar on mobile)"
    - "Horizontal rule separators are visible on all breakpoints"
    - "Map renders at correct height on fahrzeuge page"
    - "Pricing data renders on preise page"
    - "FAQ accordion functions on ueber-uns page"
  artifacts:
    - path: "site/epic/a/tailwind-out.css"
      provides: "Final compiled Tailwind CSS with all utility classes used across 8 pages"
      min_lines: 100
  key_links:
    - from: "All 8 HTML pages"
      to: "site/epic/a/tailwind-out.css"
      via: "link stylesheet"
      pattern: 'href="tailwind-out.css"'
    - from: "site/epic/a/index.html"
      to: "All other 7 pages"
      via: "nav and footer links"
      pattern: "href=\"(preise|fahrzeuge|geschaeftskunden|ueber-uns|mitglied-werden|impressum|datenschutz).html\""
---

<objective>
Visual audit all 8 pages using Playwright screenshots at 375px, 768px, and 1280px. Fix any layout breaks, overflow, accessibility issues, or visual inconsistencies found.

Purpose: Production-ready quality requires that all pages work across all breakpoints without visual regressions. This plan catches and fixes issues that individual page creation may have missed.
Output: All 8 pages visually audited and fixed. Final Tailwind CSS compiled.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-epic-skin-a/27-RESEARCH.md
@.planning/phases/27-epic-skin-a/27-01-SUMMARY.md
@.planning/phases/27-epic-skin-a/27-02-SUMMARY.md
@.planning/phases/27-epic-skin-a/27-03-SUMMARY.md
@.planning/phases/27-epic-skin-a/27-04-SUMMARY.md

All 8 pages to audit:
@site/epic/a/index.html
@site/epic/a/preise.html
@site/epic/a/fahrzeuge.html
@site/epic/a/geschaeftskunden.html
@site/epic/a/ueber-uns.html
@site/epic/a/mitglied-werden.html
@site/epic/a/impressum.html
@site/epic/a/datenschutz.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Playwright visual audit at 3 breakpoints across all 8 pages</name>
  <files>site/epic/a/index.html, site/epic/a/preise.html, site/epic/a/fahrzeuge.html, site/epic/a/geschaeftskunden.html, site/epic/a/ueber-uns.html, site/epic/a/mitglied-werden.html, site/epic/a/impressum.html, site/epic/a/datenschutz.html</files>
  <action>
**Step 1: Compile final Tailwind CSS**
```bash
cd site && npx @tailwindcss/cli -i epic/a/style.css -o epic/a/tailwind-out.css --content "epic/a/**/*.html"
```

**Step 2: Start a local server**
Use a simple HTTP server to serve the site directory:
```bash
cd site && npx serve -l 5500 &
```
Or use Python: `python -m http.server 5500 --directory site`
The pages will be at `http://localhost:5500/epic/a/index.html` etc.

**Step 3: Screenshot all 8 pages at 3 breakpoints**
Use Playwright (or Puppeteer MCP if available) to capture screenshots:
- Breakpoints: 375px (mobile), 768px (tablet), 1280px (desktop)
- Pages: index, preise, fahrzeuge, geschaeftskunden, ueber-uns, mitglied-werden, impressum, datenschutz
- That's 24 screenshots total

For each screenshot, evaluate:
1. **No horizontal overflow** -- page should not scroll horizontally
2. **Text readability** -- all text visible, proper contrast, no truncation
3. **Hero rendering** (index only) -- full-bleed dark bg, white text visible, CTAs present
4. **Nav rendering** -- desktop: horizontal links visible; mobile: hamburger visible, links hidden
5. **Section separators** -- horizontal rules visible between sections
6. **Footer** -- dark navy, 3-column on desktop, stacked on mobile
7. **Phone CTA** -- visible on desktop (nav), fixed bottom bar on mobile
8. **Map** (fahrzeuge only) -- correct height, no overflow
9. **Pricing** (preise only) -- containers rendered by pricing.js
10. **Accordion** (ueber-uns only) -- buttons present and styled

**Step 4: Fix issues found**
For each issue:
- Identify the HTML file and the specific element
- Apply Tailwind utility fixes (responsive prefixes, overflow-hidden, etc.)
- Common fixes: `overflow-x-hidden` on body, text-wrap adjustments, padding tweaks
- Rebuild Tailwind after fixes

**Step 5: Re-screenshot to verify fixes**
Take fresh screenshots of any pages that had issues to confirm fixes.

**Step 6: Final Tailwind compilation**
```bash
cd site && npx @tailwindcss/cli -i epic/a/style.css -o epic/a/tailwind-out.css --content "epic/a/**/*.html"
```

**Design A structural verification checklist:**
- [ ] Full-bleed immersive hero (NOT split, NOT stacked) on index.html
- [ ] Editorial broadsheet layout (horizontal rules, NOT cards) across all pages
- [ ] Left-aligned reading flow (NOT centered) on content pages
- [ ] Sticky dark navy nav bar (NOT floating pill, NOT light)
- [ ] Storytelling arc on homepage: convenience -> trust -> cost -> action
- [ ] Horizontal typographic rules visible as section separators on all pages
- [ ] Playfair Display serif headings + Inter sans body used consistently
- [ ] Navy/amber palette consistent across all pages
- [ ] No `rounded-full` buttons anywhere (should be `rounded-md`)
- [ ] No card containers (except pricing.js output which is acceptable)

If any structural element is wrong (e.g., cards instead of rules, centered instead of left-aligned), fix it -- these are Design A's identity.
  </action>
  <verify>
    <automated>cd C:/Dev/Repos/TeilAuto/site && npx @tailwindcss/cli -i epic/a/style.css -o epic/a/tailwind-out.css --content "epic/a/**/*.html" 2>&1 | head -3 && for f in index preise fahrzeuge geschaeftskunden ueber-uns mitglied-werden impressum datenschutz; do test -f "epic/a/$f.html" && echo "$f.html exists" || echo "$f.html MISSING"; done</automated>
  </verify>
  <done>All 8 pages audited at 375px, 768px, and 1280px. No layout breaks or horizontal overflow. Navigation functional on all breakpoints. Phone CTA accessible everywhere. Horizontal rule separators visible. Full-bleed hero renders correctly on mobile. Map and pricing integrations work. Final Tailwind CSS compiled.</done>
</task>

</tasks>

<verification>
- All 8 HTML files exist in site/epic/a/
- Tailwind compiles without errors
- No horizontal overflow at any breakpoint
- Nav hamburger works on mobile, full links on desktop
- Phone CTA visible on all breakpoints
- Design A structural identity intact across all pages
- Cross-page links work (nav, footer)
</verification>

<success_criteria>
All 8 pages are production-ready at all 3 breakpoints. Visual audit confirms no layout breaks, correct structural identity (full-bleed hero, editorial rules, left-aligned flow, dark nav), and working JavaScript integrations (pricing, map, accordion, nav toggle).
</success_criteria>

<output>
After completion, create `.planning/phases/27-epic-skin-a/27-05-SUMMARY.md`
</output>
