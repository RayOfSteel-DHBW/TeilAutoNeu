---
phase: 27-epic-skin-a
plan: 07
type: execute
wave: 2
depends_on:
  - 27-06
files_modified: []
autonomous: true
gap_closure: true
requirements:
  - IMPL-05
  - IMPL-06
  - VIS-02
  - VIS-03

must_haves:
  truths:
    - "All 8 pages render without layout breaks at 375px, 768px, and 1280px"
    - "Mobile nav hamburger toggles open and closed"
    - "pricing.js renders pricing data on preise.html"
    - "MapLibre map renders with markers on fahrzeuge.html"
    - "FAQ accordion expands and collapses on ueber-uns.html"
  artifacts: []
  key_links:
    - from: "preise.html"
      to: "../../public/data/pricing.json"
      via: "pricing.js fetch"
      pattern: "pricing-values"
    - from: "fahrzeuge.html"
      to: "MapLibre CDN"
      via: "fleet-map.js"
      pattern: "fleet-map"
---

<objective>
Run Puppeteer MCP visual audit across all 8 Design A pages at 375px, 768px, and 1280px. Verify runtime JS behavior (nav toggle, pricing.js, MapLibre map, FAQ accordion). Fix any issues found.

Purpose: The previous visual audit (27-05) claimed 24 screenshots with zero issues but produced no screenshot artifacts. This gap closure plan actually captures and evaluates screenshots via Puppeteer MCP, and verifies all runtime JavaScript integrations work correctly.
Output: Verified visual correctness at all breakpoints, confirmed JS runtime behavior. Any issues found are fixed in-place.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-epic-skin-a/27-VERIFICATION.md
@.planning/phases/27-epic-skin-a/27-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Puppeteer visual audit -- all 8 pages at 3 breakpoints</name>
  <files></files>
  <action>
**Prerequisites:** The site must be running at http://127.0.0.1:5500/site/epic/a/ via Live Server or equivalent. If not running, inform the user to start it.

**Base URL:** `http://127.0.0.1:5500/site/epic/a/`

**Pages to audit (8 total):**
- index.html
- preise.html
- fahrzeuge.html
- geschaeftskunden.html
- ueber-uns.html
- mitglied-werden.html
- impressum.html
- datenschutz.html

**For each page, at each viewport width (375px, 768px, 1280px):**

1. Use `puppeteer_navigate` to load the page
2. Use `puppeteer_screenshot` at the specified width to capture above-the-fold
3. Use `puppeteer_evaluate` to run `window.scrollTo(0, 800)` then screenshot again for below-the-fold content
4. Continue scrolling (1600, 2400, etc.) until full page is captured, using `document.body.scrollHeight` to determine page length
5. Evaluate each screenshot for:
   - Horizontal overflow (content wider than viewport)
   - Text clipping or truncation
   - Broken layouts (overlapping elements, collapsed containers)
   - Missing content (blank sections, broken images)
   - Nav bar rendering correctly (sticky, dark, all links visible on desktop, hamburger on mobile)
   - Footer rendering correctly

**Breakpoint-specific checks:**
- **375px:** Hamburger menu visible, no horizontal scroll, single column layout, fixed bottom phone bar visible
- **768px:** Transitional layout correct, grid columns appropriate
- **1280px:** Full desktop layout, multi-column grids, sticky nav with all links

**Issue logging:** For each issue found, note the page, breakpoint, scroll position, and description. After completing all screenshots, fix any issues found by editing the HTML/CSS files directly.

After any fixes, rebuild Tailwind:
```bash
cd site && npx @tailwindcss/cli -i epic/a/style.css -o epic/a/tailwind-out.css --content "epic/a/**/*.html"
```

Then re-screenshot the fixed pages to confirm the fix.
  </action>
  <verify>
    <automated>echo "Visual audit is verified by Puppeteer MCP screenshot inspection -- no automated CLI test possible for visual correctness"</automated>
  </verify>
  <done>All 8 pages screenshotted at 375px, 768px, and 1280px. No layout breaks, overflow, or visual regressions found (or all found issues fixed and re-verified).</done>
</task>

<task type="auto">
  <name>Task 2: Runtime JS verification -- nav, pricing, map, accordion</name>
  <files></files>
  <action>
**Prerequisites:** Same running server as Task 1.

**Test 1: Mobile nav toggle**
1. `puppeteer_navigate` to index.html
2. `puppeteer_screenshot` at width=375 (hamburger should be visible)
3. `puppeteer_click` on the hamburger button (selector: `button[aria-controls="primary-nav"]`)
4. `puppeteer_screenshot` -- mobile nav overlay should be visible with all 6 links + phone CTA
5. `puppeteer_click` hamburger again to close
6. `puppeteer_screenshot` -- overlay should be hidden

**Test 2: pricing.js rendering**
1. `puppeteer_navigate` to preise.html at width=1280
2. `puppeteer_screenshot` -- look for rendered pricing content (not empty divs)
3. `puppeteer_evaluate` to check: `document.getElementById('pricing-values').children.length > 0`
4. If empty, check console for fetch errors: `puppeteer_evaluate` with a try-catch around the pricing URL fetch
5. Scroll down to capture pricing-examples and pricing-disclaimer sections

**Test 3: MapLibre map rendering**
1. `puppeteer_navigate` to fahrzeuge.html at width=1280
2. Wait a moment for map tiles to load (use `puppeteer_evaluate` with `await new Promise(r => setTimeout(r, 2000))`)
3. `puppeteer_screenshot` -- map container should show rendered map tiles, not empty/grey box
4. `puppeteer_evaluate` to check: `document.querySelector('.maplibregl-canvas') !== null`

**Test 4: FAQ accordion**
1. `puppeteer_navigate` to ueber-uns.html at width=1280
2. Scroll to FAQ section using `puppeteer_evaluate`: `document.querySelector('[data-accordion]')?.scrollIntoView()` or similar
3. `puppeteer_screenshot` -- FAQ questions should be visible
4. `puppeteer_click` on the first FAQ question button/header
5. `puppeteer_screenshot` -- answer should be expanded/visible
6. `puppeteer_click` again to collapse
7. `puppeteer_screenshot` -- answer should be collapsed

**If any test fails:** Diagnose the root cause, fix the relevant file (HTML or JS), rebuild Tailwind if needed, and re-test.
  </action>
  <verify>
    <automated>echo "Runtime JS verification is confirmed by Puppeteer MCP interaction -- no automated CLI test possible for browser JS behavior"</automated>
  </verify>
  <done>Mobile nav toggle works (open/close). pricing.js renders 3 sections with real data. MapLibre map loads with markers. FAQ accordion expands/collapses on click.</done>
</task>

</tasks>

<verification>
- 24 screenshots captured (8 pages x 3 breakpoints) with no layout issues, or all issues fixed and re-verified
- Nav hamburger opens/closes on mobile viewport
- pricing.js renders non-empty content in pricing-values, pricing-examples, pricing-disclaimer containers
- MapLibre canvas element present and map tiles rendered on fahrzeuge.html
- FAQ accordion responds to clicks on ueber-uns.html
</verification>

<success_criteria>
All 8 Design A pages are visually verified at 375px, 768px, and 1280px with no layout breaks. All 4 JavaScript integrations (nav, pricing, map, accordion) confirmed working at runtime. Phase 27 verification gaps fully closed.
</success_criteria>

<output>
After completion, create `.planning/phases/27-epic-skin-a/27-07-SUMMARY.md`
</output>
