---
phase: 19-secondary-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/geschaeftskunden.html
  - site/src/ueber-uns.html
  - site/src/mitglied-werden.html
autonomous: true
requirements:
  - RFIX-05

must_haves:
  truths:
    - "geschaeftskunden 'Kosteneinsparungen' section is reframed around 'Guenstig' value messaging"
    - "geschaeftskunden insurance section reframed as organizational relief — nobody has to deal with vehicle admin"
    - "geschaeftskunden has a personal CTA section with phone number 07473-922202"
    - "ueber-uns uses the owner's authentic text from the student-project reference"
    - "ueber-uns BCS/Quernutzung section is preserved verbatim"
    - "ueber-uns does not contain 'rund um die Uhr' or any 24/7 availability claim"
    - "mitglied-werden H1 serves dual purpose — covers both how-it-works and joining"
    - "mitglied-werden how-it-works steps use filled circles, join steps use outlined circles"
    - "mitglied-werden join section has id='mitglied-werden' anchor on its H2"
  artifacts:
    - path: "site/src/geschaeftskunden.html"
      provides: "Reframed business content + phone CTA"
      contains: "nstig"
    - path: "site/src/ueber-uns.html"
      provides: "Owner's authentic about-us text"
      contains: "Buchungszentrale"
    - path: "site/src/mitglied-werden.html"
      provides: "Resolved identity + visual step differentiation"
      contains: "id=\"mitglied-werden\""
  key_links:
    - from: "site/src/geschaeftskunden.html"
      to: "tel:+497473922202"
      via: "phone CTA anchor"
      pattern: "tel:\\+497473922202"
    - from: "site/src/mitglied-werden.html"
      to: "join section"
      via: "anchor ID on H2"
      pattern: "id=\"mitglied-werden\""
    - from: "site/src/ueber-uns.html"
      to: "BCS/Quernutzung section"
      via: "preserved verbatim content block"
      pattern: "Bundesverband CarSharing"
---

<objective>
Fix content framing on three secondary pages per joint review issues R24-R27 and R29-R31: reframe geschaeftskunden messaging around value and organizational relief, replace ueber-uns with the owner's authentic text, and resolve mitglied-werden's dual-identity plus visual differentiation.

Purpose: These three pages have content misalignment identified in the Phase 14 joint review. Geschaeftskunden leads with "cost savings" which sounds like layoffs; ueber-uns uses Claude-written generic text instead of the owner's authentic voice; mitglied-werden has confusing identity and visually identical step sequences.
Output: Three updated HTML source files with corrected framing, authentic content, and clear structure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-secondary-pages/19-RESEARCH.md
@.planning/phases/14-joint-review/REVIEW-LOG.md

@site/src/geschaeftskunden.html
@site/src/ueber-uns.html
@site/src/mitglied-werden.html
@references/old/student-project/TA/TA_Website/src/ueber-uns.html
</context>

<interfaces>
<!-- Phone number used across the site (from impressum.html, mitglied-werden.html): -->
<!-- href="tel:+497473922202" display: "07473-922202" -->

<!-- Owner's text from references/old/student-project/TA/TA_Website/src/ueber-uns.html: -->
<!-- 5 paragraphs in Tera template extending base.html -->
<!-- Contains: "Firmeninhaber Ralf Stahl und Ursula Stahl (Buchungszentrale / 'Office-Managerin')" -->
<!-- Contains: "Das Unternehmen entstand auf private Initiative im Jahr 2000" -->
<!-- Contains: Okostadt Tuebingen backstory -->
<!-- Contains: "Im Jahr 2025 sind ueber 60 Fahrtberechtigte angemeldet" -->
<!-- Contains: Zweitwagen use cases paragraph -->
<!-- DANGER: Contains "ist jedoch fast rund um die Uhr moeglich" — MUST NOT be imported (contradicts Mo-Fr 09-12) -->

<!-- Current ueber-uns.html has 3 card sections: -->
<!-- "Unsere Geschichte" (lines 29-45) — REPLACE with owner's text -->
<!-- "Wie wir arbeiten" (lines 47-66) — REPLACE with owner's text -->
<!-- "Mitglied einer groesseren Gemeinschaft" (lines 68-93) — KEEP VERBATIM (BCS/Quernutzung) -->

<!-- Current mitglied-werden.html step circle pattern: -->
<!-- How-it-works (4 steps): class="...rounded-full bg-brand-primary text-lg font-bold text-white..." -->
<!-- Join (3 steps): class="...rounded-full bg-brand-primary text-lg font-bold text-white..." -->
<!-- Both identical — join steps must change to outlined: border-2 border-brand-primary text-brand-primary -->

<!-- Current geschaeftskunden.html benefit cards (lines 34-69): -->
<!-- "Kosteneinsparungen", "Flexibilitaet", "Nachhaltigkeit", "Einfache Verwaltung" -->
<!-- Phase 15 will have already changed border-brand-muted to border-brand-primary/20 on this file -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Reframe geschaeftskunden messaging and add phone CTA (R24, R25, R26)</name>
  <files>site/src/geschaeftskunden.html</files>
  <action>
    Edit `site/src/geschaeftskunden.html` to make three changes:

    **R24 — Reframe "Kosteneinsparungen" around "Guenstig":**
    Change the first benefit card (lines 34-41):
    - H3: Change from "Kosteneinsparungen" to "Guenstig"
    - P: Rewrite body text. Instead of leading with a list of costs (Kauf, Versicherung, etc.), lead with the value message: you only pay when you actually drive. Frame it as: "Kein eigenes Firmenfahrzeug bedeutet: keine Fixkosten, die weiterlaufen, wenn niemand faehrt. Bei teilAuto zahlen Sie nur, was Sie tatsaechlich nutzen &ndash; merklich guenstiger als Kauf, Leasing oder Langzeitmiete." Keep it brief (2-3 sentences max).

    **R25 — Reframe insurance as organizational relief:**
    Change the "Nachhaltigkeit" card (third card, lines 52-60) into an organizational-relief card:
    - H3: Change from "Nachhaltigkeit" to "Wir kuemmern uns um alles rund ums Auto"
    - P: Rewrite to emphasize that nobody in the customer's company has to deal with vehicle admin. Frame it as: "Versicherung, Steuern, TUeV, Wartung, Reifenwechsel &ndash; das alles uebernehmen wir. Fuer Ihr Unternehmen faellt kein Verwaltungsaufwand fuer Firmenfahrzeuge an. Sie buchen, fahren, fertig." Keep it brief (2-3 sentences).

    NOTE: Do NOT remove the "Nachhaltigkeit" messaging entirely — it is a real selling point — but demote it. Move the sustainability argument into a shorter mention at the end of the "Einfache Verwaltung" card's text (last card). Add one sentence like: "Nebenbei: Geteilte Fahrzeuge mit guter Auslastung sind auch ressourcenschonend &ndash; ein Pluspunkt fuer Unternehmen mit Nachhaltigkeitszielen." This way sustainability is acknowledged without being a primary card.

    **R26 — Add personal CTA with phone number:**
    After the closing `</div>` of the "Besonderes fuer Unternehmen" section (after line 109, before the closing `</section>` tag), add a new CTA section:

    ```html
    <div class="rounded-3xl border-2 border-brand-primary bg-brand-primary/5 p-6 text-center shadow-sm sm:p-8">
      <h2 class="text-2xl font-display text-brand-ink">Persoenliche Beratung fuer Ihr Unternehmen</h2>
      <p class="mt-3 text-brand-ink/80">
        Sie moechten wissen, ob Carsharing fuer Ihre Firma passt? Wir beraten Sie gerne &ndash; persoenlich und unverbindlich.
      </p>
      <p class="mt-4 text-3xl font-display text-brand-primary sm:text-4xl">
        <a href="tel:+497473922202" class="hover:underline" data-track="phone-cta" data-track-page="geschaeftskunden">07473-922202</a>
      </p>
      <p class="mt-2 text-sm text-brand-ink/70">
        Mo&ndash;Fr 09:00&ndash;12:00 Uhr
      </p>
    </div>
    ```

    Match the CTA style to the existing mitglied-werden.html phone CTA (lines 143-155) for visual consistency: same `border-2 border-brand-primary bg-brand-primary/5` pattern, same phone number formatting, same hours display.

    IMPORTANT: All German text must use proper HTML entities for umlauts in the Tera template context: use `&uuml;` for ue, `&ouml;` for oe, `&auml;` for ae where the actual source file uses them. Check the existing file for the convention — the current geschaeftskunden.html uses literal UTF-8 characters (e.g. "für", "günstigere"), so continue using literal UTF-8 characters, NOT HTML entities. Use `&ndash;` for dashes as the file already does.
  </action>
  <verify>
    <automated>cd site && npm run build && grep -c "Günstig" src/geschaeftskunden.html && grep -c "kümmern uns" src/geschaeftskunden.html && grep -c "tel:+497473922202" src/geschaeftskunden.html && grep -c "Kosteneinsparungen" src/geschaeftskunden.html | grep "^0$"</automated>
  </verify>
  <done>
    - "Kosteneinsparungen" card renamed to "Günstig" with value-first messaging
    - "Nachhaltigkeit" card replaced with "Wir kümmern uns um alles rund ums Auto" organizational relief card
    - Sustainability mention folded into "Einfache Verwaltung" card as a brief aside
    - Personal phone CTA section added at page bottom with 07473-922202 and hours
    - Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace ueber-uns with owner's authentic text (R27)</name>
  <files>site/src/ueber-uns.html</files>
  <action>
    Edit `site/src/ueber-uns.html` to replace Claude-written sections with the owner's authentic text from the student-project reference.

    **Source text:** Read `references/old/student-project/TA/TA_Website/src/ueber-uns.html` for the 5 paragraphs of owner-written content.

    **What to REPLACE:**

    1. Replace the intro paragraph (lines 23-26) with the owner's opening:
       "teilAuto Mössingen, das sind wir: Firmeninhaber Ralf Stahl und Ursula&nbsp;Stahl (Buchungszentrale / &bdquo;Office-Managerin&ldquo;)."
       Keep the `<p class="mt-4 text-lg text-brand-ink/80">` wrapper and styling.

    2. Replace the "Unsere Geschichte" section (the entire `<div class="rounded-3xl...">` block from lines 29-45) with a new section containing the owner's paragraphs 2 and 3 from the reference:
       - Paragraph 2: "Das Unternehmen entstand auf private Initiative im Jahr 2000..." (Okostadt Tuebingen backstory)
       - Paragraph 3: "Wir gaben der Sache einen offiziellen Rahmen..." (growth to 60+ members)

       Use section heading "Unsere Geschichte" (same as before) but with the owner's authentic text. Keep the same `rounded-3xl border ... bg-white p-6 shadow-sm sm:p-8` card styling.

       NOTE: The border class may have been changed to `border-brand-primary/20` by Phase 15. Use whatever border class is present in the file at execution time. If it's still `border-brand-muted`, use that. If Phase 15 changed it to `border-brand-primary/20`, keep that.

    3. Replace the "Wie wir arbeiten" section (lines 47-66) with the owner's paragraphs 4 and 5:
       - Paragraph 4: "Für die einen ist es die Ausweichmöglichkeit..." (use cases — Zweitwagen, backup)
       - Paragraph 5: "Da wir das Unternehmen nebenberuflich..." (operations — telephonic booking, etc.)

       **CRITICAL SANITIZATION of paragraph 5:** The reference text says "ist jedoch fast rund um die Uhr möglich". This MUST be changed to match the actual availability: "Buchungen erfolgen telefonisch (Mo&ndash;Fr 09:00&ndash;12:00 Uhr)." Rewrite the sentence to preserve the owner's voice while correcting the availability claim. Suggested rewrite of the relevant clause: "das Buchen der Fahrzeuge erfolgt telefonisch zu unseren Bürozeiten (Mo&ndash;Fr 09:00&ndash;12:00 Uhr)"

       Also review paragraph 5 for the future-tense claims about "online-Buchungssystem" and "Bordcomputern" and "gewerbliche Büroräume und Personal" — keep these as they represent the owner's genuine forward-looking vision. They are aspirational, not false claims.

       Use heading "Wie wir arbeiten" (same as before) and the same card styling.

    **What to KEEP VERBATIM:**
    - The "Mitglied einer größeren Gemeinschaft" section (lines 68-93) — BCS/Quernutzung content. Do NOT modify this section at all.

    **What to REMOVE:**
    - The `<p class="mt-3 text-sm text-brand-ink/60">Hinweis: Die genaue Mitgliederzahl...</p>` disclaimer (line 63-65) — no longer needed since we're using the owner's own "über 60 Fahrtberechtigte" figure which IS confirmed.

    **Formatting notes:**
    - Use `&shy;` for long German compound words where helpful
    - Use `&ndash;` for dashes (consistent with rest of site)
    - Use `&nbsp;` between "Ursula" and "Stahl" (as in the reference: `Ursula&nbsp;Stahl`)
    - Use `&bdquo;` and `&ldquo;` for German quotation marks (as in the reference: `&bdquo;Office-Managerin&ldquo;`)
    - Maintain `text-brand-ink/80` paragraph class convention
  </action>
  <verify>
    <automated>cd site && npm run build && grep -c "Buchungszentrale" src/ueber-uns.html && grep -c "rund um die Uhr" src/ueber-uns.html | grep "^0$" && grep -c "Mitglied einer größeren Gemeinschaft" src/ueber-uns.html</automated>
  </verify>
  <done>
    - Intro paragraph uses owner's voice: "teilAuto Mössingen, das sind wir: Firmeninhaber Ralf Stahl und Ursula Stahl..."
    - "Unsere Geschichte" section contains owner's authentic backstory (Okostadt, 2000, 60+ members)
    - "Wie wir arbeiten" section contains owner's authentic operations text with corrected availability hours (Mo-Fr 09:00-12:00, NOT "rund um die Uhr")
    - "Mitglied einer größeren Gemeinschaft" BCS/Quernutzung section preserved verbatim
    - Membership count disclaimer removed (owner's figure is confirmed)
    - Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 3: Resolve mitglied-werden identity and visual differentiation (R29, R30, R31)</name>
  <files>site/src/mitglied-werden.html</files>
  <action>
    Edit `site/src/mitglied-werden.html` to make three changes:

    **R29 — Resolve dual-identity H1:**
    1. Change the `<title>` block (line 1) from:
       `{% block title %}So funktioniert's | teilAuto Mössingen{% endblock title %}`
       to:
       `{% block title %}So funktioniert's &amp; Mitglied werden | teilAuto Mössingen{% endblock title %}`

    2. Change the `<h1>` (line 19) from:
       `<h1 class="text-3xl font-display sm:text-4xl">So funktioniert's</h1>`
       to:
       `<h1 class="text-3xl font-display sm:text-4xl">So funktioniert teilAuto &ndash; und so werden Sie Mitglied</h1>`

    3. Update the `<meta>` description (line 10) and og:title (line 11) to reflect the dual purpose:
       - meta description: "So funktioniert Carsharing bei teilAuto Mössingen: Fahrzeug reservieren, Schlüssel holen, losfahren. In drei Schritten Mitglied werden."
       - og:title: "So funktioniert's & Mitglied werden | teilAuto Mössingen"

    4. Add `id="so-funktionierts"` to the first section's implicit heading area. Since the how-it-works steps don't have a wrapping H2 (they flow directly from the intro paragraph), add a heading before the step grid (before line 27). Insert:
       ```html
       <h2 id="so-funktionierts" class="text-2xl font-display">So funktioniert's</h2>
       ```
       Place it between the closing `</div>` of the intro (line 25) and the opening `<div class="grid gap-6 sm:grid-cols-2">` (line 27). This gives the how-it-works section its own anchored H2.

       Then demote the existing step card H2 headings ("Reservieren", "Abholen", "Fahren", "Zurückbringen" on lines 30, 37, 44, 51) from `<h2>` to `<h3>` to maintain proper heading hierarchy under the new H2.

    **R31 — Add anchor to join section H2:**
    Change the "Mitglied werden" H2 (line 59) from:
    `<h2 class="text-2xl font-display">Mitglied werden</h2>`
    to:
    `<h2 id="mitglied-werden" class="text-2xl font-display">Mitglied werden</h2>`

    Also demote the join step card headings ("Anrufen", "Kennenlernen", "Losfahren" on lines 73, 84, 96) from `<h2>` to `<h3>` for heading hierarchy consistency.

    **R30 — Visual differentiation of step circles:**
    The how-it-works steps (4 cards, lines 29, 36, 43, 50) have circles with class:
    `class="mb-3 flex h-10 w-10 items-center justify-center rounded-full bg-brand-primary text-lg font-bold text-white"`

    Keep these as-is (filled circles).

    The join steps (3 cards, lines 68-70, 79-81, 91-93) have circles with the SAME class. Change each of these three circles from:
    `class="mb-3 flex h-10 w-10 items-center justify-center rounded-full bg-brand-primary text-lg font-bold text-white"`
    to:
    `class="mb-3 flex h-10 w-10 items-center justify-center rounded-full border-2 border-brand-primary text-lg font-bold text-brand-primary"`

    This changes from filled (green bg, white text) to outlined (green border, green text) for the join sequence, making the two step sequences visually distinct.

    **Build and verify:** Run `cd site && npm run build` after all changes.
  </action>
  <verify>
    <automated>cd site && npm run build && grep -c 'id="mitglied-werden"' src/mitglied-werden.html && grep -c 'id="so-funktionierts"' src/mitglied-werden.html && grep -c "border-2 border-brand-primary" src/mitglied-werden.html</automated>
  </verify>
  <done>
    - H1 reads "So funktioniert teilAuto -- und so werden Sie Mitglied" (dual purpose)
    - Page title updated to include both purposes
    - H2 with id="so-funktionierts" added before how-it-works steps
    - H2 with id="mitglied-werden" anchors the join section
    - How-it-works steps: filled green circles (unchanged)
    - Join steps: outlined green circles (border-2, no bg fill)
    - Step card headings demoted to H3 for proper hierarchy
    - Build succeeds
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:

1. Build: `cd site && npm run build` completes without errors
2. geschaeftskunden checks:
   - `grep "Günstig" site/src/geschaeftskunden.html` returns a hit
   - `grep "kümmern uns" site/src/geschaeftskunden.html` returns a hit
   - `grep "tel:+497473922202" site/src/geschaeftskunden.html` returns a hit (phone CTA)
   - `grep "Kosteneinsparungen" site/src/geschaeftskunden.html` returns zero hits (old heading gone)
3. ueber-uns checks:
   - `grep "Buchungszentrale" site/src/ueber-uns.html` returns a hit (owner's text present)
   - `grep "rund um die Uhr" site/src/ueber-uns.html` returns zero hits (stale claim removed)
   - `grep "Mitglied einer größeren Gemeinschaft" site/src/ueber-uns.html` returns a hit (BCS section preserved)
4. mitglied-werden checks:
   - `grep 'id="mitglied-werden"' site/src/mitglied-werden.html` returns a hit
   - `grep 'id="so-funktionierts"' site/src/mitglied-werden.html` returns a hit
   - `grep "border-2 border-brand-primary" site/src/mitglied-werden.html` returns 3 hits (join circles)
5. Puppeteer screenshots at 1280px and 375px confirm all three pages render cleanly
</verification>

<success_criteria>
- geschaeftskunden.html: "Günstig" replaces "Kosteneinsparungen", insurance card reframed as organizational relief, phone CTA with 07473-922202 and hours at page bottom
- ueber-uns.html: owner's authentic text from student-project reference used for intro/history/operations sections, BCS/Quernutzung section untouched, zero instances of "rund um die Uhr"
- mitglied-werden.html: dual-purpose H1, anchored H2s (id="so-funktionierts" and id="mitglied-werden"), how-it-works filled circles vs join outlined circles, H3s for step card headings
- `npm run build` succeeds without errors
- Puppeteer screenshots at 1280px and 375px confirm all three pages clean
</success_criteria>

<output>
After completion, create `.planning/phases/19-secondary-pages/19-01-SUMMARY.md`
</output>
