---
phase: 10-content-messaging-feature-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/index.html
  - site/src/mitglied-werden.html
  - site/src/base.css
  - site/public/img/logo.svg
autonomous: true
requirements:
  - CONT-01
  - CONT-07
  - UX-08

must_haves:
  truths:
    - "Homepage hero fills the first screen (100vh minus header), showing headline, logo, and two CTA buttons — nothing else visible above the fold"
    - "Clicking 'Noch unsicher?' smooth-scrolls past the hero to below-fold content"
    - "Clicking 'Mehr erfahren' navigates to mitglied-werden.html"
    - "Info cards ('Kurze Wege' / 'Gemeinschaftlich statt privat') appear below the fold with revised text, not inside the hero"
    - "Value-prop cards (Sparsam / Flexibel / Lokal) remain below fold with revised text emphasising low monthly cost and realistic 2-car flexibility"
    - "'So funktioniert teilAuto' section no longer appears on the homepage"
    - "mitglied-werden.html has a 'So funktioniert's' section before the 3-step join flow"
    - "'Sie werden Teil einer Gemeinschaft' messaging is removed and replaced with practical/transactional framing"
    - "Phone CTA (07473-922202) remains at the bottom of mitglied-werden.html"
    - "Homepage messaging works for all 6 personas without explicit segmentation tiles"
  artifacts:
    - path: "site/public/img/logo.svg"
      provides: "Brand logo SVG for hero section"
    - path: "site/src/index.html"
      provides: "Redesigned homepage with 100vh hero and revised below-fold sections"
    - path: "site/src/mitglied-werden.html"
      provides: "Hybrid 'So funktioniert's' + membership page"
    - path: "site/src/base.css"
      provides: "Smooth scroll behaviour with reduced-motion respect"
  key_links:
    - from: "site/src/index.html"
      to: "site/src/mitglied-werden.html"
      via: "Mehr erfahren CTA href"
      pattern: 'href="mitglied-werden.html"'
    - from: "site/src/index.html"
      to: "#below-hero"
      via: "Noch unsicher? anchor link"
      pattern: 'href="#below-hero"'
---

<objective>
Redesign the homepage hero to fill the viewport (100vh), add SVG logo, dual CTA buttons, move info cards below the fold with revised text, remove "So funktioniert teilAuto" section (content moves to mitglied-werden), and expand mitglied-werden.html into a hybrid "So funktioniert's" + membership page with corrected messaging.

Purpose: Close gaps CONT-01 (homepage hero/value prop), CONT-07 (membership info), and UX-08 (persona-inclusive messaging) from the v1.0 audit.
Output: Revised index.html, expanded mitglied-werden.html, new logo.svg, smooth scroll CSS.
</objective>

<execution_context>
@.planning/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-content-messaging-feature-polish/10-CONTEXT.md
@.planning/phases/10-content-messaging-feature-polish/10-RESEARCH.md

@site/src/index.html
@site/src/mitglied-werden.html
@site/templates/base.html
@site/templates/partials/header.html
@site/src/base.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Homepage hero redesign and below-fold restructure</name>
  <files>site/src/index.html, site/public/img/logo.svg, site/src/base.css</files>
  <action>
**Create logo SVG** at `site/public/img/logo.svg`:
- Simple typographic SVG rendering "teilAuto" in system-ui sans-serif font (NOT Space Grotesk — SVG loaded as `<img>` cannot access page fonts)
- Use brand colour `#0f5f3c` for fill
- Use `font-family="system-ui, -apple-system, sans-serif"` in the `<text>` element
- ViewBox approximately 160x48, with role="img" and aria-label="teilAuto Mössingen"
- Keep it minimal — owner may supply a designed version later

**Rewrite index.html hero section** (the first `<section>` after `{% block main %}`):
- Replace the entire existing hero section (lines 17-89 in current file) with a 100vh hero
- Hero wrapper: `<section class="flex min-h-[calc(100vh-4rem)] flex-col items-center justify-center px-6 py-12 text-center">` — no card styling (no rounded border, no bg-white, no shadow), just the centered content on the page background
- Inside the hero, vertically centered:
  1. `<img src="/img/logo.svg" alt="teilAuto Mössingen" class="mx-auto mb-6 h-16 w-auto">`
  2. Keep existing h1: "Die sparsame Art (k)ein Auto zu haben" — centred, same font classes
  3. Subtitle paragraph: "Carsharing für Mössingen und die Umgebung — flexibel, lokal verankert, ohne eigenes Auto." — centred, `text-lg text-brand-ink/80 mt-4 max-w-xl mx-auto`
  4. Dual CTA buttons in a flex wrapper (`mt-8 flex flex-wrap items-center justify-center gap-4`):
     - "Mehr erfahren" — primary filled button: `rounded-full bg-brand-primary px-6 py-3 text-base font-semibold text-white shadow-sm transition hover:bg-brand-accent` with `href="mitglied-werden.html"` and `data-track="nav-cta" data-track-label="mehr-erfahren"`
     - "Noch unsicher?" — outlined secondary button: `rounded-full border border-brand-primary px-6 py-3 text-base font-semibold text-brand-primary transition hover:bg-brand-muted` with `href="#below-hero"` and `data-track="nav-cta" data-track-label="noch-unsicher"`
- Remove the `<span>` helper text ("Alle Details zum Mitgliedwerden...") from the hero
- Keep the `data-scroll-milestone="hero-bottom"` sentinel before the hero section

**Add anchor target** after the hero section:
- `<div id="below-hero" aria-hidden="true"></div>`

**Move info cards below fold** — the two cards ("Kurze Wege, klare Organisation" and "Gemeinschaftlich statt privat") currently inside the hero's right column move to a new section immediately after the `#below-hero` anchor:
- Wrap in a section: `<section class="mt-12"><div class="grid gap-6 md:grid-cols-2">`
- Card 1 revised text:
  - Title: "Kurze Wege, klare Organisation"
  - Body: "Die Fahrzeuge stehen an festen Standorten in Mössingen — nah am Wohnort, immer am selben Platz. Buchung und Abrechnung laufen unkompliziert über einen Anruf."
- Card 2 revised text:
  - Title: "Teilen statt besitzen"
  - Body (revised from "Gemeinschaftlich statt privat"): "Mehrere Haushalte teilen sich ein Auto und sparen Versicherung, Werkstatt, TÜV und Stellplatz. Sie zahlen nur, wenn Sie tatsächlich fahren."
- Use the same card styling as the value-prop cards: `rounded-3xl border border-brand-muted bg-white p-6 shadow-sm`

**Revise value-prop cards** (the existing "Sparsam bleiben" / "Flexibel unterwegs" / "Lokal verankert" section):
- Keep the 3-column grid structure and section class unchanged
- "Sparsam bleiben" revised body: "Kein eigenes Auto bedeutet: keine Versicherung, keine Werkstattkosten, kein TÜV. Bei teilAuto zahlen Sie einen günstigen Jahresbeitrag und nur die Fahrten, die Sie wirklich machen."
- "Flexibel unterwegs" revised body: "Zwei Fahrzeuge stehen bereit — vom Kleinwagen für die Besorgung bis zum Kompakt-SUV für den Familienausflug. Reservieren, einsteigen, losfahren."
- "Lokal verankert" — keep as-is (text is already good)

**Remove "So funktioniert teilAuto" section** — delete the entire section (lines 118-139 in current file: the `<section>` containing `<h2>So funktioniert teilAuto</h2>` and the "Mehr zur Mitgliedschaft" link). This content moves to mitglied-werden.html.

**Keep** the `{% include "accordion.html" %}` at the bottom unchanged.

**Add smooth scroll CSS** to `site/src/base.css` — at the end of the file, add:
```css
html {
  scroll-behavior: smooth;
}

@media (prefers-reduced-motion: reduce) {
  html {
    scroll-behavior: auto;
  }
}
```

Do NOT add `scroll-behavior` if it already exists in base.css (check first).
  </action>
  <verify>
Run `npm run build` in the `site/` directory. Verify:
1. `site/build/dist/index.html` contains `min-h-[calc(100vh-4rem)]` in hero section
2. `site/build/dist/index.html` contains `id="below-hero"` anchor
3. `site/build/dist/index.html` does NOT contain "So funktioniert teilAuto"
4. `site/build/dist/index.html` contains `href="#below-hero"` on "Noch unsicher?" button
5. `site/public/img/logo.svg` exists and is valid SVG
6. `site/src/base.css` contains `scroll-behavior: smooth`
  </verify>
  <done>
Homepage hero fills viewport with logo + headline + dual CTAs. Info cards appear below fold with revised text. "So funktioniert" section removed. Value-prop card text revised for accuracy. Smooth scroll enabled with reduced-motion fallback. Logo SVG created.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand mitglied-werden.html to hybrid "So funktioniert's" + membership page</name>
  <files>site/src/mitglied-werden.html</files>
  <action>
**Rewrite mitglied-werden.html** keeping the same Tera structure (`{% extends "base.html" %}`, blocks for title/header/head/main):

**Page title**: Change `{% block title %}` to: `So funktioniert's | teilAuto Mössingen`

**Meta description**: Update to: "So funktioniert Carsharing bei teilAuto Mössingen: Fahrzeug reservieren, Schlüssel holen, losfahren. Mitglied werden in drei Schritten."

**OG title**: "So funktioniert's | teilAuto Mössingen"
**OG description**: "Carsharing im Alltag: Reservieren, Schlüssel holen, losfahren. Mitglied werden bei teilAuto Mössingen — persönlich und unkompliziert."
**OG URL**: Keep `mitglied-werden.html` (URL stays the same)

**Keep** `{% set current_page = "mitglied-werden" %}` unchanged (nav does not link here, but belt-and-suspenders).

**Page content** (inside `{% block main %}`):

1. **H1 and intro** — new headline and intro:
   ```html
   <h1 class="text-3xl font-display sm:text-4xl">So funktioniert's</h1>
   <p class="mt-4 text-lg text-brand-ink/80">
     Carsharing bei teilAuto ist unkompliziert: Sie reservieren telefonisch ein Fahrzeug,
     holen den Schlüssel am Standort ab und fahren los. Nach der Fahrt stellen Sie das
     Auto an denselben Platz zurück. Der Alltag mit teilAuto in Kürze:
   </p>
   ```

2. **"So funktioniert's" section** — new content (Claude's discretion on exact wording, but must cover these points):
   - 3-4 items in a styled list or card layout explaining carsharing day-to-day:
     - **Reservieren**: Telefonisch ein Fahrzeug für den gewünschten Zeitraum buchen (Mo–Fr 09:00–12:00)
     - **Abholen**: Schlüssel am festen Standort abholen — die Fahrzeuge stehen immer am selben Platz
     - **Fahren**: Fahrzeug nutzen wie Ihr eigenes. Kraftstoff, Versicherung und Betriebsstoffe sind im Preis enthalten
     - **Zurückbringen**: Auto an denselben Standort zurückstellen. Abgerechnet wird nach Zeit und Kilometern
   - Use a 2x2 grid or vertical list with numbered steps — consistent with existing card styling
   - Keep under 150 words total for this section

3. **Section divider** — a simple heading to transition:
   ```html
   <h2 class="text-2xl font-display">Mitglied werden</h2>
   <p class="mt-3 text-brand-ink/80">
     Die Aufnahme läuft persönlich — kein Online-Formular, kein Papierkram.
     Drei Schritte genügen:
   </p>
   ```

4. **3-step join flow** — keep the existing 3 cards (Anrufen / Kennenlernen / Losfahren) exactly as they are in the current file, including the numbered circles and text

5. **"Was Sie wissen sollten" section** — keep the section but **fix the messaging**:
   - REMOVE the first list item: `teilAuto ist ein Mitgliedsmodell — keine Einmalmiete. Sie werden Teil einer Gemeinschaft.`
   - REPLACE with: `Mitgliedschaft heißt Zugang zu Fahrzeugen, klare Regeln und faire Kosten — kein Abo mit versteckten Gebühren.`
   - Keep the remaining three items unchanged (fixed stations, phone booking, costs)

6. **Phone CTA block** — keep unchanged at the bottom (the green-bordered card with 07473-922202, Mo–Fr 09:00–12:00)
  </action>
  <verify>
Run `npm run build`. Verify:
1. `site/build/dist/mitglied-werden.html` contains "So funktioniert's" as h1
2. The page does NOT contain "Sie werden Teil einer Gemeinschaft"
3. The page contains "Mitgliedschaft heißt Zugang zu Fahrzeugen"
4. Phone CTA (07473-922202) is present at bottom of page
5. The 3-step flow (Anrufen / Kennenlernen / Losfahren) is present
6. Meta description updated
  </verify>
  <done>
mitglied-werden.html is now a hybrid "So funktioniert's" + membership page. "Gemeinschaft" framing removed, replaced with practical/transactional messaging. Homepage "Mehr erfahren" CTA links here. Day-to-day carsharing explanation precedes the join flow.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run build` in `site/` — build succeeds without errors
2. Open `site/build/dist/index.html` in browser:
   - Hero fills viewport, shows logo + headline + 2 CTA buttons
   - "Noch unsicher?" scrolls smoothly to below-fold content
   - "Mehr erfahren" links to mitglied-werden.html
   - No "So funktioniert teilAuto" section exists on homepage
   - Info cards and value-prop cards appear below fold with revised text
3. Open `site/build/dist/mitglied-werden.html`:
   - Page starts with "So funktioniert's" section
   - 3-step join flow follows
   - No "Gemeinschaft" framing in "Was Sie wissen sollten"
   - Phone CTA at bottom
4. Grep all HTML source files for "Sie werden Teil einer Gemeinschaft" — zero results
5. `site/public/img/logo.svg` renders in browser without font issues
</verification>

<success_criteria>
- Homepage hero is 100vh with SVG logo, headline, and dual CTA buttons
- "Noch unsicher?" smooth-scrolls past hero; "Mehr erfahren" links to membership page
- Info cards below fold have revised text; value-prop cards revised for accuracy
- "So funktioniert teilAuto" section removed from homepage
- mitglied-werden.html expanded with day-to-day carsharing section before join flow
- "Gemeinschaft" messaging replaced with practical framing
- Build succeeds; all 6 persona types served by general messaging (no segmentation tiles)
</success_criteria>

<output>
After completion, create `.planning/phases/10-content-messaging-feature-polish/10-01-SUMMARY.md`
</output>
